---
description: "Task list for Supabase → Postgres/Blob Application Migration (on the 001 local substrate)"
---

# Tasks: Supabase → Postgres/Blob Application Migration

**Input**: Design documents from `/specs/002-supabase-postgres-migration/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/, quickstart.md; **the 001
local substrate running** (`repostories/orbit` — `npm run local:up`: Docker pg17 + Azurite).

**Scope**: The **application-layer migration off Supabase**, built and validated against the 001 local
substrate. Real Azure resources (Terraform, PG Flexible Server, private networking, Managed Identity,
Key Vault), the Auth0 **production** tenant, and the production data-sync/cutover are **spec 004** —
not here (per spec Clarifications).

**Tests**: Validation tasks (isolation suite, auth E2E, trigger-behaviour, no-supabase check) are
**acceptance criteria** in the spec, not optional TDD.

**Paths**: all in the **Orbit application repo** (`repostories/orbit/`). The substrate lives there too
(`docker-compose.local.yml`, `scripts/local/` from 001).

**Labels**: `[US#]` = user story.

## Phase 1: Setup

- [X] T001 Add dependencies to `repostories/orbit/package.json`: Drizzle ORM + a Postgres driver (`postgres`/`node-postgres`), `@auth0/nextjs-auth0` v4. (`pg`, `@azure/storage-blob`, `dotenv` already present from 001.) Add the npm scripts the quickstart uses: `local:replay` → `scripts/local/db-replay.sh`, `check:no-supabase` → `scripts/check-no-supabase.sh`, `test:isolation`, `test:auth`, `test:storage`. Scaffold `db/`, `lib/auth/`, `lib/storage/` (no `infra/terraform/` — that is 004)
- [X] T002 [P] Add a CI static-analysis check that fails on any `supabase-js` / Supabase storage / Realtime import in `scripts/check-no-supabase.sh` (SC-005, NFR-003)
- [X] T003 [P] Add Auth0 dev-tenant config + local Postgres/Azurite connection values to `.env.local.example` (dev-only; the production tenant + real secrets are 004)

**Checkpoint**: deps + dev config ready; the 001 substrate comes up with `npm run local:up`.

---

## Phase 2: Foundational (Blocking Prerequisites)

**⚠️ No user story can proceed until schema + Drizzle + the RLS primitive exist on the substrate.**

- [X] T004 Author `repostories/orbit/scripts/local/db-replay.sh` — **compatibility shim then replay**: pre-seed roles (`postgres`, `anon`, `authenticated`, `service_role`) + schemas (`auth`, `storage`, `extensions`, `supabase_functions`) + stub `auth.uid()/auth.role()/auth.jwt()` + a no-op `supabase_functions.http_request` + `pgcrypto` into `extensions`; then apply every file in `supabase/migrations/` in timestamp order via `psql -v ON_ERROR_STOP=1`; **parameterized by `$DATABASE_URL`** so 004 reuses it against Azure (FR-002, research R8)
- [X] T005 Run the replay onto the empty substrate → all 88 tables present, `vector`/`pg_trgm`/`pgcrypto` enabled, roles/schemas created; **measure** the table/object counts and reconcile against the 88-table baseline the migrations define (offline — no Supabase access) (FR-001, FR-002, SC-004)
- [X] T006 [P] Verify ported database triggers + stored functions behave against representative inputs on the substrate (FR-010)
- [X] T007 Drizzle client + bounded connection pool, schema introspected from the replayed substrate DB, in `db/client.ts` and `db/schema.ts` (research R9, NFR-002)
- [X] T008 Implement the RLS session-identity primitive (`SET LOCAL app.user_id` per transaction, reset on release) in `db/client.ts` (contract `rls-session.md`, research R6)

**Checkpoint**: schema landed + verified on the substrate; Drizzle + RLS session primitive ready.

---

## Phase 3: User Story 1 - Application data served through Drizzle (Priority: P1) 🎯 core

**Goal**: All reads/writes on the substrate via Drizzle; zero `supabase-js` remains.

**Independent Test**: full suite + no-supabase check pass against the substrate (SC-001, SC-005).

- [X] T009 [US1] Pilot-rewrite one module's DB calls to Drizzle against the substrate and measure throughput + defect rate (research R12, NFR-004)
- [X] T010 [US1] From the pilot, define the module/table **partition plan** (batch list + owner per batch) in `docs/rewrite-partitions.md` — decomposes the ~5,100-call rewrite for parallel automation (FR-3, A1)
- [ ] T011 [US1] Execute the batched Drizzle rewrite across all partitions (~5,100 query calls + 46 stored-procedure calls), tracked per batch, across `src/**` (FR-003)
  - audiences/** leaf **COMPLETE**: preview/create/sync/update/delete/remove-contact/[id]contacts (Wave B/C) + preset/starters, preset/declining, ai-build (field-vocab read) now all on Drizzle+withUserId; whole dir has zero Supabase client refs (`npm run check:no-supabase:audiences`). Verified on the substrate over two orgs incl. cross-tenant isolation: `scripts/local/wave-b-routes-check.ts` + `scripts/local/wave-d-preset-routes-check.ts`. Follow-up: the four migrated-route jest suites (create, sync, starters, ai-build) are import-red under the `@/db/client` DATABASE_URL guard and assert the retired supabase path — rewrite them against the substrate in a consolidated task (harness is the interim verification).
  - ai/context industry-loader leaf **COMPLETE**: `src/lib/ai/context/industry.ts` (`loadIndustryContext`) repointed off its module-level service-role `createClient` onto the Drizzle twins (`load-template.drizzle`). **Classified admin→`db`, NOT `withUserId`**: no session identity reaches this leaf (callers pass only `orgId`/`OrgContext`, never a sub), so it preserves the prior service-role trust level; tenant scope is the explicit `organizations WHERE id = orgId` predicate + non-tenant `industry_template_configs` reference read. `IndustryContext`/`toPromptString()` byte-identical; whole-project `tsc --noEmit` green (all ~20 downstream callers typecheck). Verified on the substrate over two verticals (membership/non_profit) + a third key-branch vertical incl. cross-tenant no-bleed and an unknown-org-id tripwire: `scripts/local/industry-context-check.ts` (11/11, 0 defects). `industry.ts` itself now has 0 Supabase refs; 9 sibling partitions in the dir (engagement/brand/audience/contact-fields/industry-examples/brief/event/profile/org) remain for later leaves.
  - ai/context org-loader leaf **COMPLETE**: `src/lib/ai/context/org.ts` (`loadOrgContext`) repointed off its module-level service-role `createClient` onto a new Drizzle twin `src/lib/ai/context/org.drizzle.ts` (`loadOrgContextRow`, mirroring `load-template.drizzle`'s snake_case-alias + throw→try/catch + `limit(1)` hazards, preserving the original `OrgContext: failed to load org …` message). **Classified admin→`db`, NOT `withUserId`** (same rationale as the industry leaf: callers pass only `orgId`, no session sub; scope is the explicit `organizations WHERE id = orgId` PK predicate). `OrgContext` shape + `toPromptString()` byte-identical (all 14 fields incl. `timezone`/`consentModel` fallbacks preserved); whole-project `tsc --noEmit` green (all ~20 callers incl. `loadIndustryContext`'s OrgContext key-branch, sms/social generate, composeAgentPrompt). Verified on the substrate over two verticals incl. per-org correctness, cross-tenant no-bleed (unit + end-to-end through `loadIndustryContext`), and an unknown-org-id tripwire: `scripts/local/org-context-check.ts` (16/16, 0 defects). `org.ts` + `org.drizzle.ts` have 0 Supabase refs; 8 sibling partitions in the dir (engagement/brand/audience/contact-fields/industry-examples/brief/event/profile) remain for later leaves.
  - ai/context brand-loader leaf **COMPLETE**: `src/lib/ai/context/brand.ts` (`loadBrandContext`/`loadBrandContextResult`) repointed off its module-level service-role `createClient` onto a new Drizzle twin `src/lib/ai/context/brand.drizzle.ts` covering all THREE org-scoped reads (`brand_settings`, `organisation_profile`, `content_assets` banner resolution). The twin deliberately **returns the supabase-js `{ data, error }` shape** (not throw) so the caller's configured/unconfigured/failed **state machine + palette/voice mapping + `toPromptString` stay byte-identical** — only the 3 data-source sites changed; the pg SQLSTATE `.code` is preserved for the failure logger. **Classified admin→`db`, NOT `withUserId`** (callers pass only `orgId`, no session sub): every read is scoped by the explicit `org_id = orgId` predicate, and banner resolution additionally by `id IN (…)` — the same ownership predicate the prior service-role client relied on. Whole-project `tsc --noEmit` green (all ~35 `loadBrandContext*` callers incl. sms/social generate, email-context/load, composeAgentPrompt, forms-v2, public form/landing pages). Verified on the substrate over two orgs: per-org palette, profile-wins voice/tone/tagline fallback, **banner org-scoping** (org-B pointing at org-A's asset id resolves to null + `banner_asset_missing`, proving the `org_id`+`id IN` guard blocks cross-tenant asset leak), no-bleed, and an unknown-org tripwire (safe `unconfigured` default, never a neighbour's brand): `scripts/local/brand-context-check.ts` (16/16, 0 defects). `brand.ts` + `brand.drizzle.ts` have 0 Supabase refs; 7 sibling partitions in the dir (engagement/audience/contact-fields/industry-examples/brief/event/profile) remain for later leaves.
  - ai/context engagement-loader leaf **COMPLETE**: `src/lib/ai/context/engagement.ts` (`loadEngagementContext`) repointed off its module-level service-role `createClient` onto a new Drizzle twin `src/lib/ai/context/engagement.drizzle.ts` covering all SIX contact-scoped reads (`campaign_sends` recent + 3 aggregate `count()`s, `contact_activities`, `webinar_registrants`, `journey_enrollments`, `campaign_conversions`), each with its PostgREST-style embed (`campaigns`/`webinars`/`journeys`) rebuilt as an explicit `leftJoin`. Unlike the brand leaf, the loader runs **no** `{data,error}` state machine — it reads `.data ?? []` under one outer try/catch — so the twin **throws** (like `org.drizzle.ts`) and the loader's existing `catch` degrades to the honest `EMPTY_ENGAGEMENT` "(No recorded engagement signals …)" path. **Classified admin→`db`, NOT `withUserId`** (sole caller `insights/engagement-summary/generate.ts` passes only `(contactId, orgId)`, no session sub): every read is scoped by the explicit `contact_id = contactId AND org_id = orgId` predicate — the org_id half **hardens** the three reads (`campaign_sends`/`webinar_registrants`/`campaign_conversions`) the supabase-js version filtered by `contact_id` alone, which on the no-RLS admin handle would otherwise return a mismatched org's rows for a `(contact, wrong-org)` call. `EngagementContext` shape + `toPromptString()` byte-identical (mapping loops untouched; #915 true-aggregate vs capped-recent invariant preserved); whole-project `tsc --noEmit` green. Regression test `engagement.test.ts` reworked to mock the twin instead of supabase-js (2/2 green). Verified on the substrate over two orgs + a second same-org contact: per-org correctness across all six categories, **contact-by-id** scoping (same-org second contact's sends do NOT inflate the aggregate — still 7), no cross-tenant bleed (distinct aggregates; org-B's form/journey/webinar never in org-A's prompt), an **org-scoping tripwire** (contactA under wrong org B → `exists=false`, 0 sends — a `contact_id`-only port would leak here), and an unknown-id tripwire: `scripts/local/engagement-context-check.ts` (26/26, 0 defects). `engagement.ts` + `engagement.drizzle.ts` have 0 Supabase refs; 6 sibling partitions in the dir (audience/contact-fields/industry-examples/brief/event/profile) remain for later leaves. Noted risk: `campaign_sends.org_id` / `webinar_registrants.org_id` are nullable — a legacy row with `NULL org_id` would be dropped by the new predicate (soft under-count → honest degrade, never a leak or a 500); writers populate org_id, so this is introspection slack, not an org-less send.
  - ai/context profile-loader leaf **COMPLETE**: `src/lib/ai/context/profile.ts` (`loadProfileContext`) repointed off its module-level service-role `createClient` onto a new Drizzle twin `src/lib/ai/context/profile.drizzle.ts` covering both org-scoped reads (`organisation_profile` single row + `voice_samples` capped list). Like the engagement leaf (and unlike brand), the loader runs **no** `{data,error}` state machine — it read `res.data` / `res.data ?? []` and never inspected `.error` — so the twin **throws** (like `org.drizzle.ts`/`engagement.drizzle.ts`) and a **new single outer try/catch** degrades to the honest `EMPTY_PROFILE` (`exists=false` → `toPromptString()===''`) path, exactly what the old code produced when both `.data` were null. `.maybeSingle()`→`.limit(1)+rows[0]??null`; snake_case-aliased selects keep the mapping (`profile?.about_paragraph`, `s.source_type`, the jsonb coercers) byte-identical. **Classified admin→`db`, NOT `withUserId`**: all EIGHT call sites pass only `orgId` (insights engagement-summary/generate, email-context/load, composeAgentPrompt, forms-v2 generate/mapping/landing-pages generate+switch-layout, events copy-grounding), no session sub — nothing to seed `app.user_id` with, and the tables' RLS policies key off Supabase `auth.uid()` (unreachable via `withUserId`), so admin `db` + explicit predicate is the only working equivalent to the prior service-role trust level. Both reads scoped by the explicit `org_id = orgId` predicate; **both `org_id` columns are `NOT NULL`** (no nullable-org under-count risk — strictly stronger than the engagement leaf). `ProfileContext` shape + `toPromptString()` + the `#916` untrusted voice-sample fencing byte-identical; whole-project `tsc --noEmit` green (all downstream callers incl. insights/engagement-summary/generate typecheck). Verified on the substrate over two orgs with disjoint `ORGA_*`/`ORGB_*` figures: per-org correctness across about/audience/value/tiers/programmes/terminology/key-links + own voice samples (orgA 2, orgB 1), no cross-tenant bleed (distinct fields; orgA `toPromptString` leaks no `ORGB_` sentinel), and an unknown-org-id tripwire (`exists=false`, empty prompt, no borrowed rows): `scripts/local/profile-context-check.ts` (27/27, 0 defects). `profile.ts` + `profile.drizzle.ts` have 0 Supabase refs; 5 sibling partitions in the dir (audience/contact-fields/industry-examples/brief/event) remain for later leaves.
  - ai/context brief-loader leaf **COMPLETE**: `src/lib/ai/context/brief.ts` (`loadBriefContext`) repointed off its module-level service-role `createClient` onto a new Drizzle twin `src/lib/ai/context/brief.drizzle.ts` covering the single event-scoped read (`event_briefs`, 17 columns). Like the profile/engagement leaves (and unlike brand), the loader runs **no** real `{data,error}` state machine — it read `{data,error}` but collapsed BOTH `error` and no-row to the same `EMPTY_BRIEF_CONTEXT` (`if (error || !brief) return EMPTY…`) — so the twin **throws** (like `org.drizzle.ts`) and the loader's **new outer try/catch** + retained `!brief` guard reproduce that collapse byte-for-byte. `.maybeSingle()`→`.limit(1)+rows[0]??null`; snake_case-aliased select keeps the mapping (`brief.audience_description`, `brief.do_not_mention`, the `#916` `ADDITIONAL_NOTES`/`PASTED_BRIEF` fences) byte-identical. **Classified admin→`db`, NOT `withUserId`**: the sole real caller `ai/email-context/load.ts` passes only `eventId` (a webinar id), no session sub. **SCOPE is `webinar_id = eventId`, deliberately NOT org_id-hardened** (contrast the engagement leaf): (a) no orgId reaches this loader — the signature is `loadBriefContext(eventId)`, eventId-only, so there is nothing to add an `org_id` predicate against without a signature change; and (b) `webinar_id` is **globally UNIQUE** (`event_briefs_webinar_id_key`) and FK'd to `webinars.id` (a UUID PK), so one eventId resolves to exactly one org's brief — a shared child id cannot straddle tenants, making `webinar_id` alone an unambiguous, non-leaking key (the same predicate the prior service-role client relied on). `BriefContext` shape + `toPromptString()` byte-identical; whole-project `tsc --noEmit` green (callers: email-context/load, email-generate type-only import). Verified on the substrate over two orgs each owning one webinar + one brief with disjoint `ORGA_*`/`ORGB_*` sentinels: per-event correctness across universal + webinar-specific (orgA: speaker/format/recording) + in-person-specific (orgB: venue/transport/networking) fields, no cross-tenant bleed (eventA `toPromptString` leaks no `ORGB_`; eventB's venue never on eventA), an unknown-event-id tripwire (`exists=false`, empty prompt), and a null-eventId early-return tripwire: `scripts/local/brief-context-check.ts` (28/28, 0 defects). `brief.ts` + `brief.drizzle.ts` have 0 Supabase refs; 4 sibling partitions in the dir (audience/contact-fields/industry-examples/event) remain for later leaves.
  - ai/context event-loader leaf **COMPLETE**: `src/lib/ai/context/event.ts` (`loadEventContext`) repointed off its module-level service-role `createClient` onto a new Drizzle twin `src/lib/ai/context/event.drizzle.ts` covering the single event-scoped read (`webinars` by PK, 11 real columns). Like the brief/profile/engagement leaves (and unlike brand), the loader runs **no** real `{data,error}` state machine — it read `{data,error}` but collapsed BOTH `error` and no-row to the same `EMPTY_EVENT_CONTEXT` (`if (error || !event) return EMPTY…`) — so the twin **throws** (like `org.drizzle.ts`) and the loader's **new outer try/catch** + retained `!event` guard reproduce that collapse byte-for-byte. `.maybeSingle()`→`.limit(1)+rows[0]??null`; snake_case-aliased select keeps the mapping + the URL-never-in-prompt split (`registrationUrl`/`recordingUrl` typed fields + `hasRegistrationUrl`/`hasRecordingUrl` booleans, raw URLs excluded from `toPromptString`) byte-identical. **Classified admin→`db`, NOT `withUserId`**: every runtime caller (`events/commit-ai-draft`, `events/[id]/add-invitations-journey`, `events/load-event-generation-context`, `ai/email-context/load`) passes only a webinar id, no session sub. **SCOPE is `id = eventId` (the webinars PRIMARY KEY), deliberately NOT org_id-hardened** (like brief, unlike engagement): no orgId reaches the loader (signature is eventId-only) AND the PK is the strongest global-uniqueness guarantee, so one id resolves exactly one org's event — no cross-tenant ambiguity, the same predicate the prior service-role client relied on. **DISCOVERED SCHEMA DRIFT (flagged, not fixed here):** the supabase select + loader read `webinars.description_override`, but that column exists on **no migration**, is absent from the Drizzle schema, and is absent from the substrate table (verified via `information_schema`); app code also **writes** it (`commit-ai-draft/route.ts:275`, a route still on supabase → later leaf). To keep the loader's `description_override ?? description` fallback byte-identical without a shared-schema change or a runtime "column does not exist" error, the twin synthesizes `description_override: null` (fallback resolves to `description`, the only value the migrated world holds). **Follow-up:** a schema/migration owner should decide whether to (re)add `webinars.description_override` (then the write at commit-ai-draft persists editorial copy and the twin selects it) or retire the field from both call sites. `EventContext` shape + `toPromptString()` byte-identical (note: `startTime`/`endTime` surface as the Drizzle `mode:'string'` pg-timestamptz representation `YYYY-MM-DD HH:MM:SS+00`, shared by every migrated leaf; `new Date()` parses it and `toPromptString` emits only the `Intl`-formatted date, so no downstream change); whole-project `tsc --noEmit` green (callers: email-context/load, load-event-generation-context, commit-ai-draft, add-invitations-journey, event-invite scaffold + templates type-imports). Verified on the substrate over two orgs each owning one webinar with disjoint `ORGA_*`/`ORGB_*` sentinels: per-event correctness, the URL-split invariant (URLs on typed fields + booleans, never in the prompt string), no cross-tenant bleed, an unknown-event-id tripwire (`exists=false`, empty prompt), and a null-eventId early-return tripwire: `scripts/local/event-context-check.ts` (27/27, 0 defects). `event.ts` + `event.drizzle.ts` have 0 Supabase refs; 3 sibling partitions in the dir (audience/contact-fields/industry-examples) remain for later leaves.
  - ai/context contact-fields-loader leaf **COMPLETE**: `src/lib/ai/context/contact-fields.ts` (`loadContactFieldsContext`) repointed off its module-level service-role `createClient` onto a new Drizzle twin `src/lib/ai/context/contact-fields.drizzle.ts` covering the single org-scoped read (`contact_field_definitions WHERE org_id = orgId AND visible = true ORDER BY position ASC`, 8 mapped columns). Like the profile/brief/event leaves (and unlike brand), the loader runs **no** `{data,error}` state machine — on `error` it LOGGED and degraded to `(defsData || [])` (system fields only) — so the twin **throws** (like `org.drizzle.ts`) and the loader's **new try/catch re-logs the identical "Falling back to system fields only" line** and degrades to `[]`, byte-identical observable behaviour. This is a LIST read (no `.maybeSingle()`), snake_case-aliased so the `#1061` untrusted-label/description fencing + tag rendering + `sampleValueForType` mapping stay byte-identical. **Classified admin→`db`, NOT `withUserId`**: all ~10 call sites pass only `orgId` (field-keys route, email test-send/send-campaign/authoring-save, inngest functions, forms-v2 mapping/generate/refine, events copy-grounding, email render-preview), no session sub. **SCOPE is the explicit `org_id = orgId` predicate** (org-keyed, like profile — NOT a child-id read); `org_id` is **NOT NULL** (no nullable-org under-count risk). **No schema drift found** — all 8 columns + `org_id`/`visible`/`position` verified present on the substrate via `information_schema` (contrast the event leaf's missing `description_override`). `ContactFieldsContext` shape + `toPromptString()` byte-identical (note: the failure-path log now carries the pg/Drizzle error text instead of the PostgREST message — same format + same degrade, only the underlying error string differs with the data layer); whole-project `tsc --noEmit` green (all callers incl. forms-v2 + inngest + email routes). Verified on the substrate over two orgs with disjoint field vocabularies: per-org correctness (system-6 + own custom/template fields), **visible filter** (a `visible:false` field excluded), **position ordering**, source/options/usageHint/tags resolution, no cross-tenant bleed (orgA prompt leaks no orgB key), and an unknown-org tripwire (system-fields-only, `hasCustomFields=false`, no borrowed keys): `scripts/local/contact-fields-context-check.ts` (21/21, 0 defects). `contact-fields.ts` + `contact-fields.drizzle.ts` have 0 Supabase refs; 2 sibling partitions in the dir (audience/industry-examples) remain for later leaves.
  - ai/context audience-loader leaf **COMPLETE**: `src/lib/ai/context/audience.ts` (`loadAudienceContext`) repointed off its module-level service-role `createClient` onto a new Drizzle twin `src/lib/ai/context/audience.drizzle.ts` covering BOTH reads — the audiences row (`loadAudienceRow`) and the cohort aggregate (`loadAudienceAggregate`). This is the **engagement pattern** (id + orgId both in hand), the richest leaf so far. `audience.ts` runs no `{data,error}` return-shape state machine — read 1 THREW `AudienceContextReferenceError` on error-or-miss, read 2 DEGRADED to an `aggregateError` context on error — so both twins **throw** and the loader's two try/catch blocks reproduce those paths byte-for-byte (throw/null→reference-error for read 1; throw→degrade for read 2). The PostgREST embed + `count:'exact'` + `.range(0,N-1)` was rebuilt as an explicit **inner join** (`audience_contacts ⋈ contacts`) capped at `MAX_AGGREGATE_CONTACTS` plus a separate `count()` for the true total — folding the old post-`.filter(contact && !deleted_at)` + `contact.org_id` predicates into the join's WHERE, which yields the identical contact set unambiguously. **Classified admin→`db`, NOT `withUserId`** (the two runtime callers — `ai/email-context/load.ts` via the barrel — pass only `(orgId, audienceId)`, no session sub). **SCOPE is BOTH keys** (the engagement hardening, NOT the brief/event single-key case): read 1 `id = audienceId AND org_id = orgId` (an other-org audience is a miss → reference error, never a cross-tenant read); read 2 `audience_id = audienceId AND contacts.org_id = orgId AND contacts.deleted_at IS NULL` (the `contacts.org_id` half excludes a cross-org contact mistakenly linked into the audience from the counts). `org_id` NOT NULL on both tables. **No schema drift** — all columns on `audiences`/`audience_contacts`/`contacts` verified present via `information_schema`. The loader's dead `options.client?: SupabaseClient` injection seam (only ever used to inject a supabase client for tests + `load.ts`) was **removed**: `LoadAudienceContextOptions` deleted, the barrel re-export dropped, `email-context/load.ts` updated to call `loadAudienceContext(orgId, audienceId)` (it keeps its own supabase client for `assertEventOwnership` — its own later leaf), and `audience.test.ts` **reworked to mock the twin** (engagement precedent) — 6/6 green; `load.test.ts` audience-call assertion updated (4/4 green). `AudienceContext` shape + `toPromptString()` + the `#…` untrusted `AUDIENCE_CONTEXT` fencing byte-identical; whole-project `tsc --noEmit` green. Verified on the substrate over two orgs: per-audience correctness, privacy thresholds (cohort≥10, category≥5) + true-count-vs-capped-rows, **deleted + cross-org contacts excluded from the aggregate** (count=18 not 20), **ROW org-hardening** (`loadAudienceContext(orgB, audienceA)` throws — a naive id-only port would leak here), **AGGREGATE org-hardening** (orgB's `pending` cohort never in orgA's distribution), and an unknown-id tripwire: `scripts/local/audience-context-check.ts` (16/16, 0 defects). `audience.ts` + `audience.drizzle.ts` have 0 Supabase refs; 1 sibling partition in the dir (industry-examples) remains for the final leaf.
  - ai/context industry-examples-loader leaf **COMPLETE — FINAL context leaf; `src/lib/ai/context/` is now 100% supabase-free** (`npm run check:no-supabase 2>&1 | grep 'ai/context/'` → 0 hits; the only residual `context/`-substring match is `email-context/load.ts`, a separate later leaf). **PREMISE CORRECTION:** unlike every prior leaf, `src/lib/ai/context/industry-examples.ts` never had a module-level service-role `createClient` — it was a thin ADAPTER (`getIndustryExamples(supabase, orgId)`) forwarding the caller's client to `loadIndustryTemplateForOrg`, and its only supabase-js touch was the `import type { SupabaseClient }` param annotation. Its TWO runtime callers are **BROWSER client components** (`AIEmailModal.tsx`, `CreateWithAIWizard.tsx`) that passed a browser `createClient()` — so it ran in the browser under RLS, not service-role, and a Drizzle pool cannot replace it in-place. Migration therefore reshaped the leaf into a `'use server'` **server action**: `getIndustryExamples()` (no args) derives orgId from the **session** (`requireAuth`, never a client-supplied id) and runs `withUserId(userId, tx => getIndustryExamplesForOrg(orgId, tx))` on the new Drizzle twin `src/lib/ai/context/industry-examples.drizzle.ts` — the exact `withUserId`+`loadIndustryTemplateForOrg` shape the already-migrated `audiences/preset/starters` route uses. **Classified admin `db` (via `withUserId`), same as the industry-loader sibling**: `loadIndustryTemplateForOrg` does an ORG-KEYED read (`organizations WHERE id = orgId`, a globally-unique PK → own template or null→throws `organization_not_found`) chained into a NON-TENANT REFERENCE read (`industry_template_configs WHERE template = key` — four shared rows, **no `org_id` column at all**, verified 21 cols via `information_schema`). The only fact that flows out is which of four shared templates the org uses; cross-tenant bleed is structurally impossible. **THROW (not `{data,error}`), one outer try/catch** (like every leaf except brand): the twin throws `IndustryTemplateConfigError`, the single `catch` degrades to `GENERIC_FALLBACK` byte-for-byte with the old adapter. The reshape + `GENERIC_FALLBACK` + `EmailExample` type moved verbatim into the twin (this leaf has **no `toPromptString`** — it returns `EmailExample[]` UI chips, not a prompt block, and is not in the context barrel). **No schema drift** (contrast the event leaf's `description_override`). The two browser callers were repointed to `getIndustryExamples()` (drop the client + orgId args); `industry-examples.test.ts` **reworked to mock the twin** (`getIndustryExamplesForOrg`, stubbing `@/db/client` + `load-template.drizzle`) — 2/2 green; whole-project `tsc --noEmit` green (no other typed caller). Verified on the substrate over two orgs on different verticals: membership resolves its 4 own configured example chips (texts verbatim, valid `TemplateType` styles only), non_profit (0 configured) degrades to `GENERIC_FALLBACK` and **never borrows the membership examples**, no cross-tenant text bleed, and an unknown-org tripwire (`organization_not_found` → caught → fallback, never another org's set): `scripts/local/industry-examples-context-check.ts` (12/12, 0 defects). Deliberate new surface: the `'use server'` action is a public POST reachable by any authenticated user, but org is session-derived and the payload is low-sensitivity reference chips — no cross-tenant exposure. `industry-examples.ts` + `industry-examples.drizzle.ts` have 0 Supabase refs; **0 sibling partitions remain — the ai/context directory rewrite (US1) is complete.**
  - ai/**email-context** loader leaf **COMPLETE — `src/lib/ai/email-context/` is now 100% supabase-free** (`npm run check:no-supabase 2>&1 | grep 'email-context/'` → 0 hits). `src/lib/ai/email-context/load.ts` (`loadEmailGenerationContext`) orchestrates NINE already-migrated ai/context twins (brand/org/industry/event/brief/contact-fields/knowledge/profile/audience, all Drizzle-on-`db`), so its **only own** supabase read was `assertEventOwnership` — an existence check that the requested event belongs to the requesting org. Migrated onto a new Drizzle twin `src/lib/ai/email-context/load.drizzle.ts` (`webinarBelongsToOrg(orgId, eventId)` — `select id from webinars where id = eventId and org_id = orgId limit 1` → boolean). **Classified admin `db`, NOT `withUserId`** (mirrors all nine composed twins): every one of the SIX runtime callers (email generate / refine / authoring-block-ai / templates-fill routes, activation generate route, create-draft-from-template) passes only its session-derived `orgId`, never a session sub. **SCOPE is BOTH keys — `id = eventId AND org_id = orgId`** (the id+orgId ownership hardening, like engagement/audience, NOT the event leaf's single-PK read): this is an OWNERSHIP ASSERTION, so `org_id` is the load-bearing guard — without it any org's eventId would pass (cross-tenant). `webinars.org_id` is **NULLABLE** (verified via `information_schema`) — a NULL-org webinar can never satisfy `org_id = orgId` (SQL NULL comparison), so it **fails closed** to `EmailContextReferenceError('event')`, byte-identical to the old PostgREST `.eq('org_id', orgId)` (which also never matched NULL). **THROW→collapse, not `{data,error}`**: the old `assertEventOwnership` collapsed BOTH `error` and no-row to the same `throw EmailContextReferenceError('event')`; the new wrapper maps the twin's throw (query failure) AND a `false` (not-owned/missing) both to that same reference error — byte-for-byte. The dead `input.client?: SupabaseClient` injection seam + the lazy `createClient(SERVICE_ROLE)` fallback were **removed**; the `client: supabase` argument was dropped from the 4 callers that injected it (activation/generate, email/generate, templates/[id]/fill, create-draft-from-template — the other 2 never passed one), leaving each route's own `supabase` var intact for its remaining reads. `LoadEmailGenerationContextInput` shape (minus `client`) + `EmailGenerationContext` + the receipt/metadata projection byte-identical; whole-project `tsc --noEmit` green (all 6 callers typecheck). `load.test.ts` **reworked to mock the twin** (dropped the supabase-js `noQueryClient`/`eventOwnershipClient` mocks; added a query-error-fails-closed case) — 5/5 green; all 6 caller route/lib test suites + the client-boundary test still green (11 suites / 78 tests). Verified on the substrate over two orgs each owning one webinar + a NULL-org orphan webinar: per-org ownership (orgA owns eventA), **no cross-tenant bleed** (orgB does NOT own eventA and vice versa — the `org_id` guard holds), an unknown-event tripwire, and NULL-org fail-closed: `scripts/local/email-context-check.ts` (7/7, 0 defects). `load.ts` + `load.drizzle.ts` have 0 Supabase refs.
  - campaigns **slug-helper** leaf **COMPLETE** (1st of the 5 `commit-ai-draft` helper leaves) — `src/lib/campaigns/slug.ts` is now 100% supabase-free (`check:no-supabase` over `slug.ts` + twin → 0 hits). The old `getAvailableSlug(supabase, orgId, desiredSlug)` did ONE org-scoped read (`campaigns.select('utm_campaign').eq('org_id', orgId).like('utm_campaign', '<desired>%')`) then a suffix scan. Migrated by **splitting into a pure core + a Drizzle twin**: `slug.ts` keeps `nameToSlug` (pure) and a new **pure** `nextFreeSlug(desiredSlug, taken)` (the byte-identical collision-increment — guard empty → not-taken → first free -2/-3/…); the org-scoped read moved to the new Drizzle twin `src/lib/campaigns/slug.drizzle.ts` (`getAvailableSlug(orgId, desiredSlug, exec=db)`) which reads the taken-set and defers to `nextFreeSlug`. **Classified admin→`db`, NOT `withUserId`** (mirrors audience/load + every ai/context twin): the read is scoped by the **explicit `eq(org_id)`** predicate, not RLS, so tenant isolation holds on the pooled admin handle exactly as under the prior service-role client; the migrated caller (`commit-ai-draft`, a service-role root) passes only `orgId` — no session sub to seed. The `exec` param ACCEPTS a `Tx` so when the root migrates to `withUserId` it can pass its own tx (documented at the call site). **SCOPE is single key `org_id`** (audiences-list shape, NOT id+orgId): `org_id` is the whole tenant guard and is **NOT NULL** on campaigns, so two orgs can independently hold the same slug (per-(org_id,utm_campaign) unique index). **THROW→try/catch**: the old read destructured only `{ data }` (a failure → `data=null→[]`, silent best-effort miss, never a throw); Drizzle throws, so the twin catches → empty taken-set → returns `desiredSlug`, letting the unique index be the backstop — byte-identical. **BROWSER-CALLER FINDING (task didn't anticipate):** besides the server root, `getAvailableSlug` had a second caller — `dashboard/campaigns/components/CampaignModals.tsx`, a **`'use client'` component** that calls it (and inserts campaigns) via a browser Supabase client; a Drizzle pool cannot run in the browser. Unlike the industry-examples leaf (browser-ONLY callers, no server root → reshaped to a `'use server'` action), this leaf's target is the server root and the modal does a browser INSERT immediately after the slug read, so the clean server-action boundary is the whole create flow (a **separate future leaf**). Resolved minimally: both modal call sites now do the org-scoped read via their existing browser Supabase client and feed the shared pure `nextFreeSlug` — a faithful RELOCATION of the same read (net-zero supabase, byte-identical returned slug), leaving the modal's create flow otherwise intact for its own later migration. `commit-ai-draft/route.ts` repointed to import `getAvailableSlug` from the twin (`nameToSlug` still from `slug.ts`), keeping its supabase client for the OTHER 4 helpers; `route.test.ts` reworked to mock `@/lib/campaigns/slug.drizzle` (15/15 green). Whole-project `tsc --noEmit` green. Verified on the substrate over two orgs: collision-increment (-3), **cross-tenant isolation** (org B → -2 not -3, the divergence proving `eq(org_id)` holds), cross-tenant coexistence (both orgs hold `summer-sale`), first-slug no-collision, loose-prefix boundary (`summer` → `summer-2`, longer over-matched slugs ignored), and empty-slug short-circuit: `scripts/local/slug-check.ts` (10/10, 0 defects). **Known non-blocking limits (adversarial review):** (1) under production RLS with a non-superuser admin role + no `app.user_id`, the admin-`db` read could return 0 rows → dedup disabled → unique-index backstop + the route's existing 23505 retry (same best-effort degradation the precedent twins accept; a spec-wide admin-role concern, T018, not this leaf); (2) `nameToSlug` keeps `_` (JS `\w`), and `_` is a LIKE single-char wildcard, so the prefix over-matches — harmless (a superset taken-set never causes a false-free; the exact suffix scan ignores extras) and **byte-identical to the old supabase `.like(...)`**, not introduced here. `slug.ts` + `slug.drizzle.ts` have 0 Supabase refs; 4 helper leaves (`resolveCustomFill`, `createNativeRegistrationForm`, `createLinearJourney`, `createFollowupJourney`) + the root remain.
  - activation **resolveCustomFill** leaf **COMPLETE** (2nd of the 5 `commit-ai-draft` helper leaves) — `resolveCustomFill` (`src/lib/activation/create-draft-from-template.ts`) shed its `supabase` param. Its ONLY supabase touch was a single `email_templates` read by (id, org_id) selecting `id, html_content, region_schema, template_type`; that read moved to the new Drizzle twin `src/lib/activation/create-draft-from-template.drizzle.ts` (`loadCustomTemplateRow(orgId, templateId, exec=db)`). The surrounding null-logic (not-`custom` / no `html_content` / `regionSchemaArray.safeParse` fail → null) and the already-migrated `loadBrandContext` stayed in `resolveCustomFill`, so the returned `CustomFillSpec` is **byte-identical** (the `as string` casts dropped — the twin types `id`/`html_content`, which narrow after the guards). **Classified admin→`db`, NOT `withUserId`** (mirrors slug/audience/load + every ai/context twin): both callers are service-role ROOTS — `events/commit-ai-draft` and this file's own `createDraftFromTemplate` (via `activation/draft`) — passing only `orgId`, no session sub; the read is scoped by the **explicit `eq(id) AND eq(org_id)`** predicate, so tenant isolation holds on the pooled admin handle. `exec` ACCEPTS a `Tx` for a future withUserId root. **SCOPE is BOTH keys** (id+orgId hardening, like engagement/audience): `email_templates.id` is a UUID PK but `org_id` is the **load-bearing guard** — without it any org could resolve (and fill each step into) another org's saved template design; both columns are **NOT NULL**. **THROW→try/catch**: the old `.maybeSingle()` destructured only `{ data: tpl }` (a query failure → `tpl=null`, silent degrade to default generation, never a throw); Drizzle throws, so the twin catches → null, byte-identical (`.maybeSingle()`→`.limit(1)+rows[0]??null`; the PK means ≤1 row regardless). **CO-LOCATED-ROOT constraint (like commit-ai-draft):** the leaf function is supabase-free but its FILE stays supabase-red — `createDraftFromTemplate` (the `activation/draft` ROOT) + `createCampaignEmails` + `resolvePlanGoalId` still thread a `SupabaseClient` (their own later leaf/root), so `check:no-supabase` over the FILE cannot go green here; the twin has 0 supabase refs and `resolveCustomFill`'s body has 0 supabase refs (gate satisfied at the leaf granularity, same as slug's browser-caller carve-out). **TEST-IMPORT cascade found + fixed:** adding the static twin import to `create-draft-from-template.ts` pulled `@/db/client` into every importer's module graph; the 4 sibling suites (`create-draft-cta/goal/enrolment/timing.test.ts`) mock `@/lib/ai/context/brand` (cutting the old db path) but not the twin, so they went import-red under the `@/db/client` DATABASE_URL guard — each now also mocks the twin (bare `jest.fn()`, since they use no saved-custom selection so `resolveCustomFill` early-returns). `create-draft-template-fill.test.ts` reworked to mock the twin (`loadCustomTemplateRow` → fixture row in `beforeEach`) instead of the supabase `email_templates` branch; `commit-ai-draft/route.test.ts` unaffected (it mocks the whole `create-draft-from-template` module). All 17 activation + commit-ai-draft suites (95 tests) green; whole-project `tsc --noEmit` green. Verified on the substrate over two orgs: per-org read correctness, **cross-tenant no-bleed** (`loadCustomTemplateRow(orgB, tplA)` and `(orgA, tplB)` both → null — a naive id-only port would leak/fill another org's template), raw-type fidelity (an announcement row returns honestly; the null-mapping is resolveCustomFill's guard, not the read), and an unknown-id tripwire: `scripts/local/create-draft-from-template-check.ts` (16/16, 0 defects). The harness tests the TWIN directly (not `resolveCustomFill` end-to-end) because the leaf's transitive imports (`ai/client`→`billing/consumption`) construct a module-level Supabase client at load that a headless tsx run can't satisfy — resolveCustomFill's null-logic is covered by the reworked jest suite. **Known non-blocking limits (adversarial review):** (1) the twin's catch→null silently degrades a chosen custom template to default generation on a transient DB error — pre-existing (old code ignored `{ error }`), not introduced; (2) under prod RLS with a non-superuser admin role + no `app.user_id` the read could return 0 rows → null → default generation — the same best-effort admin-`db` assumption already accepted for 11 prior leaves (spec-wide T018 concern). `create-draft-from-template.drizzle.ts` has 0 Supabase refs; `resolveCustomFill` body has 0 Supabase refs; 3 helper leaves (`createNativeRegistrationForm`, `createLinearJourney`, `createFollowupJourney`) + the root remain.
  - forms-v2 **derive-slug** leaf **COMPLETE** (1st of the 2 sub-leaves under helper 3 `createNativeRegistrationForm`) — `src/lib/forms-v2/landing-pages/derive-slug.ts` is now 100% supabase-free (`check:no-supabase` over `derive-slug.ts` + twin → 0 hits). **SUBTREE DISCOVERY:** helper 3 `createNativeRegistrationForm` is NOT a clean leaf — it's a small subtree that (a) does 3 org-scoped inserts of its own (`captured_forms`/`form_field_mappings`/`landing_pages`) + cleanup deletes, and (b) calls TWO still-supabase sub-leaves: `pickAvailableFormSlug` (`activation/create-capture-form.ts`, reads `captured_forms`, 4 callers) and `findAvailableSlug` (`forms-v2/landing-pages/derive-slug.ts`, reads `landing_pages`, 5 callers). It can't shed its `supabase` client until BOTH sub-leaves migrate, so — per the "small isolated batches" rule + a user scope decision — the sub-leaves go FIRST, each its own leaf, then helper 3's inserts. This entry is sub-leaf #1. `findAvailableSlug` + `checkSlugAvailability` shed their `supabase` param; the single `landing_pages` slug-uniqueness probe moved to the new Drizzle twin `derive-slug.drizzle.ts` (`landingPageSlugExists(slug, excludeId?, exec=db)`); the pure `slugifyTitle` + the 50-try collision-walk + reserved-word logic stayed, so every returned slug is byte-identical. **Classified admin→`db`, NOT `withUserId`, and a NON-TENANT / GLOBAL reference read (NO org predicate)** — this is the KEY divergence from the campaigns slug leaf: the unique index `landing_pages_slug_uniq` is on `(slug) WHERE slug IS NOT NULL` (NO org_id — migration 20260617010000; the module docstring confirms "uniqueness is global across orgs"), so two orgs cannot both publish `/page/register` and the walker gives the second one `register-2`. Adding an `eq(org_id)` would let two orgs pick the same slug then collide on the global unique index at insert — so the twin deliberately has no org filter (byte-identical to the old `.eq('slug', …)`). The 6 callers (slug route, publish route ×2, build-capture-surface, create-registration-form) pass only a title/slug, no session sub → admin `db`; `exec` accepts a `Tx` for a future withUserId caller. **THROW→per-caller semantics**: the twin throws on a query failure; `findAvailableSlug` catches per-candidate and walks on (treat-as-taken, byte-identical to the old `if (error) continue`), `checkSlugAvailability` maps a throw to `{ ok:false, reason:'invalid' }` — reproducing the two callers' DIFFERENT `{data,error}` handling exactly. `.maybeSingle()` + `.neq('id', excludeId)` → `.limit(1)` + `ne(id, excludeId)` + `rows.length > 0`. **IMPORT-CASCADE handled by DYNAMIC import** (the slug-leaf "keep pure exports db-free" principle): `derive-slug.ts` exports the PURE `slugifyTitle`, imported by `create-capture-form` (and thus its ~5 test suites), so a STATIC twin import would drag `@/db/client` into all of them (import-red under the DATABASE_URL guard). Instead the twin is `await import('./derive-slug.drizzle')` INSIDE the two async fns, so `slugifyTitle` stays pool-free and ZERO caller-tests needed twin mocks (established pattern — `create-draft-from-template.ts` dynamic-imports the same way). All 6 call sites updated to drop the client arg; whole-project `tsc --noEmit` green; the affected suites (activation/events/forms-v2/commit-ai-draft, 430 tests) green except 2 PRE-EXISTING import-reds unrelated to this leaf (`forms-v2/landing-pages/[id]/route.test.ts` via the route's own `@/db/client`; `capBlocksAtLimit.test.ts` via `run-switch-layout`→`org.ts`→`org.drizzle`). Verified on the substrate over two orgs: the **GLOBAL cross-tenant collision walk** (orgA `summer-fest` + orgB `summer-fest-2` → `summer-fest-3`), no-collision, reserved→`page` fallback, `excludeId` self-skip, checkSlugAvailability taken/available/reserved/invalid, and twin true/false/exclude: `scripts/local/derive-slug-check.ts` (15/15, 0 defects). `derive-slug.ts` + `derive-slug.drizzle.ts` have 0 Supabase refs. **Remaining under helper 3:** sub-leaf `pickAvailableFormSlug` (create-capture-form.ts — co-located with an insert root, so close at leaf granularity), THEN `createNativeRegistrationForm`'s own 3 inserts + cleanup, THEN it sheds its client.
  - activation **pickAvailableFormSlug** leaf **COMPLETE** (2nd/LAST sub-leaf under helper 3 `createNativeRegistrationForm`; both sub-leaves now done) — `pickAvailableFormSlug` (`src/lib/activation/create-capture-form.ts`) shed its `supabase` param; its single `captured_forms` slug-uniqueness probe moved to the new Drizzle twin `create-capture-form.drizzle.ts` (`capturedFormSlugExists(orgId, slug, exec=db)`). The pure `slugifyTitle` + collision-walk + reserved-word logic + the uuid fallback stayed, so the returned slug is byte-identical. **Classified admin→`db`, NOT `withUserId`, PER-ORG read (explicit eq(org_id)+eq(slug))** — the OPPOSITE of the derive-slug sub-leaf: the unique index `captured_forms_org_slug_uniq` is on `(org_id, slug) WHERE slug IS NOT NULL` (per-org, like the campaigns slug leaf), so org_id is the load-bearing tenant guard and MUST stay in the predicate (dropping it would falsely report another org's slug as taken); two orgs can both publish `/f/member-guide`. All 4 callers (forms-v2/generate ×2, create-capture-form's own `createCaptureForm` root, create-registration-form) pass only `orgId`, no session sub → admin `db`; `exec` accepts a `Tx`. **THROW→per-candidate try/catch**: the old `.maybeSingle()` WARNED + treated a query error as taken (walk on); the twin throws and the caller's per-candidate try/catch reproduces that byte-for-byte (same warn line). **CO-LOCATED-ROOT constraint (like resolveCustomFill):** the leaf function is supabase-free but the FILE stays supabase-red — `createCaptureForm` (the `activation/create-capture` + `build-capture-surface` insert ROOT) still threads a `SupabaseClient` (its own later root), so `check:no-supabase` over the FILE cannot go green; the twin has 0 supabase refs and `pickAvailableFormSlug`'s body has 0 supabase refs (gate satisfied at leaf granularity). **DYNAMIC-import twin** (the derive-slug precedent): `create-capture-form.ts` exports PURE helpers (`slugifyTitle` re-users, `buildCaptureFields`, `identityFields`) imported by sibling tests (`build-capture-fields.test.ts` etc.), so the twin is `await import('./create-capture-form.drizzle')` INSIDE `pickAvailableFormSlug` — keeping the module's pure exports free of the `@/db/client` pool → ZERO caller-test twin-mocks needed. All 4 call sites updated to drop the client arg; whole-project `tsc --noEmit` green; affected suites (activation/events/forms-v2/commit-ai-draft, 430 tests) green except the SAME 2 PRE-EXISTING import-reds unrelated to this leaf (`[id]/route.test.ts`, `capBlocksAtLimit.test.ts`). Verified on the substrate over two orgs: the **PER-ORG collision walk** (orgA `member-guide`+`member-guide-2` → `member-guide-3`; orgB `member-guide` → `member-guide-2`, the -3/-2 divergence proving eq(org_id)), cross-tenant coexistence (both orgs hold `member-guide`), no-collision, reserved→`form`, and twin true/false per-org: `scripts/local/pick-form-slug-check.ts` (12/12, 0 defects). `create-capture-form.drizzle.ts` has 0 Supabase refs; `pickAvailableFormSlug` body has 0 Supabase refs. **Helper 3 status:** BOTH sub-leaves (`findAvailableSlug`/`derive-slug`, `pickAvailableFormSlug`) now migrated → `createNativeRegistrationForm` no longer passes a client to either. **Remaining to close helper 3:** its OWN 3 inserts (`captured_forms`/`form_field_mappings`/`landing_pages`) + the 2 `bestEffortDelete` cleanups → a Drizzle twin (org-scoped writes, admin db + explicit org_id stamping), then it sheds its `supabase` param entirely.
  - events **createNativeRegistrationForm** leaf **COMPLETE — helper 3 of 5 FULLY CLOSED** (`src/lib/events/create-registration-form.ts` is now 100% supabase-free — `check:no-supabase` over the FILE goes GREEN, unlike the co-located-root leaves, because this file hosts no insert root). This closed the subtree: both slug sub-leaves (derive-slug, pickAvailableFormSlug) landed first, then this step migrated the helper's OWN 3 org-scoped INSERTS (`captured_forms` + `form_field_mappings` bulk + `landing_pages`) + shed the `supabase` param. The inserts moved to the new Drizzle twin `create-registration-form.drizzle.ts` (`createRegistrationFormRows`), run **ATOMICALLY in one `db.transaction`** — deliberately REPLACING the old best-effort manual rollback (`bestEffortDelete` on captured_forms). **Adversarial-review rationale for the transaction:** the old path inserted sequentially and, on a mappings/landing failure, issued a follow-up delete of captured_forms — a real orphan window if that delete itself failed; the transaction rolls back all earlier inserts automatically, so a captured_forms/mappings row can never orphan. **Byte-identical observable contract preserved:** returns the same `NativeRegistrationForm` (formId/landingPageId/landingUrl/createdRefs, createdRefs only on success) and throws the same per-step `RegistrationFormError` messages — the twin throws a discriminated `RegistrationRowsError{step}` and the leaf maps `form`/`mappings`/`landing` → the exact old message strings (underlying error text differs pg-vs-PostgREST, format identical). The landing-slug derivation was moved BEFORE the transaction (an unavailable slug throws before any write — nothing to roll back), observably identical to the old (which rolled back on that path). **Classified admin→`db`, NOT `withUserId`; org-scoped WRITES with EXPLICIT org_id stamping** on all 3 tables (all 3 callers pass only `orgId`, no session sub); `exec` accepts a `Tx` so a future withUserId root runs the inserts inside its own identity tx (`exec === db` → own transaction; a passed Tx → inline). Verified every inserted column against `information_schema` + all CHECK enums (captured_forms source/status; form_field_mappings contact_field_tier/mapping_source/confidence/action_on_unmapped/form_field_type; landing_pages status) — all inserted values valid; timestamps are `mode:'string'` (defaults left to now()); pure-null optional columns omitted (byte-identical NULL); `layout` omitted (default 'signup', as the old insert did). **DYNAMIC-import twin** (derive-slug precedent): the leaf `await import('./create-registration-form.drizzle')` inside the fn so its pure exports (DEFAULT_REGISTRATION_FIELDS, types, RegistrationFormError) stay `@/db/client`-free — keeping `commit-ai-draft.test` (which uses the REAL createNativeRegistrationForm, unmocked) importable. **Callers + tests:** the 3 route call sites (events/create, events/[id]/registration-form, commit-ai-draft) drop the client arg; the co-located `create-registration-form.test.ts` reworked to mock the twin + assert the built row payloads + per-step error mapping (real atomic rollback moved to the harness — a mocked twin can't exercise a tx); `create/route.test` + `[id]/registration-form/route.test` `toHaveBeenCalledWith` fixed to the single-arg signature; `commit-ai-draft.test` given a twin mock returning `formId:'captured_forms-1'` (so its `capture_form_id` assertion holds) — all 4 directly-affected suites green (40 tests). Whole-project `tsc --noEmit` green. Broader `api/events` shows 5 PRE-EXISTING import-reds unrelated to this leaf (add-invitations/add-followup/add-confirm-journey, regenerate-email, generate-from-draft — all via `event-copy-grounding`→`profile.drizzle`→`@/db/client`, the prior profile leaf). Verified on the substrate over two orgs: per-org insert correctness + **org_id stamping on all 3 tables**, FK wiring (mappings.captured_form_id, landing.form_id), cross-tenant no-bleed, and **ATOMIC ROLLBACK** (a duplicate-global-slug landing failure rolls back the already-inserted captured_forms + mappings — no orphan): `scripts/local/create-registration-form-check.ts` (20/20, 0 defects). `create-registration-form.ts` + `create-registration-form.drizzle.ts` have 0 Supabase refs. **Helper 3 done → commit-ai-draft no longer threads a client into it.** Remaining before the root: helpers `createLinearJourney` + `createFollowupJourney` (2 of 5).
  - events **createLinearJourney** leaf **COMPLETE — helper 4 of 5** (`src/lib/events/build-linear-journey.ts` is now 100% supabase-free — `check:no-supabase` over the FILE goes GREEN, like create-registration-form.ts and unlike the co-located-root leaves, because this file hosts only helpers + pure exports, no root). Migrated BOTH writer functions in the file: `createLinearJourney` (writes `journeys` + `journey_steps` + `journey_edges`) and its shared sub-helper `createCampaignEmail` (writes `campaign_emails`) — the latter is ALSO called by `createFollowupJourney` (helper 5), so migrating it here unblocks that leaf's email write too (helper 5 keeps its own supabase client only for its journeys/steps/edges). The 4 writes moved to the new Drizzle twin `build-linear-journey.drizzle.ts` (`insertJourneyRow`, `insertCampaignEmailRow`, `insertJourneyStepsAndEdges`). **Classified admin→`db`, NOT `withUserId`** — all four callers (commit-ai-draft, add-confirm-journey, add-invitations-journey, and via the followup path createFollowupJourney) pass only `orgId`, no session sub; `exec` accepts a `Tx` for a future withUserId root. **Per-table classification (verified via `information_schema`):** `journeys` + `campaign_emails` are org-scoped WRITES with EXPLICIT `org_id` stamping (both `org_id` NOT NULL — the tenant guard on the admin handle); `journey_steps` + `journey_edges` have **NO `org_id` column** — their tenant scope is the FK to `journeys.id` (both `journey_id_fkey` ON DELETE CASCADE) + the RLS policy that joins through `journeys.org_id`, so they are children keyed by the supplied `journey_id`, nothing to stamp. Checked every written column against NOT NULL/defaults: journeys NN (org_id/name/trigger_type) all written, status='draft'/version=1 explicit; campaign_emails `authoring_mode` (NOT NULL, default 'html_compat') left unwritten to its default exactly as the old insert did; every value byte-identical to the old supabase insert (phase/trigger_type/created_via/send_type/config/position_x·y/label). **TRANSACTION scoping:** only `journey_steps`+`journey_edges` run in one atomic tx — `journeys` and each `campaign_emails` are single-row statements (atomic already), and `createCampaignEmail` does AI work (custom-template fill) that must NOT be held inside a transaction, so it writes on `db` outside any tx. The steps+edges tx is **defence-in-depth (a local all-or-nothing invariant), NOT a live-orphan fix** — adversarial review corrected an earlier overstatement of mine: on the old two-statement path an edge failure did leave journey_steps committed, but the journey row is always in `created` and both child FKs are ON DELETE CASCADE, so the caller's journey-delete already cascade-cleaned them; the tx just makes the guarantee local instead of leaning on that downstream cascade (comments in twin + leaf + harness corrected to say so). **Byte-identical contract preserved:** returns `{ journeyId, created } | { error, created }` and NEVER throws for a DB failure (the twin throws Drizzle-native / a discriminated `JourneyRowsError{part:'steps'|'edges'}`, and the leaf maps back to the exact old `Failed to insert <phase> steps/edges` message strings); `created` contents+order unchanged (journey, then a campaign_email per email step, then all journey_steps — edges never listed, as before, since they cascade). **DYNAMIC-import twin** (the derive-slug/create-registration-form precedent): both writers `await import('./build-linear-journey.drizzle')` inside the fn so the module's PURE exports (normalizeEmail, describeWait, isStepAlreadyPast, stepConfigForEmail, waitConfigForTiming, the types) stay `@/db/client`-free — `build-followup-journey.ts` + the four event routes import them, and none newly drag the pool. **Callers + tests:** the 4 `createLinearJourney` call sites (commit-ai-draft ×2, add-confirm-journey, add-invitations-journey) + the 2 `createCampaignEmail` sites in build-followup-journey drop the `supabase` arg; the co-located `build-linear-journey.test.ts` reworked to mock the twin + capture the campaign_emails values (the #1207 send_type regressions now assert Drizzle `sendType`) — 4/4 green; commit-ai-draft ROOT test 15/15 green. Whole-project `tsc --noEmit` green. Broader `api/events` shows the SAME 5 PRE-EXISTING import-reds unrelated to this leaf (add-confirm/add-followup/add-invitations-journey, generate-from-draft, regenerate-email — all via `event-copy-grounding`→`profile.drizzle`→`@/db/client`, the prior profile leaf; 0 traces touch this twin). Independent adversarial review (org_id stamping / NOT NULL·CHECK·mapping / rollback semantics): **all three areas CLEAN, no correctness bug** (only the overstated-orphan comment, now fixed, + two non-defect standing notes: the dynamic import sits outside try/catch but is practically unreachable; and compensation deletes still go through the supabase admin handle, safe while both point at the same physical Postgres — spec-002's premise). Verified on the substrate over two orgs: per-org `org_id` stamping on journeys+campaign_emails, journey_steps/journey_edges carry no org_id + correct journey_id FK wiring (edge source/target reference the inserted step ids), cross-tenant no-bleed (orgB steps unreachable under orgA), and **ATOMIC ROLLBACK** (a forced dangling-target_step_id edge FK failure rolls back the already-inserted journey_steps): `scripts/local/build-linear-journey-check.ts` (20/20, 0 defects). `build-linear-journey.ts` + `build-linear-journey.drizzle.ts` have 0 Supabase refs. **Remaining before the commit-ai-draft root: `createFollowupJourney` (`build-followup-journey.ts`) — helper 5 of 5, the last.**
  - events **createFollowupJourney** leaf **COMPLETE — helper 5 of 5, the LAST helper; ALL 5 commit-ai-draft helper leaves now migrated** (`src/lib/events/build-followup-journey.ts` is 100% supabase-free — `check:no-supabase` over the FILE goes GREEN; no root here). Its 3 writes (`journeys` 1 insert + `journey_steps` bulk + `journey_edges` bulk) **REUSE the shared createLinearJourney twin verbatim** (`build-linear-journey.drizzle.ts` — `insertJourneyRow` + `insertJourneyStepsAndEdges`); **no second twin created** — the followup journeys row only differs in literals (`trigger_type='webinar_ended'`, `phase='followup'`), and its steps/edges are the same `JourneyStepInsert`/`JourneyEdgeInsert` shape. Its campaign_emails write already routed through the migrated `createCampaignEmail` (helper 4), so only these 3 own writes remained. **Classified admin→`db`, NOT `withUserId`** (both callers — commit-ai-draft, add-followup-journey — pass only `orgId`, no session sub); `journeys` org-scoped write with EXPLICIT `org_id` stamping (NOT NULL); `journey_steps`/`journey_edges` have no org_id (child-keyed by `journey_id` FK, both ON DELETE CASCADE). **Branch/condition wiring converted to camelCase** across BOTH paths — the single-'all'-branch (trigger→wait/email chain) and the attended/no_show two-branch (a `condition` step + `buildBranchSteps` per branch, with the 'Yes'/'No' first-edge labels and the label→null reset after). Adversarial review verified field-by-field that every step id, edge source/target direction, branch label, label-reset, position, and threaded `journeyId` is byte-identical to the old snake_case (no swap, no lost label). **Byte-identical contract:** returns `{ journeyId, created } | { error, created }`, NEVER throws on a DB failure, `created` order unchanged (followup_journey, then campaign_email per step, then all journey_steps; edges omitted), old error strings preserved via the discriminated `JourneyRowsError{part}` ("Failed to insert followup steps/edges"). Same steps+edges atomic tx (**defence-in-depth, not a live-orphan fix** — journey is always in `created` + both child FKs cascade). **DYNAMIC-import twin** (createLinearJourney precedent): `await import('./build-linear-journey.drizzle')` inside the fn so the module's re-exported pure helpers stay `@/db/client`-free. `buildBranchSteps` also shed its now-unused `supabase` param (typed arrays). **Callers + tests:** the 2 `createFollowupJourney` call sites (commit-ai-draft, add-followup-journey) drop the `supabase` arg; a NEW co-located `build-followup-journey.test.ts` (there was none) mocks the twin + AI deps and asserts both branch GRAPHS (single-'all' → trigger→email no condition; attended/no_show → trigger→condition, condition→Yes/No branch edges) — 2/2 green; **commit-ai-draft ROOT test fixed** (it never mocked build-followup-journey and used the REAL createFollowupJourney via a mock supabase — now that the fn writes through the twin's `@/db/client` import it must be module-mocked like createLinearJourney already is; added `jest.mock('@/lib/events/build-followup-journey')` resolving success) → root 15/15 green, whole `api/events` 115/115. Whole `src/lib/events` jest 10 suites / 52 tests green. Whole-project `tsc --noEmit` green. Broader `api/events` shows the SAME 5 PRE-EXISTING import-reds unrelated to this leaf (add-confirm/add-followup/add-invitations-journey, generate-from-draft, regenerate-email — all via `event-copy-grounding`→`profile.drizzle`→`@/db/client`, the prior profile leaf; add-followup's red trace confirmed through profile.drizzle, 0 via any journey twin). Independent adversarial review (org_id stamping / branch-condition edge wiring / rollback semantics): **all three CLEAN, no correctness defect** (only two non-defect notes: cosmetic error-string wording drift, and the non-JourneyRowsError fallback hardcodes "steps" — reachable only on a transaction-open failure where no partial rows exist). Verified on the substrate over two orgs: per-org `journeys.org_id` stamping on a phase='followup' row, the two-branch shape round-trips (condition step type + 'Yes'/'No' edge labels persist, 4 steps / 3 edges), the single-'all' shape (no condition step), FK wiring, cross-tenant no-bleed, and atomic steps/edges rollback: `scripts/local/build-followup-journey-check.ts` (14/14, 0 defects). `build-followup-journey.ts` has 0 Supabase refs. **ALL 5 HELPER LEAVES DONE (getAvailableSlug, resolveCustomFill, createNativeRegistrationForm, createLinearJourney, createFollowupJourney) → commit-ai-draft threads a client into ZERO helpers.** Only the `commit-ai-draft` ROOT itself remains (its own ~14 direct reads/writes over ai_generation_drafts CAS/webinars/event_briefs/campaigns/campaign_events + the reverse-order compensate()) — see the root bullet below.
  - events `commit-ai-draft` route — **`description_override` DRIFT RESOLVED (Option b); full supabase-js migration DEFERRED (root, not leaf)**. **Drift decision:** the event context leaf flagged `webinars.description_override` as a column in NO migration / NOT in `schema.ts` / NOT on the substrate, written at `commit-ai-draft/route.ts:275` (`description_override: structure.event_summary.description`) and read ONLY as the event twin's synthesized `null`. Verified on the substrate: all 19 other `webinarInsert` columns exist, `description_override` is the ONLY missing one — so the webinar INSERT **failed on the substrate solely because of it**. Chose **Option (b): drop the write** (not add a migration for a field nothing meaningfully consumes — YAGNI) and **retire the field end-to-end**: removed the write (route), removed `description_override` from `EventRow` + the synthesized-`null` in `event.drizzle.ts`, and simplified `event.ts` from `event.description_override ?? event.description` to `event.description` (BYTE-IDENTICAL — the coalesce never surfaced a non-null override in the migrated world, since the twin always synthesized null). No new `description_override` reader/writer remains anywhere. `route.test.ts` (15/15) unaffected (it never asserted the field); the event leaf harness stays green (byte-identical read); whole-project `tsc --noEmit` green. Verified on the substrate: drift confirmed (column absent), the migrated `webinarInsert` shape (minus the field) round-trips + is org_id-stamped, the SAME insert WITH `description_override` fails (proving the drop was required), and `loadEventContext` reads `description` unchanged + unknown-id tripwire: `scripts/local/events-commit-ai-draft-check.ts` (8/8, 0 defects). **Supabase migration DEFERRED — this route is a ROOT, not a leaf:** it constructs a service-role client (`createClient` from `@supabase/supabase-js`) and threads it into FIVE helper leaves — **3 now MIGRATED** (`getAvailableSlug` `campaigns/slug.ts` → `campaigns/slug.drizzle.ts`; `resolveCustomFill` `activation/create-draft-from-template.ts` → `activation/create-draft-from-template.drizzle.ts`; `createNativeRegistrationForm` `events/create-registration-form.ts` → `events/create-registration-form.drizzle.ts` [subtree: also migrated its two slug sub-leaves derive-slug + pickAvailableFormSlug] — see the helper-leaf entries above; the route imports all three off their twins and no longer passes them a client), **2 still on supabase-js** (`createLinearJourney` `events/build-linear-journey.ts`, `createFollowupJourney` `events/build-followup-journey.ts`) PLUS its own ~14 direct reads/writes over `ai_generation_drafts` (select + the #739/#1001 two-step atomic-CAS claim + terminal commit + finally reset), `webinars` (insert + GTW update), `event_briefs` (upsert), `campaigns` (insert + delete), `campaign_events` (insert + delete), `journeys` (campaign-link update), and the reverse-order JS `compensate()`. The client cannot be deleted while the 5 helpers require one, so `check:no-supabase` on this file cannot go green without migrating the whole subtree, and the transactional CAS/compensation core is a delicate rewrite that should NOT be bundled with a mechanical mass-migration. **Classification of the route's own reads/writes (teed up for the future leaf):** all are **org-scoped writes → `withUserId` + explicit `eq(org_id)`** (the `audiences/preset/starters` precedent), gated by the existing `draft.org_id !== sessionOrgId → 404` ownership check; the `ai_generation_drafts` CAS claims are `id`-keyed compare-and-swap on `status`/`committing_at` (keep the exact WHERE columns for race-safety). **Recommended sequence: migrate the 5 helper leaves first (each its own leaf), THEN this root** — `commit-ai-draft/route.ts` still imports `@supabase/supabase-js` (EXPECTED red until the subtree lands).
  - events **commit-ai-draft ROOT** **COMPLETE — the events/commit-ai-draft SUBTREE IS FULLY CLOSED** (`route.ts` + its new twin `commit-ai-draft.drizzle.ts` are 100% supabase-free; `check:no-supabase` over the whole `src/app/api/events/commit-ai-draft/` directory GREEN). With all 5 helper leaves already migrated, the route threaded a client into ZERO helpers, so this step migrated its OWN ~14 reads/writes to the new Drizzle twin `commit-ai-draft.drizzle.ts` and deleted the `@supabase/supabase-js` import + the `service()` factory. **Classified admin→`db`, NOT `withUserId`** (deliberately, correcting the earlier tee-up): the route is a service-role root that enforces tenant scope via the existing `draft.orgId !== sessionOrgId → 404` ownership gate + explicit `org_id` stamping on every insert, and its CAS is service-role bookkeeping that must run regardless of RLS — and the tables' RLS still keys off Supabase `auth.uid()` (not repointed until T018), so admin `db` is the only working equivalent, consistent with all 5 helpers. Every function takes an `exec` seam for the future withUserId flip (T018). **NON-TRANSACTIONAL BY DESIGN (key call):** the commit spans an external GoToWebinar API call + brand/event loads + AI email generation for up to 3 journeys (maxDuration 300s), so it is NOT wrapped in one `withUserId` transaction (that would pin a pooled connection + locks for minutes and can't enclose the 3 helper sub-transactions or the AI); the route keeps its JS-managed compensation (`createdIds` + reverse-order `compensate`), now running on `db` — the `compensationTableFor` type→table map moved into the twin's `deleteCreatedRef` (same types, journey_step/journey_edge still skipped as cascade). **CAS byte-identical:** `claimDraftReady`/`claimDraftStale`/`commitDraft`/`resetDraftToReady` reproduce the exact old WHERE columns (`status='ready'`; `status='committing' AND committing_at < staleBefore`; `committing_at = claimedAt`; `status='committing' AND committing_at = claimedAt`), a lost claim returns `rows.length>0 === false` → the 409, and the committing_at token threads claim→commit→reset unchanged (`mode:'string'` timestamptz compared server-side, no JS round-trip). **loadDraft micro-change (only observable diff):** the old `.single()` 404'd on BOTH a DB error and no-row; the twin returns `null` on no-row (→ same 404) but lets a genuine DB error throw to the outer catch (→ 500) — strictly more correct, race-neutral (nothing is created pre-claim). **No new drift:** verified every written column across ai_generation_drafts/webinars/event_briefs/campaigns/campaign_events on the substrate — all present (`committing_at` exists; `description_override` already retired in the drift bullet above), all CHECK literals (webinars status/event_type/platform; campaigns campaign_type='events'/status='draft'/initiative_kind='campaign') accepted. **Tests:** `route.test.ts` reworked off its stateful supabase fake onto twin mocks — the route test now asserts orchestration + call-args (which CAS fires, the token threading, converted timestamps, capture_form_id), and the CAS WHERE-clause semantics (a losing claim, the owner-guarded reset/commit, stale-lt takeover) moved to the substrate harness — 14/14 green (was 15; the standalone zombie route-test folded into the harness); whole `src/lib/events` + the route green; whole-project `tsc --noEmit` green. Broader `api/events` shows the SAME 5 PRE-EXISTING profile-leaf import-reds (add-confirm/add-followup/add-invitations-journey, generate-from-draft, regenerate-email via `event-copy-grounding`→`profile.drizzle`); commit-ai-draft is no longer among them. Independent adversarial review (CAS race-safety / compensation completeness / org ownership): **all three CLEAN, no correctness defect** (two considered-and-cleared notes: variable-passed insert objects skip TS excess-property checks — all 32 keys manually verified against the schema; and the committing_at round-trip is safe). Verified on the substrate over two orgs: the #739 double-claim loses, the #1001 stale takeover wins / recent doesn't, owner-guarded reset+commit (a WRONG committing_at token is a no-op), org_id stamping on all 4 write tables, the event_briefs UNIQUE(webinar_id) upsert idempotency, CHECK literals, linkJourneysToCampaign, updateWebinarGtw, deleteCreatedRef compensation (+ journey_step no-op), cross-tenant no-bleed, and the draft-ownership loadDraft proxy: `scripts/local/commit-ai-draft-check.ts` (30/30, 0 defects). `route.ts` + `commit-ai-draft.drizzle.ts` have 0 Supabase refs. **This closes the commit-ai-draft subtree entirely.** NOT the whole events area: 13 OTHER event routes remain on supabase (series, create, create-with-ai-draft, [id], [id]/finalize, [id]/registration-form, [id]/registrants(+check-in), add-invitations/confirm/followup-journey, generate-from-draft, regenerate-email) — each its own future root; repo-wide ~322 app/job files still import supabase-js (the T012 finish line). T011 remains open across those.
  - events **add-confirm-journey ROUTE** **COMPLETE** (closest sibling to the now-closed commit-ai-draft subtree). The journey helper (`createLinearJourney`) + brand/event context loaders were already migrated; this migrated the route's OWN last supabase touches — the webinar + event_briefs reads, the single-per-event phase guard, and the compensation delete — that ran through the SHARED `@/lib/events/load-event-generation-context`. Approach: (a) extracted that helper's provider-agnostic tail into `buildGenerationContextFromRows(webinar, brief)` (brand/org/industry/event/KB assembly), leaving the supabase-based `loadEventGenerationContext(supabase,…)` wrapper + `journeyExistsForPhase`/`compensateCreatedRows` UNCHANGED so the still-supabase `add-followup-journey` sibling is byte-identical/untouched; (b) added a route-local Drizzle twin `add-confirm-journey.drizzle.ts` (`loadEventGenerationContext` = the two reads + 404 gate → shared assembler; `journeyExistsForPhase`; `compensateCreatedRows` reusing commit-ai-draft's `deleteCreatedRef`); (c) route imports the twin, `@supabase/supabase-js` + `service()` deleted. **Classified admin→`db`, NOT `withUserId`** (same as commit-ai-draft): service-role root, explicit `webinar.org_id !== sessionOrgId → 404` ownership gate, `journeyExistsForPhase` scoped by `webinar_id` (a sibling event's journey on the same org can't satisfy it), RLS still on `auth.uid()` till T018; every twin fn takes an `exec` seam. **Byte-identical preserved:** response shape + 409-on-conflict + 500 paths; the two reads use raw `sql select` (snake_case rows) because prompt builders read `brief.key_message`/`webinar.registration_url` by DB column name. **Adversarial fix (byte-identical gap closed):** old `.single()` collapsed a malformed non-uuid `[id]` (Postgres 22P02) → 404; the raw read threw → a 500 leaking the parse error — now caught → 404 (brief-read errors likewise degrade to `{}`, matching the old ignored-`.maybeSingle()`-error). **Tests:** `route.test.ts` reworked onto a twin mock (like commit-ai-draft's) — this both keeps it supabase-free AND cuts the `event-copy-grounding→profile.drizzle→@/db/client` import cascade that had it among the 5 pre-existing import-reds → now GREEN (8/8); per-org/per-webinar existence + reverse-order compensation moved to the substrate harness. Whole-project `tsc --noEmit` green; `check:no-supabase` over `route.ts` + twin clean. Independent adversarial review (ownership gate / check-then-act race / compensation completeness): race + compensation CLEAN, one ownership-gate defect found + fixed (above). Verified on the substrate over two orgs: missing/malformed/foreign-org 404 tripwire, owner passes the gate, per-webinar+per-phase existence (a busy org's confirm journeys never false-positive a fresh event), cross-tenant no-bleed, reverse-order compensation (non-cascading campaign_email + phase-tagged journey both deleted, journey_step no-op): `scripts/local/add-confirm-journey-check.ts` (13/13, 0 defects). **This closes the add-confirm-journey route.** Remaining event roots on supabase: 12 (series, create, create-with-ai-draft, [id], [id]/finalize, [id]/registration-form, [id]/registrants(+check-in), add-invitations-journey, add-followup-journey, generate-from-draft, regenerate-email).
  - events **add-followup-journey ROUTE** **COMPLETE** (twin of the closed add-confirm-journey; the last of the three journey-recovery routes' pair migrated). Structurally identical to add-confirm-journey — same shared `loadEventGenerationContext`/`journeyExistsForPhase('followup')` guard+re-check/`compensateCreatedRows` calls, same `service()` root — differing only in the journey builder (`createFollowupJourney` from build-followup-journey.ts, NOT createLinearJourney) and content prompt (`buildEventEmailContentPrompt` → body_html blob + EmailContentSchema, NOT the slot-based event_invite shape); the attendance-blind `webinar_ended` trigger + attended/no_show branch fan-out is untouched. **ZERO logic duplication:** the twin `add-followup-journey.drizzle.ts` is a thin RE-EXPORT of the confirm twin's three phase-generic impls (`journeyExistsForPhase` already takes the phase param; `loadEventGenerationContext`/`compensateCreatedRows` are phase-agnostic), so followup inherits the confirm twin's byte-identical behavior for free — including the malformed-non-uuid-`[id]` → 404 catch and the brief-read-error → `{}` degradation. (Noted follow-up: if either route root is later retired, promote the three impls to a neutral `src/lib/events/*.drizzle` module and re-point both twins — a one-line move.) Route imports its own `./add-followup-journey.drizzle`; `@supabase/supabase-js` + `service()` deleted. **Classified admin→`db`, NOT `withUserId`** (same rationale as confirm): service-role root, explicit `webinar.org_id !== sessionOrgId → 404` gate, `journeyExistsForPhase` scoped by `webinar_id` (a confirm journey on the same webinar does NOT satisfy the followup guard — different phase value), RLS on `auth.uid()` till T018, `exec` seam inherited. **Byte-identical:** response shape + 409-on-conflict + 500 paths + the followup branch/trigger. **Tests:** `route.test.ts` reworked onto a twin mock (`jest.mock('./add-followup-journey.drizzle', …)`) — this both keeps it supabase-free AND cuts the `twin → confirm twin → event-copy-grounding → profile.drizzle → @/db/client` import cascade that had it among the 5 pre-existing import-reds → now GREEN (8/8, both branches' per-step content + reverse-order compensation asserted). Whole-project `tsc --noEmit` green; `check:no-supabase` over `route.ts` + twin clean. Independent adversarial review (ownership gate / check-then-act race / compensation completeness): **all three CLEAN, no defect** — the gate + race are inherited byte-for-byte from the already-reviewed confirm twin, and compensation was re-verified against createFollowupJourney's actual `created` types (`followup_journey` → journeys, `campaign_email` → campaign_emails, `journey_step` → skip-cascade — all routed identically to the old `endsWith('_journey')`/`campaign_email` map). Verified on the substrate over two orgs: missing/malformed/foreign-org 404 tripwire, owner passes the gate, per-webinar+per-phase('followup') existence (confirm phase on the same webinar correctly false; a busy org's followup journeys never false-positive a fresh event), cross-tenant no-bleed, reverse-order compensation (campaign_email + phase-tagged journey both deleted, journey_step no-op): `scripts/local/add-followup-journey-check.ts` (13/13, 0 defects). **This closes the add-followup-journey route** and both single-journey recovery siblings (confirm + followup). Remaining event roots on supabase: 11 (series, create, create-with-ai-draft, [id], [id]/finalize, [id]/registration-form, [id]/registrants(+check-in), add-invitations-journey, generate-from-draft, regenerate-email).
  - events **add-invitations-journey ROUTE** **COMPLETE** (the third + last journey-recovery route; all three recovery siblings now closed). STRUCTURALLY DIFFERENT from the confirm/followup pair — invitations is NOT audience-blind and NOT single-per-event, so it never used the shared `loadEventGenerationContext`/`journeyExistsForPhase`/`compensateCreatedRows` at all; it inlined its own supabase reads. Migrated its SIX own supabase touches to a route-local twin `add-invitations-journey.drizzle.ts`: (1) the webinar read + org gate; (2) an AUDIENCE read + org gate (invitations-specific — a picked audience, client-supplied id); (3) a PER-AUDIENCE existence guard `(webinar_id, phase='invitations', audience_id)` — a different shape from the single-per-event `journeyExistsForPhase`, because the confirm/followup unique index does NOT cover phase='invitations'; (4) the event_briefs read; (5) the `MAX(priority_order)` siblings read that feeds the #746 retry-on-unique-conflict loop; (6) the inline reverse-order compensation. **Twin design:** 5 invitations-specific raw snake_case reads (byte-identical row shape for the prompt builders / eventCtx) each with the `exec` seam, + `compensateCreatedRows` delegating to commit-ai-draft's `deleteCreatedRef` — imported from the **commit-ai-draft** twin (only @/db/client + schema), NOT the confirm twin, so the module stays free of the shared context-assembler's KB import chain and the harness needs **no env shim** (just DATABASE_URL). The route KEEPS its already-migrated direct context-loader imports (brand/org/industry/event/event-copy-grounding) — those carry no supabase. `@supabase/supabase-js` + `service()` deleted. **Classified admin→`db`, NOT withUserId** (same as every event root): service-role root, TWO explicit ownership gates (`webinar.org_id !== sessionOrgId → 404` + `audience.org_id !== orgId → 404`), all journeys reads scoped by webinar_id, RLS on auth.uid() till T018, `exec` seam throughout. **Byte-identical:** response shape (incl. 400 audience_id-required, both 404s, per-audience 409, the #746 bounded-retry + constraint-name scrub, 500) + the whole priority_order MAX+1 retry loop + the per-audience fan-out; the malformed-non-uuid-id → 404 catch is applied to BOTH the webinar and (client-supplied) audience reads. One considered-and-cleared micro-divergence: the per-audience guard's `limit 1 → 409` vs the old `.maybeSingle()` differs only if ≥2 duplicate journeys already exist for an audience (an invariant violation) — the new 409 is strictly safer; identical for the real 0/1 cases. **Tests:** `route.test.ts` reworked onto twin mocks + a new `@/lib/events/event-copy-grounding` mock (the one remaining unmocked leaf whose `event-copy-grounding → profile.drizzle → @/db/client` chain was THIS route's pre-T011 import-red) → now GREEN (12/12, incl. all four priority-race retry-loop cases). Whole-project `tsc --noEmit` green; `check:no-supabase` over `route.ts` + twin clean. Independent adversarial review (ownership gate / check-then-act race / compensation completeness): **all three CLEAN, no defect** — gates collapse missing/malformed/foreign → 404; the #746 retry loop is preserved byte-for-byte (siblings re-read each attempt); compensation routes invitations' actual `created` types identically to the old inline map. Verified on the substrate over two orgs: webinar + audience ownership tripwires (missing/malformed/foreign → null), per-audience existence (audience-scoped AND webinar-scoped, cross-tenant no-bleed), the sibling-priority read the MAX+1 loop consumes (only invitations phase, no-bleed), and reverse-order compensation (campaign_email + invitations_journey both deleted, journey_step no-op): `scripts/local/add-invitations-journey-check.ts` (19/19, 0 defects). **This closes the add-invitations-journey route and ALL THREE journey-recovery siblings.** Of the 5 pre-existing profile-leaf import-reds, 3 are now green (the recovery trio); 2 remain (generate-from-draft, regenerate-email — each its own future root). Remaining event roots on supabase: 10 (series, create, create-with-ai-draft, [id], [id]/finalize, [id]/registration-form, [id]/registrants(+check-in), generate-from-draft, regenerate-email — plus [id]/registrants check-in as a sub-route).
  - events **generate-from-draft ROUTE** **COMPLETE** (the AI-generation half of the draft lifecycle; commit-ai-draft is the commit half). SIMPLER than the journey-recovery roots: it creates NO journeys/campaign_emails (all scaffolding is at commit time), so there is NOTHING to compensate — no deleteCreatedRef reuse, deliberately. Migrated its THREE own supabase touches to a route-local twin `generate-from-draft.drizzle.ts`: (1) the draft read (`ai_generation_drafts` by client-supplied draft_id → the route's org gate + committed/committing status checks); (2) an org-scoped audiences names read (id→name for the picked `brief.audience_ids`); (3) the terminal payload/status write (status → 'ready'). **Twin:** `loadDraftForGeneration` (raw snake_case select + malformed-uuid → null catch), `loadAudienceNames` (drizzle `inArray` builder, org-scoped, degrades to `[]` on a bad-uuid cast error — byte-identical to the old `.in()` error → `{}`; NOTE: an initial raw `any(${ids}::uuid[])` returned 0 rows due to array-param encoding, switched to the query builder), `updateDraftReady` (raw `update … set status,payload where id`, returns `false` on error so the route keeps its own 'Failed to save generated content' 500 rather than the raw error hitting the generic outer catch). Each takes the `exec` seam. Route KEEPS its already-migrated direct context-loader imports (brand/org/industry + event-copy-grounding) — no supabase. `@supabase/supabase-js` + `service()` deleted. **Classified admin→`db`, NOT withUserId**: service-role root, explicit `draft.org_id !== sessionOrgId → 404` gate (+ committed→400 / committing→409 status checks) kept in the route off the loaded row, audiences read org-scoped, RLS on auth.uid() till T018. **Byte-identical:** all response shapes (400 invalid-body, 404 draft-not-found, 400 already-committed, 409 committing, 503 spend-cap, 502 validation/generation, 500 save-failed) + the multi-audience reconciliation + per-email step_id pinning + the payload jsonb. The malformed-non-uuid draft_id → 404 catch matches the old `.single()`. **Tests:** `route.test.ts` reworked onto a twin mock + a new `@/lib/events/event-copy-grounding` mock (the one remaining unmocked leaf whose `event-copy-grounding → profile.drizzle → @/db/client` chain was THIS route's pre-T011 import-red) → now GREEN (5/5). Whole-project `tsc --noEmit` green; `check:no-supabase` over `route.ts` + twin clean. Independent adversarial review (ownership gate / check-then-act race / compensation): **all CLEAN, no defect** — the gate collapses missing/malformed → 404; the status-check→payload-write TOCTOU (#739) is a PRE-EXISTING design characteristic the route documents, preserved byte-identical (unconditional `update where id`, not newly guarded, to stay byte-identical); compensation is N/A (no rows created). Verified on the substrate over two orgs: draft ownership tripwire (missing/malformed → null, owner presents its org_id), org-scoped audiences read (drops unknown + malformed ids, cross-tenant no-bleed), payload/status write round-trip + sibling-draft isolation: `scripts/local/generate-from-draft-check.ts` (15/15, 0 defects). **This closes generate-from-draft.** Of the 5 pre-existing profile-leaf import-reds, 4 are now green (the recovery trio + generate-from-draft); only regenerate-email remains. Remaining event roots on supabase: 9 (series, create, create-with-ai-draft, [id], [id]/finalize, [id]/registration-form, [id]/registrants(+check-in), regenerate-email).
  - events **regenerate-email ROUTE** **COMPLETE** (the LAST of the 5 pre-existing profile-leaf import-reds — all 5 now GREEN). Regenerates ONE email on a 'ready' draft and swaps it into payload.emails; creates no journeys → NOTHING to compensate. TWO supabase touches migrated to a route-local twin `regenerate-email.drizzle.ts`: (1) the draft read (client-supplied draft_id → org gate + status='ready' check; selects `payload` too, unlike generate-from-draft's read) → `loadDraftForRegen` (raw snake_case select + malformed-uuid → null catch, `exec` seam); (2) the terminal write, which is NOT a plain update but the #746 atomic **`update_draft_email(uuid, text, jsonb)` stored function** (a single `jsonb_set` that rebuilds payload.emails from the row's CURRENT value, so two concurrent regen calls for different step_ids can't clobber) → `updateDraftEmail` calls the SAME function verbatim via `exec.execute(sql\`select update_draft_email(...)\`)` (NOT reimplemented as a read-modify-write, preserving the race guarantee), returning `false` on a DB error so the route keeps its own 'Failed to save regenerated email' 500 without leaking. Route KEEPS its already-migrated context-loader imports (brand/org/industry + event-copy-grounding). `@supabase/supabase-js` + `service()` deleted. **Classified admin→`db`, NOT withUserId**: service-role root, explicit `draft.org_id !== sessionOrgId → 404` gate + status check kept in the route; the stored function is SECURITY INVOKER + id-scoped (the route verifies ownership on read, the write trusts the same draft_id — unchanged). **Byte-identical:** all response shapes (400 invalid-body, 404 draft-not-found, 400 wrong-status, 404 step-not-found, 503 spend-cap, 500 validation/generation/save-failed, 200 {email}) + the three-journey step walk + dash-scrub; malformed draft_id → 404 matches the old `.single()`. **Tests:** `route.test.ts` reworked onto twin mocks (loadDraftForRegen + updateDraftEmail, recording the swap call the way the old test recorded the rpc call) + a new `@/lib/events/event-copy-grounding` mock (the last unmocked leaf, this route's import-red) → GREEN (7/7). Whole-project `tsc --noEmit` green; `check:no-supabase` over `route.ts` + twin clean. **ALL 5 formerly-import-red sibling route tests now pass together (40 tests, 5 suites).** Independent adversarial review (ownership gate / check-then-act race / persistence): **all CLEAN, no defect** — the gate collapses missing/malformed → 404; the #746 atomic swap is preserved byte-for-byte by calling the same stored function; persistence surfaces a fixed no-leak 500 on error and needs no compensation. Verified on the substrate over two orgs — crucially exercising the REAL update_draft_email function (which the mocked unit test cannot): draft ownership tripwire (missing/malformed → null, owner presents org_id + payload), atomic swap replaces ONLY the targeted step_id and preserves siblings + array length, a non-matching step_id is a true-returning no-op, and cross-tenant no-bleed (a swap on orgA's draft never touches orgB's): `scripts/local/regenerate-email-check.ts` (12/12, 0 defects). **This closes regenerate-email and eliminates the entire class of 5 pre-existing profile-leaf import-reds.** Remaining event roots on supabase: 8 (series, create, create-with-ai-draft, [id], [id]/finalize, [id]/registration-form, [id]/registrants (+ its check-in sub-route)).
  - events **AREA SWEEP — 7 of the last 8 roots migrated via a parallel multi-agent workflow** (migrate → adversarial-verify pipeline, one agent per route, each isolated to its own dir; coordinator integrated centrally). Migrated + twins added: **series** (event_series get-or-create upsert), **create** (webinars insert), **create-with-ai-draft** (ai_generation_drafts insert), **[id]/registration-form** (webinars+captured_forms), **[id]/registrants** (webinar_registrants read), **[id]/registrants/check-in** (webinars+webinar_registrants toggle+recount), and **[id]** (400L multi-method: webinars+captured_forms+landing_pages → `id.drizzle.ts`). Each: admin `db` + explicit org gate + `exec` seam + raw snake_case reads + malformed-uuid handling, `@supabase`/`service()` deleted, test reworked onto twin mocks + the event-copy-grounding cascade cut, substrate harness added. **Central gate GREEN:** whole-project `tsc --noEmit` 0 errors; all 14 events test suites (154 tests) pass; every route's harness PASS 0 defects (series/registrants/registration-form/create-with-ai-draft/create/[id]/check-in); `check:no-supabase` over events clean except finalize. **Adversarial verify verdicts:** 4 clean (series, create, create-with-ai-draft, registration-form); 3 with findings — **check-in** had a MEDIUM (genuine-DB-error 500 echoed drizzle's `DrizzleQueryError.message`, leaking the SQL text + bound org/webinar/registrant ids) + a LOW (count/attendee_count errors flipped silent-200 → 500, a false-failure after the check-in already committed) — **both FIXED byte-identical in the coordinator pass** (re-throw the underlying pg error `e.cause` for the concise message + no leak; count/attendee writes made best-effort → 200, matching the old unchecked supabase writes); **registrants + [id] GET** each had a LOW (raw `select *` under node-pg default parsers returns `numeric` cols as JS strings — incl. `0`→`"0"` — and `timestamptz` as ms-precision `Z` form, vs PostgREST's JSON numbers + µs `+00:00`), latent (the only in-repo consumers read byte-identical columns) — **DOCUMENTED as a known follow-up, not fixed**: it is a spec-wide raw-`select *` characteristic (any route returning a webinars/registrants row wholesale), so the fix belongs in one place (explicit typed column lists or a node-pg numeric/timestamptz parser policy), not per-route. **[id]/finalize — needs-manual (NOT migrated), real blocker:** its own 4 touches are trivially migratable, but at route.ts:137 it calls `markAttendance({ supabase, … })`, and `src/lib/events/mark-attendance.ts` (203 lines, contact_activities/contacts/campaign_events + tag/attribution/webinar_ended journey enrolment) REQUIRES a `SupabaseClient` and has no Drizzle twin — and is ALSO called by the still-on-supabase GTW attendees route. **Prerequisite:** give `markAttendance` a Drizzle path (a `mark-attendance.drizzle` twin with an `exec` seam, or an overload taking `exec`) as its own task; then finalize migrates trivially. No files written for finalize (correct bail-out over a broken 'migrated' that still imports @supabase). **Repo-wide supabase-js: 317 → 310 shipped files** (events area now 8 → 1, only finalize). Remaining event roots on supabase: 1 (**[id]/finalize**, blocked on the markAttendance twin). The bulk of T011 is now OUTSIDE events: lib/insights (~30), inngest (~29), lib/billing (~19), api/email (~17), api/integrations (~15), + ~20 more areas — each its own area-sweep.
  - events **[id]/finalize ROUTE** **COMPLETE — the LAST events API root; ZERO `src/app/api/events/**` routes remain on supabase** (verified by `check:no-supabase`). finalize was the one route the earlier AREA SWEEP bailed on (its `markAttendance({ supabase })` call at route.ts:137 had no Drizzle path). Built the teed-up prerequisite — **`src/lib/events/mark-attendance.drizzle.ts` (`markAttendanceExec(input, exec)`)** — a SEPARATE twin, NOT an overload, because the supabase `markAttendance` stays live for the still-on-supabase GTW attendees root (`api/integrations/gotowebinar/attendees`), its only other real caller (`inngest/journey-matching.ts` only name-drops it in comments). The twin reproduces all 5 DB touches — the (contact, org, activity_type, `metadata->>'webinar_id'`) idempotency existence read, the contact_activities insert (jsonb metadata via the drizzle query builder), the tags read, the campaign_events attribution read, and the contacts tags/last-touch update — and copies the two NON-DB side effects (the `contact/changed` inngest send + the two `journey/enroll` fetches) BYTE-IDENTICAL incl. their best-effort try/catch. **Classified admin `db`, NOT withUserId**: org scoping replicated EXACTLY where supabase had it (activity read/insert + tags read org-scoped; the campaign_events read is event-scoped and the contacts update is `id` + `erased_at is null`-scoped, byte-identical to the original which did NOT org-scope those two — documented in the twin header), `exec` seam throughout for the T018 withUserId flip. Migrated finalize's OWN 4 touches to **`finalize.drizzle.ts`** (`loadWebinarForFinalize` with the org gate + title; the atomic claim scheduled→ending; the org+webinar-scoped registrants read; the CAS-guarded ending→ended terminal write) — CAS byte-identical (the claim matches only `status='scheduled'` → a lost race returns false → the route's `already_finalizing`; the terminal write matches only `status='ending'`), and a genuine db error on either write is re-thrown as the concise pg message (`e.cause ?? e`, no SQL/param leak) to the route's outer catch → 500, distinct from the 0-rows outcome, byte-identical to the old `if (claimErr)`/`if (statusErr)` 500s. `@supabase/supabase-js` + `service()` deleted from route.ts. **Tests:** `route.test.ts` reworked off the stateful supabase fake onto twin mocks — now asserts the route's ORCHESTRATION (which twin fires, the claim-before-markEnded order, the per-registrant `markAttendanceExec` call-args + skipped-unmatched, and the #742 dual-auth org threading) — 12/12 green. **Gate:** whole-project `tsc --noEmit` 0 errors; the full events jest area 240/241 (the 1 red is a PRE-EXISTING stale contract test — `src/lib/insights-v2/events/setup-contract.test.ts` asserts the already-migrated **series** route's OLD supabase source, broken by commit 85d492b2, NOT this change); `check:no-supabase` over the finalize dir + both twins clean. Verified on the substrate over two orgs: the finalize load/claim/registrants/markEnded ownership + CAS tripwires (a foreign-org caller loses the claim WITHOUT flipping another tenant's row; a non-'ending' webinar is a terminal no-op via the CAS guard) and markAttendanceExec's activity insert + idempotent no-dup re-run + opposite-tag removal on a flip + campaign-link attribution (`last_touch_initiative_id`) + contact-scoped existence read — `scripts/local/finalize-check.ts` (20/20, 0 defects; the journey-enrol fetch pinned to a dead port so the harness can't hit prod). **This closes the ENTIRE events API-route area** (finalize was root #8 of 8; the AREA SWEEP closed the other 7). Two shared LEAF-libs still import supabase — `src/lib/events/mark-attendance.ts` (kept for the GTW attendees root) and `load-event-generation-context.ts` — each sheds supabase only when its LAST non-events caller migrates, not as its own root. T011's remaining mass is entirely OUTSIDE events (~311 app/job refs by the official check): lib/insights, inngest, lib/billing, api/email, api/integrations, + ~20 more areas, each its own area-sweep. **T011 remains open.**
  - integrations **gotowebinar/attendees SYNC ROUTE** **COMPLETE — and it drops `src/lib/events/mark-attendance.ts` off supabase too** (`check:no-supabase` count 311 → 309; the route + mark-attendance.ts both gone). This was the **last real caller of the supabase `markAttendance()`** (finalize now uses `markAttendanceExec`; check-in never called it) — switched its per-registrant call to `markAttendanceExec` and then **FOLDED `mark-attendance.ts`**: deleted the SupabaseClient-based `markAttendance()` + `MarkAttendanceInput` + the `@supabase/supabase-js` type import, leaving only the shared `AttendanceSource`/`MarkAttendanceResult` types the twin re-exports. **Second blocker found + solved (not in the tee-up):** the route also passed its supabase client to `getValidGtwToken(supabase, orgId)` — the encrypted-token helper — so a `getValidGtwTokenExec(orgId, exec)` **twin** was required to shed supabase (same split as markAttendance: `getValidGtwToken` has 6 callers, the other 5 GTW routes still on supabase keep `token.ts`). The twin ports the delicate refresh path BYTE-IDENTICAL: the 5-min expiry buffer, the in-process refresh de-dupe (#794), the compare-and-swap on `expires_at` (drizzle `update … returning id`, `rows.length===0` = the old `count===0`), `reReadIfFresher`, and the force-persist fallback — with `expires_at` read as `::text` so the CAS/reRead string comparisons match the old PostgREST semantics (two node-pg `Date` objects for the same instant are `!==` by reference, which would break the race guard). Migrated the route's OWN 5 touches to **`attendees.drizzle.ts`**: `loadWebinarForSync` (org gate), `gtwIntegrationConnected` (the explicit connected pre-check), `updateWebinarSyncStats` (the absolute-snapshot counts + performance write, with the #764 no-clobber recording_url as a conditional SQL fragment — `id`-scoped byte-identical, the old update had no org predicate), `loadCanonicalContactByEmail` (oldest-first #768), and `upsertRegistrant` (raw `insert … on conflict (webinar_id, email) do update`). `@supabase/supabase-js` + `service()` deleted from the route. **Tests:** `route.test.ts` reworked onto twin mocks — auth-gate (#761) + recording-pull (#764) preserved, plus a new registrant-loop test (matched → upsert + markAttendanceExec, unmatched → upsert only, correct breakdown); the old supabase `mark-attendance.test.ts` **ported** to `mark-attendance.drizzle.test.ts` (idempotency short-circuit / tag-swap / webinarKey-in-metadata / currentTags-skip, now on a mock `exec`, camelCase payloads); `paginate-gtw.test.ts` given a `@/db/client` stub (importing the route for the pure `paginateGtw` now transitively loads the client, which throws without DATABASE_URL). **Gate:** whole-project `tsc --noEmit` 0 errors; the gotowebinar+integrations+events jest area 447/448 (the 1 red is the SAME pre-existing `insights-v2/events/setup-contract.test.ts` series stale-contract test, not this change); `check:no-supabase` no longer lists the route OR mark-attendance.ts (309 total; `token.ts` remains — 5 other callers). Verified on the substrate over two orgs: loadWebinar ownership tripwire, gtwIntegrationConnected no-bleed, updateWebinarSyncStats + #764 no-clobber (recordingUrl:null leaves the existing URL, non-null overwrites), loadCanonicalContactByEmail org-scope, upsertRegistrant insert + ON CONFLICT in-place update (no dup), and getValidGtwTokenExec read path (not-connected → GtwTokenError; valid future-expiry plaintext → token) — `scripts/local/gtw-attendees-check.ts` (18/18, 0 defects). **NOTE — token refresh/CAS path:** the substrate harness covers the read path only; the HTTP-refresh + CAS path (hits GoTo's live OAuth endpoint) is the byte-identical port of the already-tested `token.ts` logic, not exercised end-to-end here. Remaining GTW routes on supabase: 5 (register, disconnect, webinars, + the webhooks/create-webinar libs) — each keeps `token.ts` until it migrates. **T011 remains open** — mass still outside events/GTW (lib/insights, inngest, lib/billing, api/email, + ~20 areas).
  - integrations **GoToWebinar AREA SWEEP — 5 remaining getValidGtwToken callers migrated via a parallel multi-agent workflow + `token.ts` folded** (`check:no-supabase` 309 → 303). Fan-out (one agent per file, migrate → adversarial-verify pipeline, disjoint files in the main tree; coordinator integrated centrally), scoped by an inline pre-scout that resolved the interdependencies (disconnect's `teardownGtwWebhooks` call is unchanged because webhooks.ts keeps its orgId-only public signatures; register's `isContactErased(supabase)` inlined into its twin; create-webinar has NO own DB touches). Migrated + twins: **register** (register.drizzle — contact_activities/contacts/integrations/webinar_registrants/webinars, 9 touches + inlined erased-check, ON CONFLICT (webinar_key,email) upsert, IDOR fail-closed), **webinars** (webinars-list.drizzle — integrations + webinars upsert/attendee-count/list), **webhooks.ts LIB** (webhooks.drizzle — 4 integrations touches; setup/teardown public signatures preserved), **disconnect** (disconnect.drizzle — 2 integrations touches), **create-webinar** (token-only, no twin). Each switched `getValidGtwToken(supabase,orgId)` → `getValidGtwTokenExec(orgId)`, deleted `@supabase`/`service()`, reworked its test onto twin mocks, added a substrate harness where it has stateful writes. **All 5 adversarial verdicts CLEAN (0 issues).** **token.ts FOLDED:** with its last caller gone, deleted the SupabaseClient `getValidGtwToken` + refresh/CAS machinery, keeping only `GtwTokenError` (imported by token.drizzle + create-webinar). **The old supabase `token.test.ts` (11 tests: decrypt/return, refresh sends DECRYPTED token, encrypt-on-persist, the #794 CAS race — single-flight, CAS-lost-reRead, force-persist-no-winner, GoTo-reject recovery) was PORTED to `token.drizzle.test.ts`** onto a scripted mock `exec` (bound params read back via drizzle `PgDialect` for the encrypted-persist assertions) — this CLOSES the earlier caveat that getValidGtwTokenExec's refresh/CAS path was unexercised. **Central integration (coordinator, post-workflow):** whole-project `tsc --noEmit` 0 errors — which surfaced a PRE-EXISTING latent type error in committed `events/[id]/id.drizzle.ts:117` (`set[prop]` where `prop: keyof typeof webinars.$inferInsert` widened to include `symbol`) that the incremental `tsconfig.tsbuildinfo` cache had been masking; fixed with a byte-safe `prop as string` cast + cleared the stale cache so the full typecheck is honest. Also fixed 3 sibling tests that transitively load `@/db/client` now that the lib pulls a twin (`paginate-gtw.test`, `webhooks/route.test`, and the webhooks lib test) via a `jest.mock('@/db/client', () => ({ db: {} }))` stub; `webhooks/route.test.ts` (the deep setup-logic integration test) re-pointed off its supabase fake onto `webhooks.drizzle` + `token.drizzle` mocks, all 5 setup-orchestration cases preserved. **Gate:** full clean `tsc` 0 errors; the gotowebinar+integrations+events jest area 449/450 (the 1 red is the SAME pre-existing `insights-v2/events/setup-contract.test.ts` series stale-contract test); `check:no-supabase` no longer lists any of the 5 files OR token.ts (303 total). Verified on the substrate: `gtw-register-check` (31 checks), `gtw-webinars-check` (11), `gtw-webhooks-check`, `gtw-disconnect-check` — all PASS 0 defects; `token.drizzle.test` 11/11. Remaining GoToWebinar roots on supabase: **3** (`callback`, `connect` OAuth routes + the `webhooks/gotowebinar` inbound handler) — none call getValidGtwToken, each its own future root. **Workflow cost:** 10 agents, ~971k tokens, ~21 min wall-clock. **T011 remains open** — mass still outside events/GTW (lib/insights ~30, lib/billing ~19, api/email ~17, forms-v2 ~24, + ~20 areas).
  - **lib/insights AREA + its dependency web** **COMPLETE — 34 files off supabase via 4 chained multi-agent workflows** (`check:no-supabase` 303 → 269). **Scope reality (honest):** lib/insights is the hub of a connected component — the 30 helpers are a THREADED layer (every one took a `supabase: SupabaseClient` param and they call each other, driven by ~15 external callers), NOT independent roots like GTW. Migrating them cascaded through 4 leaf-blocker callees + ~40 caller files; **~75 files touched total.** **Convention (uniform across all agents):** replace each helper's `supabase: SupabaseClient` param with `exec: DbExecutor = db` IN PLACE (no separate twin — these ARE the data functions), translate reads to `exec.execute(sql\`…\`)` (raw snake_case, byte-identical WHERE + org scope, `::text` on timestamptz a caller treats as string) / writes via the drizzle query builder; internal insights→insights calls re-pointed to `exec`; external callers pass `db`. **WF1 (30 insights helpers, 60 agents):** 22 CLEAN + 8 flagged — 5 were pure external-caller hand-offs, 2 were genuine cross-boundary blockers (engagement-summary/generate→`contacts/memory/generate`, plan-option→`marketing-framework/readiness-queries`), 1 a low-severity `::text` timestamp-format nit (traced consumers unaffected). **WF2 (3 leaf blockers + 21 caller flips):** migrated `contacts/memory/generate` (+ contact_memory upsert via builder), `readiness-queries` (8 fetchers), `initiative-detail/queries` (10 reads). **WF3 (scoreboard):** `initiative-detail/queries` pulled in `scoreboard/queries` (5 exports, self-contained LEAF — cascade terminates) + 18 more caller flips (activation/plan/programmes pages, scoreboard UI, insights-v2 overviews). **Central integration (coordinator):** fixed a shared generic-loader typing bug (`exec.execute<T>` needed `<T extends Record<string,unknown>>` + `as T[]`) in channel-glance/contributor-metrics/today-health; drove full clean `tsc` from 58 → 32 → 0 errors across 4 passes (`rm tsconfig.tsbuildinfo` each time); fixed 14 sibling test suites — 12 needed a `jest.mock('@/db/client', () => ({ db: {} }))` stub (transitive client load), the deliverability route test reworked to feed the migrated helper via a `db.execute` mock (preserving its coercion coverage), and 2 stale source-grep contract assertions updated (`ask-server-contract` `auth.supabase`→`db`; the `series` one is PRE-EXISTING, not this change). **Gate:** full clean `tsc` 0 errors; the insights+scoreboard+marketing-framework+initiative-detail+contacts/memory+insights-v2 jest area **473/474** (the 1 red is the same pre-existing `insights-v2/events/setup-contract.test.ts` series stale test); `check:no-supabase` no longer lists ANY `src/lib/insights/*` file (269 total, −34 = 30 insights + 4 blockers). Verified on the substrate: the 5 writer harnesses — `insights-{engagement-summary,feature-settings,recommendation-action,recommendations,signal-state}-check` — all PASS 0 defects (readers gated by reworked unit tests + check). **Known follow-ups (disclosed, non-blocking):** several route tests fully mock their insights helper so the migrated exec read path isn't CI-driven (pre-existing test design — the substrate harnesses + reworked unit tests cover the SQL); the `::text` vs ISO timestamp format on a few raw reads (cohorts/engagement-dormant) is latent, traced consumers don't re-parse. **Workflow cost:** 4 workflows, ~110 agents, ~13M tokens. **T011 remains open** — ~269 files across ~18 areas still on supabase (inngest, lib/billing, api/email, forms-v2, lib/activation, insights-v2's own reads, + more).
  - **api/email AREA (PARTIAL) — 14 of 17 routes off supabase via a multi-agent workflow; 3 deep/hot routes deliberately DEFERRED** (`check:no-supabase` 269 → 255, −14; email area 17 → 3). **Scope reality (honest, opposite of insights):** the email routes are independent ROOTS (each mints its own `createClient`/`service()`; none takes a `supabase:` param), so an inline pre-scout classified the area rather than fanning blindly. **10 clean self-contained roots** (own `.from` only) + **4 Bucket-B routes** that pass their client into a shared LEAF helper + **2 leaf-helper twins**; the remaining **3 (`send-campaign`, `generate`, `sync-suppression`) were STOPPED-and-reported before spend** because they cascade 2 levels deep into HOT cross-cutting primitives shared with inngest/proxy/auth/ai (`prepare-send`→`billing/access-state` — `resolveOrgAccessState` has 7 callers incl `proxy.ts`/`auth.ts`/`ai/client.ts`; `sync-suppression` lib→`unsubscribeContact`; `generate`→`resolveDefaultPresetSnapshot`+`structured-editor-entitlement`+`brand/revision`) — each its own careful future pass, NOT a blind fan-out. **Convention:** each root migrated its OWN touches to a co-located `<name>.drizzle.ts` twin (admin `db` + `exec: DbExecutor = db` seam, raw snake_case reads, org-scoped, malformed-uuid→null, concise pg re-throw `e.cause ?? e`), deleted `@supabase`/`service()`, reworked its test onto twin mocks, added a substrate writer harness. The **2 leaf helpers got SEPARATE twins** (the mark-attendance/token split, NOT in-place) because their external callers stay on supabase: `render-preview.drizzle.ts` (`renderEmailPreviewExec`) — `render-preview.ts` untouched, still used by `inbox-render-lifecycle.ts`; `resolve-campaign-recipients.drizzle.ts` (`computeRecipientBreakdownExec`/`countUnsubscribedBypassedExec`) — original untouched (`SupabaseLike=any`, never in the count), still used by `inngest/functions.ts:1780`. `inbox-render`+`preview` switched to `renderEmailPreviewExec`; `recipient-count`+`transactional-bypass-check` to the recipients twin (`preview` had ZERO own touches → pure swap, no twin). **Workflow:** 30 agents (2 twins barrier → 14 routes migrate→adversarial-verify pipeline), ~2.9M tokens, ~44 min, 0 errors. **9 CLEAN, 3 MEDIUM findings — ALL the same known systemic raw-read timestamptz drift** (node-pg wire text `2026-.. +00`, space/non-ISO, vs the old PostgREST ISO `…T…+00:00`). The verifiers' "date column → JS Date → `String(new Date())` BST corruption" repro was OVER-STATED: an empirical substrate probe shows node-pg/drizzle returns `date` as `'YYYY-MM-DD'` (byte-identical) and `timestamptz` as the non-ISO string — **only timestamptz drifts**. **Fixed byte-identical + SCOPED (zero global blast):** `inbox-render.drizzle.ts` (`SELECT_COLUMNS` + the builder `.returning()`) and `inbox-render-id.drizzle.ts` (`RENDER_COLUMNS`) now emit `created_at/submitted_at/completed_at`(+`expires_at`) via `to_json(col)#>>'{}'` (exact PostgREST ISO, matters because these are browser-poll responses a strict parser would reject); `render-preview.drizzle.ts`'s contact read switched `select *` → `select to_jsonb(contacts) as row` (serializes every column PostgREST-identically — timestamptz→ISO, date→string, numeric→number, jsonb→nested — so a contact-persona preview + its paid-Mailgun-Inspect renderHash stay stable). Substrate-verified (contact to_jsonb probe: date `'2026-05-01'`, ts `…T…+00:00` µs, numeric JSON number). **The one-place systemic fix (a node-pg timestamptz type-parser policy in `@/db/client`) is left as a documented follow-up — NOT smuggled into this batch** (repo-wide blast radius across the ~20 already-migrated twins; the prior sweeps teed it up as its own decision). **Gate:** `rm tsconfig.tsbuildinfo` → full clean `tsc --noEmit` 0 errors; all 10 email writer harnesses PASS 0 defects (`email-{cancel-schedule,import-html,inbound,inbox-render,inbox-render-id,templates-custom,templates-id-fill,validate-address,validate-audience,verify-domain}-check`); the `api/email` jest area **23 suites / 220 tests green**; no sibling `@/db/client` test-load breakage (no external test imports the 2 new twins); `check:no-supabase` 255. **T011 remains open** — email area has 3 routes left (Bucket C, deferred) + mass still outside (inngest, lib/billing, forms-v2, lib/activation, + more).
  - **accounts+contacts+team ROOTS AREA — 10 clean service-role roots off supabase via a multi-agent workflow** (`check:no-supabase` 255 → 245, −10). **Scope reset (start of a fresh drive):** re-measured the remaining surface and pinned T011's actual scope to the **non-inngest** data plane — `src/inngest/**` (29 files) is **T023/US5**, storage is T021/US4, auth is T015/US3, RLS-repoint is T018/US2 — so the real T011 remainder is **224 files** (253 supabase files − 29 inngest), not the raw 255 the CI gate counts. Substrate brought up fresh (`local:up` + `local:replay`, pg17 :5435 + Azurite, smoke green) to verify against. **Files (all independent ROOTS — module-level service-role `createClient(SERVICE_ROLE_KEY)`/`service()`, orgId from `requireAuth()` SESSION never the body/query, explicit `org_id` predicate):** `accounts/{create, import-jobs/status, link-contact, list, set-primary-contact, unlink-contact, update}` + `contacts/{bulk-update, import}` + `team/accept-invite`. **Classified ADMIN `db`, NOT withUserId** (the service-role client bypasses RLS by design; scope rests on the preserved `org_id` predicate + the `requireAuth` gate), `exec: DbExecutor = db` seam on every extracted fn for the T018 flip. **Deliberately NOT in this sweep — the 5 COUPLED contacts/team roots** (`contacts/{create, erase, preferences, update}`, `team/send-invite`): each threads its client into a not-yet-migrated **inngest-shared** helper (`contacts/erasure-suppression`, `contacts/validate-field-values`, `sms/consent`, `email/system-notify`) → deferred to those helpers' own sweeps (twin-split, keep-original-for-inngest, the mark-attendance pattern). **Convention:** co-located `<name>.drizzle.ts` twin (admin `db` + exec seam, org-scoped byte-identical, malformed-uuid→null, concise pg re-throw `e.cause ?? e`, no SQL/param leak), `.or('name.ilike…,website.ilike…')`→drizzle `or(ilike…)` with the #354 metachar strip preserved, `count:'exact'`→drizzle count, `.range()`→`.limit/.offset`, timestamptz shaped via `to_json(col)#>>'{}'`/`to_jsonb(row)` for PostgREST-identical ISO; deleted `@supabase`/`createClient`/`service`, reworked each sibling `route.test.ts` onto twin mocks. **Workflow:** 20 agents (10 migrate → 10 adversarial-verify pipeline), ~1.68M tokens, ~18 min, 0 errors. **9 CLEAN + 1 findings** — `accounts/update`'s op2 zero-row update returns `{account:null}`/200 vs the original `.single()` 500, but it is **UNREACHABLE** (the route gates on op1 `accountBelongsToOrg`'s identical `id`+`org_id` predicates → 404 first) → accepted; the rest are the known-systemic pg-vs-PostgREST 500-message wording + timestamptz-offset nuances (non-blocking). **Gate:** `rm tsconfig.tsbuildinfo` → full clean `tsc --noEmit` 0 errors; `check:no-supabase` over all 3 dirs = 0 (full 245); jest `accounts`+`contacts/{bulk-update,import}`+`team/accept-invite` = **5 suites / 52 tests green**; substrate harnesses **9/10 PASS 0 defects** (`accounts-create`, `accounts-import-jobs-status`, `accounts-list`, `accounts-update`, `link-contact`, `set-primary-contact`, `unlink-contact`, `bulk-update`, `contacts-import` — the last after correcting a harness assertion that type-checked a raw node-pg `timestamptz` read as a string instead of the Date it returns). **⚠️ NEW SUBSTRATE-DRIFT BLOCKER surfaced (first profiles-write ever exercised on the substrate):** `team/accept-invite`'s profiles upsert can't run on the substrate — migration `20260714200000_profiles_guard_trust_sql_editor.sql`'s trigger `profiles_guard_role_org_change` uses **double-quoted `"current_user"`** (parsed as a non-existent column on plain pg17) where it needs the unquoted `current_user` builtin → `column "current_user" does not exist` on EVERY profiles insert/update **regardless of client** (the old supabase-js path fails identically) — a schema-replay/drift bug (T028-adjacent), NOT a T011 migration defect. accept-invite's twin is verified by tsc + adversarial-verify-clean + its reworked unit suite + the harness's non-write assertions; the profiles-WRITE path stays substrate-unverified until the one-line `"current_user"`→`current_user` fix lands (unblocks all future profiles-touching sweeps: team, onboarding, some contacts). Fix deferred to the drift lane. **T011 remains open** — 245 refs across ~17 non-inngest areas (lib/billing, forms-v2, lib/activation, lib/insights-v2, lib/admin, lib/contacts, api/{integrations,forms-v2,agents,track,onboarding,chat,webhooks}, lib/sms, lib/ai, the 3 hot email routes, + Batch 11 `.rpc`).
  - **agents/chat-feedback + chat/[orgId] + onboarding + track ROOTS AREA — 8 clean roots fully off supabase + 1 partial, via a multi-agent workflow** (`check:no-supabase` 245 → 237, −8). **Files:** `agents/chat-feedback/{route, summary, promote}`, `chat/[orgId]/{config, resolve}`, `onboarding/{brand-pull, dismiss-banner}`, `track/{settings, test-event}`. **Classification — admin `db` uniformly** (each mints a SERVICE_ROLE `createClient`; the 3 MIXED routes — `onboarding/brand-pull`, `onboarding/dismiss-banner`, `track/test-event` — ALSO had a `createServerSupabaseClient` SESSION client, whose call sites were migrated to `db` + `exec` seam too, NOT withUserId, because RLS still keys off `auth.uid()` until T018 → withUserId returns 0 rows today; **the former-RLS tenant scope was made EXPLICIT (`org_id = <server-derived orgId>`) on every such site** so bare `db` can't drop isolation — verified: all 9 isolation-safe, zero former-session sites left without an explicit predicate). orgId source preserved from the original: `requireAuth()` (agents/track), the `[orgId]` PATH param (public chat endpoints), or the session — never the body. **Batch 11 `.rpc` progress:** 3 files carry 4 stored-proc calls, translated `.rpc('fn',{args})` → `exec.execute(sql\`select fn(p_arg => ${v})\`)` with NAMED args (`save_chat_message_feedback`; `promote_chat_message_feedback` + `set_chat_feedback_promotion_status` + `remove_chat_feedback_promotion` — all SECURITY DEFINER, filter by `p_org_id` internally; the unique-violation `23505 → 409` branch preserved via re-thrown `e.cause`). **User guardrail honored** ("do not touch the algorithm, only swap the client"): every branch/status-code/response byte-identical; the verifier explicitly graded `algorithmByteIdentical`. **`promote` is PARTIAL** — its 8 own data touches are on Drizzle, but it RETAINS `@supabase` + the module-level service-role `createClient` solely to hand the client to `ingestKnowledgeSource({ supabase })` (shared `src/lib/kb/ingest-source.ts`, also used by `/api/kb/ingest` — a separate lane); it sheds supabase when that kb lane migrates (the mark-attendance/token keep-original pattern), so it stays in the CI count for now. **Workflow:** 18 agents (9 migrate → 9 adversarial-verify pipeline), ~1.7M tokens, ~27 min, 0 errors. **7 CLEAN + 2 findings — BOTH FIXED at integration** (byte-identity, per the guardrail): (1) **`chat/[orgId]/config` MEDIUM** — `loadAgentConfig` used `limit 1` + `rows[0]`, returning an arbitrary first row when ≥2 enabled `website` agent_configs match, but the original `.maybeSingle()` sets `data=null` on >1 row → route 404; **fixed** to `limit 2` + return the row only when exactly one matched (0/≥2 → null). (2) **`promote` 2×LOW** — `loadAssistantMessageCreatedAt` + `loadAgentConfig` lacked try/catch, so a genuine DB error threw the raw `DrizzleQueryError` (SQL + bound UUIDs) into the log, vs the original folding it into a friendly throw with the raw error discarded; **fixed** to swallow → null (mirroring `loadPriorUserMessage`), reproducing the original's friendly throw with no SQL/param leak. Remaining notes accepted/documented (systemic pg-vs-PostgREST error-string wording in logs/`detail`; timestamptz UTC-session assumption; `chat/resolve` inlines `embed-auth.ts`'s constant-time token compare as a follow-up until that shared helper migrates; `chat/config`'s `agent_configs.visitor_capture` jsonb is a pre-existing drift, documented-not-fixed per guardrail). **Gate:** `rm tsconfig.tsbuildinfo` → full clean `tsc --noEmit` 0 errors; `check:no-supabase` 237 (scoped over the swept dirs = only `promote/route.ts`, the kb handoff); substrate harnesses **9/9 PASS 0 defects** (`chat-feedback-promote` incl. the 3-rpc path, `chat-feedback`, `chat-feedback-summary`, `chat-config`, `chat-resolve`, `brand-pull`, `dismiss-banner`, `track-settings`, `test-event` — the mixed-session ones scoped to their own tables to dodge the profiles `current_user` drift); jest area **5 suites / 21 tests green**. **Deferred (coupled to not-yet-migrated inngest-shared helpers → go with those sweeps):** `chat/[orgId]` main route, `agents/drafts/[id]/send`, `onboarding/provision`, `track/{conversion,identify,website}`, `webhooks/{gotowebinar,mailgun,stripe}`. **T011 remains open** — 237 refs across ~16 non-inngest areas.
  - **integrations OAuth/token ROOTS AREA — 10 clean roots off supabase + 1 partial (Storage), via a multi-agent workflow** (`check:no-supabase` 237 → 227, −10). **Files:** `canva/{callback, designs, import}`, `gotowebinar/{callback, connect}`, `linkedin/{callback, post}`, `salesforce/{auth, callback, disconnect, sync}`. **Security-critical batch** (each read/writes the `integrations` table holding ENCRYPTED third-party OAuth tokens) — a dedicated adversarial-SECURITY verify graded `tokenSecuritySafe` + `encryptionUntouched` on top of isolation/byte-identity. **All 11 verified isolation-safe + token-secret-safe + encryption-untouched:** the `@/lib/encryption` encrypt/decrypt boundary is byte-identical (twin only moves ciphertext), every `integrations` touch keeps its exact `org_id` predicate, and the `UNIQUE(org_id, provider)` arbiter confines every upsert to the caller's own provider row (a conflict can't resolve onto another tenant's token). GTW roots mirror the already-migrated sibling `token.drizzle.ts`/`attendees.drizzle.ts`. **Classification admin `db`** uniformly (SERVICE_ROLE createClient; orgId server-derived from the OAuth `state` param / session / requireAuth, never the body), `exec` seam throughout. `salesforce/disconnect` translated 1 `.rpc` stored proc; `salesforce/sync` ENQUEUES an Inngest event — the `inngest.send` + algorithm were left untouched, only its 2 own DB ops migrated. **Workflow:** 22 agents (11 migrate → 11 adversarial-verify pipeline), ~2.06M tokens, ~23 min, 0 errors. **10 CLEAN + 1 findings** (`linkedin/callback` LOW — the empty catch swallows a connection-level throw too; `byteId=True`, an accepted faithful preservation of the original's silent-discard upsert). **REAL twin bug caught by the substrate harness + FIXED:** `canva/import`'s `insertCanvaContentAsset` bound the `tags text[]` column via `${input.tags}::text[]`, but drizzle's `sql` template EXPANDS a JS array into a comma-separated value list → the single element bound as a scalar → `malformed array literal: "canva"` (would break the real content-asset insert, not just the harness). **Fixed** to `array[${input.tags}]::text[]` (a SQL array constructor over the expanded bound params — safe, no manual escaping, handles the empty case as `array[]::text[]`); harness green. This is the adversarial-verify miss the substrate harness exists to catch. **`canva/import` is PARTIAL** — 2 of its 4 touches are Supabase **STORAGE** (`.storage.from('content-assets').upload/getPublicUrl`), which is **T021/US4 (Azure Blob)**, NOT the T011 data plane; it migrated its 2 DB touches and RETAINS the client for Storage only (precedent: `content-storage-cleanup`), so it stays in the CI count until the storage wave. **⚠️ NEW DRIFT surfaced (3rd, alongside `webinars.description_override` + profiles `current_user`):** `linkedin/callback`'s upsert writes an `integrations.metadata` column that exists in NO schema/migration/substrate (16 cols, verified) → `42703 undefined_column` swallowed → the LinkedIn grant never persists locally, byte-identical old+new (PostgREST rejected it too). Documented (memory `env-integrations-metadata-drift`), NOT fixed per the guardrail; open question for the drift lane whether prod carries an out-of-migration `metadata` column. **Gate:** `rm tsconfig.tsbuildinfo` → full clean `tsc --noEmit` 0 errors; `check:no-supabase` 227 (scoped over `integrations` = only `canva/import`, the Storage handoff); substrate harnesses **11/11 PASS 0 defects** (canva ×3, gtw-callback/connect, linkedin ×2, salesforce ×4 — cross-tenant token tripwires); jest `api/integrations` area **14 suites / 111 tests green**. **Deferred (coupled):** all `admin/*` (thread into unmigrated `@/lib/billing/*`), `agents/drafts/[id]/send`, `sms/{broadcast,send}`. **T011 remains open** — 227 refs across ~15 non-inngest areas (lib/billing, forms-v2, lib/activation, lib/insights-v2, lib/admin, lib/contacts, lib/sms, lib/ai, api/{forms-v2,admin,chat-main,track,webhooks}, the 3 hot email routes + remaining `.rpc`).
  - **BIG CLEAN-ROOTS BATCH — 24 independent files migrated in ONE workflow** (`check:no-supabase` 227 → 207, −20). Batched deliberately (owner: stop the 10-at-a-time dribble — too much overhead per file). **Selection method:** computed the clean set programmatically = every ROOT that imports ZERO still-on-supabase helper (28 candidates), minus `canva/import` (Supabase Storage → T021, already T011-done) and the 4 inngest-coupled CRM libs (`salesforce`, `crm/field-mapping`, `dynamics` → a CRM twin-split sweep) = **24**. **Files:** 18 API routes + server pages — `forms-v2/{landing-pages/[id], .../slug, prefill, view}`, `event/[id]/{page, ics, join, recording, checkin/page}`, `f/[slug]/thanks`, `page/[slug]/thanks`, `dashboard/events/[id]/checkin-poster`, `contacts/preferences` (~17 ops), `journey/delete`, `request-access`, `auth/callback`, `webinar-campaigns/create-in-gtw`, `auth/org-name-available` — + 6 standalone libs — `kb/search` (pgvector), `admin/team`, `activation/{outcome-projection, spend-cap}`, `ai/rate-limit/research`, `billing/consumption`. **Classification admin `db`** (former-session sites → db + explicit org predicate; storage/auth calls retained where out of scope). **Workflow:** 48 agents (24 migrate → 24 verify pipeline), ~4M tokens, ~43 min, 0 errors, **24/24 verify CLEAN, 0 findings**. **3 kept supabase, all scope-correct:** `auth/callback` + `admin/team` retain the client only for Supabase **Auth** calls (`auth.exchangeCodeForSession` / `auth.admin.listUsers`+`inviteUserByEmail` → T015; their data-plane touches ARE migrated), and `billing/consumption` bailed cleanly (coupled to unmigrated `ledger`/`rate-card` → goes with the billing sweep). **Central-integration fixes:** (real code) `webinar-campaigns/create-in-gtw`'s twin input `registration_url: string → string | null` (the column is nullable + the GTW result is nullable — a real tsc break); (test) `forms-v2/prefill/route.test.ts` mock spread-arg typing. **3 substrate-harness assertion fixes — ALL confirmed twin-correct by an in-tree probe (the twins were right, the harnesses over-strict):** `contacts-preferences` (the harness tried to un-erase a contact via `set erased_at = null`, blocked by the substrate's `contacts_erased_immutability_guard` GDPR trigger — dropped the illegal reset; the erased-guard assertion itself passed), `landing-page-id` (compared jsonb `blocks` by raw `JSON.stringify` → failed on **jsonb key-order normalization**, not data loss — added a recursively key-sorted `canon()` compare; the twin round-trips blocks byte-identically, proven by probe), `request-access` (the leak-check wrongly flagged the **relation name** in a normal NOT-NULL constraint message as a SQL leak — tightened to real statement/bound-param patterns). **Gate:** `rm tsconfig.tsbuildinfo` → full clean `tsc --noEmit` **0 errors**; `check:no-supabase` **207**; batch substrate harnesses **22/22 PASS 0 defects**; jest touched areas **919 pass**. **2 PRE-EXISTING reds (NOT this batch, flagged for a test-rework lane):** `lib/ai/context/{brand,industry-brand-contract}.test.ts` still `jest.mock('@supabase/supabase-js')` for loaders already **committed-migrated** to Drizzle (`brand.ts` has 0 supabase refs) → stale, same class as the long-noted `insights-v2/events/setup-contract.test.ts`. **T011 remains open** — 207 refs; the remainder is now almost entirely the **threaded-helper cascade domains** (lib/billing ~19, forms-v2 submission chain, lib/contacts, lib/insights-v2, lib/activation, lib/admin, lib/sms, CRM salesforce/dynamics) + the hot 4 (`proxy`/`supabase.ts`/`supabase-server.ts`/`access-state`/`ai/client`) + 3 hot email routes + remaining `.rpc` — each needs a domain sweep (in-place `exec` seam / twin-split for inngest-shared), not a flat fan-out.
  - **forms-v2 + insights-v2 + content + org-profile CLEAN-ROOTS BATCH — 6 independent service-role roots migrated via parallel subagents** (`check:no-supabase` 131 → 125, −6). Selection: the largest cleanly-independent set of standalone roots (each mints its own `SUPABASE_SERVICE_ROLE_KEY` client, orgId from `requireAuth()`/session/path, explicit `org_id`/id predicate, imports zero still-on-supabase helper). **Files:** `insights-v2/recommendations/[id]/lifecycle` (1 stored fn `mutate_insights_v2_recommendation(p_org_id,p_recommendation_id,p_user_id,p_action_type,p_snooze_days)`), `forms-v2/[id]/definition` (captured_forms read), `forms-v2/domains/confirm` (tracking_settings read + jsonb `authorised_domains` upsert), `forms-v2/landing-pages/[id]/export` (landing_pages + captured_forms + content_assets reads), `content/analyse` (content_assets read), `organisation-profile/research` (organizations.name read — verified NO write path: research returns drafts to the client, persistence is a separate route). **Convention (uniform):** co-located `<name>.drizzle.ts` twin, admin `db` + `exec: DbExecutor = db` seam (NOT withUserId — RLS still keys off `auth.uid()` till T018), raw snake_case `exec.execute(sql\`…\`)` reads, `.maybeSingle()/.single()`→`.limit(1)+rows[0]??null`, `.upsert(onConflict)`→`insert … on conflict … do update`, `.rpc(fn,{args})`→`exec.execute(sql\`select fn(p_arg => …)\`)` with NAMED args calling the SAME SECURITY-DEFINER fn verbatim (org+status guard preserved, NOT a reimplemented read-modify-write), `.in()`→drizzle `inArray` (param-bound, dodges the comma-expansion bug), jsonb (`authorised_domains`/`blocks`/`settings`) bound/returned byte-identical, malformed-uuid/DB-error on a client-supplied id → null (route's existing 404), `requireAuth`/auth kept (T015). **Deliberate per-route error semantics:** domains/confirm re-throws the concise pg error to keep its DISTINCT read-failed/write-failed 500 branches (its old `.single()` surfaced errors as 500, unlike the drafts precedent whose old `.single()` collapsed error→null); definition/lp-export collapse a malformed-uuid to their existing 404 (their old `.single()`/`.maybeSingle()` ignored the error → null), documented per file. **Verified centrally:** whole-project `tsc --noEmit` 0 errors (stale `tsconfig.tsbuildinfo` cleared); `check:no-supabase` 125 (all 6 routes clean); 6 new substrate harnesses (`insights-v2-lifecycle`/`forms-v2-definition`/`forms-v2-domains-confirm`/`forms-v2-lp-export`/`content-analyse`/`org-profile-research`-check.ts) all PASS 0 defects over two orgs each (per-org correctness, cross-tenant no-bleed, unknown + malformed-id tripwires, upsert in-place idempotency, stored-fn mutate + action-log + already-closed guard, jsonb round-trip via key-order-insensitive compare); the 2 routes with existing tests (content/analyse, organisation-profile/research) reworked onto twin mocks (2 suites/12 tests); broad `forms-v2`+`insights-v2`+`content`+`organisation-profile` jest sweep 11 suites/89 tests green (no transitive `@/db/client` import-red). **T011 remains open** — 125 gate references (import LINES, not files) remain: ~29 `src/inngest/**` (T023/US5), ~10 auth-plane (T015/US3: proxy/supabase/supabase-server/admin-auth/auth-callback/settings-access + the SSR pages), 1 storage (canva/import, data already done → T021/US4), ~35 type-only helper shims whose data is already migrated but keep the `SupabaseClient` TYPE import until their inngest callers migrate (T023), ~13 comment-only `.drizzle.ts` twins the gate flags purely on explanatory comments (recommend: make `check-no-supabase.sh` skip comment lines — one change clears all 13 + prevents recurrence), leaving roughly two-to-three dozen genuine data-plane roots: the hot email trio (generate/send-campaign/sync-suppression), ai/client, crm/salesforce/run-mapping (inngest-shared, twin-split), the 435-line preferences (19 touches), track/*, webhooks/{mailgun,gotowebinar,stripe}, sms/{send,broadcast,usage}, kb/ingest, agents/*, contacts/{create,erase,update}, team/send-invite, activation/publish, unsubscribe/one-click, event/[id]/checkin, the two public `f|page/[slug]` pages, forms-v2/{submit,ingest,typeform-webhook,domains} — each a future clean-root batch or a domain twin-split. **⚠️ FULL gate-green (T012) is architecturally gated on T015 (auth) + T021 (storage) + T023 (inngest), not on T011 effort alone** — the type-only shims cannot shed their import until inngest stops passing them a `SupabaseClient`.
  - **forms-v2 public/index pages + unsubscribe + landing-page publish — 5 clean roots migrated INLINE (no fan-out, per user directive)** (`check:no-supabase` 125 → 120, −5). First no-fan-out grind batch: every root done sequentially in-session (twin + route/page rewire + test rework + substrate harness + tsc/jest/prettier gate), no multi-agent workflow. **Files:** the two PUBLIC service-role pages `f/[slug]/page.tsx` (captured_forms-by-slug + form_views fire-and-forget insert) and `page/[slug]/page.tsx` (landing_pages-by-slug + the #1348 org-scoped embedded-form guard + content_assets batch); the MIXED index page `dashboard/forms-v2/landing-pages/page.tsx` (profiles org-lookup + landing_pages list + form_submissions/website_events metrics — KEEPS `createServerSupabaseClient` for `getSession()` only, T015); `api/unsubscribe/one-click` (email_unsubscribe_tokens lookup; `unsubscribeAllForContact` already on db); `api/forms-v2/landing-pages/[id]/publish` (landing_pages load + slug-assign + publish flip). **Convention:** co-located `.drizzle.ts` twin, admin `db` + `exec: DbExecutor = db` seam (RLS still on auth.uid() till T018), raw snake_case reads, `inArray`/`array[${sql.join}]::uuid[]` for id lists (NOT the broken `any(${arr}::uuid[])`), jsonb round-trip (settings/blocks/after_submission), timestamptz shaped `to_json(col)#>>'{}'` where a string flows to a client component. **Byte-identical error semantics preserved per-route:** public pages collapse error→null→notFound; publish's load THROWS on malformed-uuid/DB-error (→ route 500) vs null-on-no-row (→ 404) and its slug-assign returns `{ok,isUniqueViolation}` for the 23505 retry; unsubscribe collapses ignored-error→null (honour-token-regardless-of-age). The #1348 org guard now lives inside `loadEmbeddedForm(id, orgId)` (substrate-verified). **Tests:** `page/[slug]/page.test.ts` (#1348 regression) + `unsubscribe/one-click/route.test.ts` + `publish/route.test.ts` all reworked off supabase-js fakes onto twin mocks (11/11). **Gate:** whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 120 (all 5 files gone from the gate); prettier clean; jest over the touched areas 87/87; two substrate harnesses `forms-v2-public-pages-check.ts` (30 checks) + `unsub-publish-check.ts` (16) PASS 0 defects over two orgs (per-org correctness, cross-tenant no-bleed incl. #1348 + landing-slug global-uniqueness 23505, jsonb + ISO shaping, the malformed-id throw-vs-null contract, first-publish-vs-republish published_at idempotency; profiles fixture inserted under `session_replication_role=replica` to dodge the `"current_user"` guard-trigger drift). **T011 remains open** — 120 gate refs; genuine remaining data-plane roots ≈28 (activation/publish, event/[id]/checkin [capture-guard coupled], the hot email trio generate/send-campaign/sync-suppression, contacts/{create,erase,update}, team/send-invite, sms/{send,broadcast,usage}, track/{conversion,identify,website}, webhooks/{mailgun,gotowebinar}, kb/ingest, preferences, forms-v2 submission chain [ingest/submit/typeform], chat/[orgId], agents/drafts/[id]/send, onboarding/provision, crm salesforce/field-mapping/run-mapping, ai/client) + the T015/T021/T023-gated shims/comments that cannot shed the import until auth/storage/inngest migrate.
  - **activation/publish + forms-v2/submit + kb/ingest — 3 more clean roots migrated INLINE (no fan-out)** (`check:no-supabase` 120 → 117, −3). Second no-fan-out grind batch (sequential, in-session). **Files:** `api/activation/publish` (the biggest — 10 touches: a MIXED root using a SESSION client for RLS reads + a SERVICE client for the bundle-flip writes; migrated to admin `db` with EXPLICIT `org_id` predicates on the formerly-RLS-scoped journey + initiative-gate reads so a caller can't publish another org's journey by id; the writes keep their org predicates; twin covers journey/initiative activate + the compensating rollback + capture-surface flip + audience/member backfill reads); `api/forms-v2/submit` (a key-auth server-to-server root — 4 own touches: API-key-by-hash resolution [key IS the auth, org resolved FROM the key, NOT org-scoped — byte-identical], sliding-window rate count + insert, last_used_at touch; the durable pipeline `runServerSubmission` was already on `db`); `api/kb/ingest` (DEAD-CODE removal — its module-level service-role `createClient` was constructed but NEVER used, since `ingestKnowledgeSource({orgId, sourceId})` already runs on `db`; deleted with zero behaviour change, no twin). **Convention:** co-located `.drizzle.ts` twin, admin `db` + `exec: DbExecutor = db` seam, byte-identical error semantics (activation's journey load THROWS on malformed-uuid/DB-error → route 500 vs null-on-no-row → 404; the two activate writes return `{ ok, message? }` carrying a CONCISE pg message [`(e.cause ?? e).message`, not the DrizzleQueryError SQL+params dump] so the route keeps its `Failed to activate …: <msg>` body without leaking bound params; the submit reads collapse ignored-error → null/0 as before); timestamptz `revoked_at` shaped `to_json()#>>'{}'`. **Tests:** `activation/publish` had NO test — added an 8-case orchestration test (auth gate / not-found / already-active idempotency / wrong-status 409 / happy flip / initiative-fail→rollback→500) on twin mocks; `forms-v2/submit` + `kb/ingest` tests reworked off supabase-js fakes onto twin mocks (kb's dead-client mock deleted). **Gate:** whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 117 (all 3 files gone from the gate); prettier clean; jest `api/activation`+`api/forms-v2`+`api/kb` 11 suites / 88 tests; two new substrate harnesses `activation-publish-check.ts` (18 checks — cross-tenant journey/initiative/audience no-bleed, malformed-id throw-vs-null, activate/rollback/capture-surface flips) + `forms-v2-submit-check.ts` (8 — key-by-hash incl. revoked-ISO, sliding-window count excludes out-of-window hits, record + last_used_at) PASS 0 defects. **T011 remains open** — 117 gate refs; genuine remaining data-plane roots ≈25 (event/[id]/checkin [capture-guard coupled], the hot email trio, contacts/{create,erase,update}, team/send-invite, sms/{send,broadcast,usage}, track/{conversion,identify,website} [reactivate-archived/system-notify coupled], webhooks/{mailgun,gotowebinar}, preferences, forms-v2/{ingest,webhooks/typeform} [run-pipeline/ensure-captured-form/capture-guard coupled], chat/[orgId], agents/{chat-feedback/promote [kb handoff],drafts/[id]/send}, onboarding/provision, crm salesforce/field-mapping/run-mapping, ai/client) + the T015/T021/T023-gated shims. Coupled roots need their inngest-shared helper twin-split first.
  - **contacts cluster (create + update + erase) — the first COUPLED-root batch, migrated INLINE (no fan-out)** (`check:no-supabase` 117 → 114, −3). These are the three roots that thread a client into the inngest-shared `contacts/erasure-suppression` + `contacts/validate-field-values` helpers, so they were deferred until those helpers' Drizzle `…Exec` twins existed — they now do (`isEmailErasedExec`/`getErasedEmailHashesExec` in `erasure-suppression.drizzle.ts`, `validateContactFieldValuesForOrgExec` in `validate-field-values.drizzle.ts`, both admin `db` + `exec` seam, FAIL-CLOSED throw semantics preserved, the supabase originals kept live for the T023 inngest callers). **`contacts/create`:** dropped its module-level service-role `admin` client + the session-client field-validation, repointed to `isEmailErasedExec(orgId, email)` + `validateContactFieldValuesForOrgExec(orgId, custom_fields, {isNewContact})`; `createContactWithDefaults(db,…)` was already on Drizzle. **`contacts/update`:** new co-located `update.drizzle.ts` twin — `fetchContactForUpdate` (the org-ownership read-back, malformed-uuid→null→404) + `updateContact` (the DYNAMIC org-scoped update: the route's conditional allowlist assembled via `sql.identifier` + per-value binding — a plain object → `${JSON.stringify}::jsonb` for `custom_fields`, a JS array → `array[…]::text[]` constructor for `tags` [a bare `${arr}::text[]` fails: a text param can't cast to text[]], scalars bind direct; `to_jsonb(contacts)` for the PostgREST-identical snake_case + ISO response; partial-update safety preserved so omitted columns are never nulled). **`contacts/erase`:** new co-located `erase.drizzle.ts` twin over the security-critical GDPR path — `fetchErasureSeed` (session→admin `db`, explicit `org_id` predicate is now what enforces tenant scope), `resolveErasureComponent` + `eraseContact` invoke the SAME `resolve_erasure_component`/`erase_contact` SECURITY-DEFINER functions verbatim via `select … from fn(...)` (no logic reimplemented); `p_component` is a JS ARRAY so it is `JSON.stringify`'d + cast `::jsonb` (node-pg would otherwise emit an array literal, not JSON); the supabase `{data,error}` → Drizzle THROW is caught in the retry loop and branched on `(err.cause ?? err).message` (`already erased`→409, `erasure_conflict`→bounded retry, else 500), preserving the exact status/branch semantics + the concise-pg-message-only rule. Both routes drop the session `supabase` from the `requireAuth`/`requireOwner` destructure. **Tests:** all three `route.test.ts` reworked off supabase-js/admin-client fakes onto twin mocks (create's field-validation test runs the REAL `validateContactFieldValuesForOrgExec` against a fake `exec` so the option/required checks stay genuinely exercised) — **30 tests / 3 suites green**. **Gate:** `rm tsconfig.tsbuildinfo` → whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 114 (all 3 routes gone from the gate); prettier clean; three new substrate harnesses `contacts-create-check.ts` (10 — suppression HIT/case-normalise/MISS, cross-tenant no-bleed, fail-closed on malformed org, select-option + required-field validation, org-scoped defs), `contacts-update-check.ts` (12 — owning/foreign/bad-uuid fetch, partial-update-no-null, jsonb + text[] round-trip, cross-tenant write TRIPWIRE), `contacts-erase-check.ts` (10 — seed gate, component resolution + foreign-org EMPTY component, a REAL end-to-end erase: erased_at set + email tombstoned + erasure_suppressions row written + erasure_requests id, idempotent re-erase gate; actor seeded via `auth.users`+`profiles` INSERT which doesn't fire the `"current_user"` guard-trigger drift) all **PASS 0 defects** over two orgs. **T011 remains open** — 114 gate refs; genuine remaining data-plane roots ≈22 (event/[id]/checkin, the hot email trio, team/send-invite, sms/{send,broadcast,usage}, track/{conversion,identify,website}, webhooks/{mailgun,gotowebinar}, preferences, forms-v2/{ingest,webhooks/typeform}, chat/[orgId], agents/{chat-feedback/promote,drafts/[id]/send}, onboarding/provision, crm salesforce/field-mapping/run-mapping, ai/client) + the T015/T021/T023-gated shims/comments.
  - **agents/drafts/[id]/send — the agent-draft-reply Mailgun send root, migrated INLINE (no fan-out)** (`check:no-supabase` 114 → 113, −1). Third grind batch; a self-contained root whose only shared coupling — `isContactErased` (the F10 #1436 fail-closed erased-recipient gate) — already had its Drizzle twin (`isContactErasedExec` in `contacts/erased-state.drizzle.ts`), so all that remained was the route's OWN cross-table surface. **File:** new co-located `send.drizzle.ts` twin (admin `db` + `exec: DbExecutor = db` seam) carrying: `fetchDraftForSend` (the `agent_reply_drafts` load WITH its `agent_configs(*)` embed rebuilt as `to_jsonb(d)`+`to_jsonb(ac)` over the `agent_id` FK — COLLAPSES a malformed-uuid/no-row/DB-error → null so the route keeps its `.single()`→`error||!draft`→404, incl. the org-mismatch 404 that stays indistinguishable from not-found), `fetchOrgSendConfig` (the `mailgun_*`+`email_from_*` send-config load → null on no-row/error → 404), and the six POST-SEND writes `markDraftSent`/`markInboundReplied`/`fetchLatestConversationId`/`insertChatMessage`/`updateConversationLastMessage`/`insertContactActivity` — all made **BEST-EFFORT (catch + log, never throw)** to preserve the supabase originals' exact resilience (they awaited `{data,error}` results that never threw, so a post-send DB hiccup silently continued rather than turning an already-sent email into a 500); `contact_activities.metadata` bound `${JSON.stringify(...)}::jsonb`. Route rewired to the twin + `isContactErasedExec` + `resolveOrgSendTarget`; dropped the module-level service-role `createClient` and the `supabase` argument threaded into the erased check. The `domain` fallback kept byte-identical (cast to `string` to match the original's implicit-`any` org row). **Tests:** NEW `route.test.ts` (none existed) — 9 cases on twin + `isContactErasedExec` + `mailgunSend` mocks (auth passthrough, not-found 404, cross-org 404, already-sent 409, org-not-found 404, erased-recipient 409-never-sends, happy send marks draft/inbound + threads reply + logs activity with the jsonb metadata, no-conversation skip, Mailgun-failure 500-doesn't-mark-sent). **Gate:** `rm tsconfig.tsbuildinfo` → whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 113 (route gone from the gate); prettier clean; jest 9/9; new substrate harness `drafts-send-check.ts` (17 checks over two orgs — draft+embed load, malformed/unknown-id → null, org send-config load + unknown → null, draft→sent + inbound→replied flips, newest-conversation selection + the FOREIGN-org cross-tenant tripwire, chat-message append + last_message_at bump, contact-activity jsonb round-trip) **PASS 0 defects**. **T011 remains open** — 113 gate refs; genuine remaining data-plane roots ≈21 (event/[id]/checkin, the hot email trio, team/send-invite, sms/{send,broadcast,usage}, track/{conversion,identify,website}, webhooks/{mailgun,gotowebinar}, preferences, forms-v2/{ingest,webhooks/typeform}, chat/[orgId], agents/chat-feedback/promote, onboarding/provision, crm salesforce/field-mapping/run-mapping, ai/client) + the T015/T021/T023-gated shims/comments.
  - **agents/chat-feedback/promote — DEAD-CODE service-role client removed** (`check:no-supabase` 113 → 112, −1). Fourth grind item; a near-zero-risk cleanup. The route's entire data layer (`loadOwnedFeedback`/`loadAgentConfig`/`loadAssistantMessageCreatedAt`/`loadPriorUserMessage`/`promoteChatMessageFeedback`/`setChatFeedbackPromotionStatus`/`removeChatFeedbackPromotion`) was ALREADY migrated to a co-located `promote.drizzle.ts` twin in a prior sweep, and the shared kb ingest lib it hands off to (`ingestKnowledgeSource`) is ALSO already on Drizzle — its signature is `{ orgId, sourceId, fetchImpl?, exec? }`, taking a `DbExecutor`, NOT a `SupabaseClient`. The route's module-level `createClient(SERVICE_ROLE_KEY)` `admin` client was therefore constructed but NEVER referenced in any handler (the `ingestKnowledgeSource` call passes only `{ orgId, sourceId }`) — identical to the earlier `kb/ingest` dead-client finding. Deleted the `admin` const + the `@supabase/supabase-js` import + the now-stale comment (replaced with an accurate one), zero behaviour change. Cleaned the sibling `route.test.ts` too: dropped the obsolete `jest.mock('@supabase/supabase-js', …)` import-stub and the stray `supabase: {}` on the `requireAuth` mock (the route never reads it). **Gate:** `rm tsconfig.tsbuildinfo` → whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 112 (route gone from the gate); prettier clean; jest 4/4; the pre-existing substrate harness `chat-feedback-promote-check.ts` re-run **PASS 0 defects** (confirms the already-migrated twin + kb ingest path are healthy end-to-end). **T011 remains open** — 112 gate refs; genuine remaining data-plane roots ≈20 (event/[id]/checkin, the hot email trio, team/send-invite, sms/{send,broadcast,usage}, track/{conversion,identify,website}, webhooks/{mailgun,gotowebinar}, preferences, forms-v2/{ingest,webhooks/typeform}, chat/[orgId], onboarding/provision, crm salesforce/field-mapping/run-mapping, ai/client) + the T015/T021/T023-gated shims/comments.
  - **team/send-invite — the owner-gated invitation root, migrated INLINE (no fan-out)** (`check:no-supabase` 112 → 111, −1). Fifth grind item. The route's email helper (`sendManagedSystemEmail`) already had its Drizzle twin (`sendManagedSystemEmailExec` in `email/system-notify.drizzle.ts`, `exec` seam, `ManagedSendParamsExec = Omit<…, 'supabase'>`), so this migrated the route's OWN surface. **File:** new co-located `send-invite.drizzle.ts` twin (admin `db` + `exec: DbExecutor = db` seam): `fetchActorRole` (the owner-gate profile read — COLLAPSES no-row/DB-error → null so the route's `role !== 'admin'` still 403s, exactly as the old `.single()` swallowed the error into an undefined actor), `upsertOrgInvitation` (`insert … on conflict (org_id,email) do update set role/token/expires_at/invited_by = excluded.*` — the org-scoped uniqueness upsert; THROWS on error), and `fetchInviteEmailContext` (org name+region and inviter full_name+email read INDEPENDENTLY, each swallowing a per-read error to null to match the two originals' `.maybeSingle()` error-swallow — a display-lookup blip must not lose a saved invite). Route rewired: dropped the module-level service-role `createClient`/`service`, dropped `supabase` from the `requireAuth` destructure, moved the actor-role read off the session client onto `fetchActorRole`, wrapped `upsertOrgInvitation` in a try/catch that returns the existing 500 with a CONCISE pg message (`(e.cause ?? e).message`, never the DrizzleQueryError SQL+params dump), and swapped `sendManagedSystemEmail({ supabase: service, … })` → `sendManagedSystemEmailExec({ key, to, vars, target, dedupeKey, orgId, tag })` (no client). **Tests:** NEW `route.test.ts` (none existed) — 9 cases on twin + email-exec mocks (auth passthrough, owner-gate 403 for member + null-role, invalid-JSON 400, invalid-role 400, upsert-throw → 500 with the concise cause message, happy send lowercases the invitee email + passes no `supabase` in the email params, permanent-fail `emailSent:false`, send-throw `emailSent:false` invite-still-saved). **Gate:** `rm tsconfig.tsbuildinfo` → whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 111 (route gone from the gate); prettier clean; jest 9/9; new substrate harness `team-send-invite-check.ts` (12 checks over two orgs — owner/member/unknown role reads, invite write + persisted role/token + invited_by FK, on-conflict UPDATE-in-place-not-duplicate, the FOREIGN-org same-email DISTINCT-row tripwire, org+inviter email-context load + unknown → {null,null}; profiles seeded via plain INSERT to dodge the current_user guard-trigger drift) **PASS 0 defects**. **T011 remains open** — 111 gate refs; genuine remaining data-plane roots ≈19 (event/[id]/checkin, the hot email trio, sms/{send,broadcast,usage}, track/{conversion,identify,website}, webhooks/{mailgun,gotowebinar}, preferences, forms-v2/{ingest,webhooks/typeform}, chat/[orgId], onboarding/provision, crm salesforce/field-mapping/run-mapping, ai/client) + the T015/T021/T023-gated shims/comments.
  - **preferences (preference-centre GET+POST) — the token-authed compliance root, migrated with fan-in twin** (`check:no-supabase` 111 → 110, −1). Sixth grind item. The route is authenticated ONLY by the 24-byte unsubscribe token (a contact clicking an email link is not an authenticated org member), so its data layer stays on admin `db` with EXPLICIT id/org_id predicates — the exact rows the token record authorises. **File:** new co-located `preferences.drizzle.ts` twin (admin `db` + `exec: DbExecutor = db` seam) carrying all ~19 supabase touches: `fetchTokenRecord` (token → {contact_id,org_id,expires_at}; no-row/error → null so the route 404s), `fetchPreferenceCentreData` (the 5 GET reads — contact+org+preferences+ordered comm_types+brand, each best-effort → null/[] so a null contact/org still surfaces in the JSON body), `fetchContactSuppression` (hard-bounce/spam gate), `updateContactGlobals` (DYNAMIC scalar UPDATE assembled from whichever of globalEmail/globalSms/frequency are present + the patchForOptIn/patchForOptOut sms consent stamps — all scalar columns bind direct; keeps the `where … and erased_at is null` guard; BEST-EFFORT like the original's unchecked `.update()`), `insertConsentRecord` (the TCPA/GDPR proof-of-consent write, reproduced byte-for-byte for BOTH email and sms #1142), `upsertContactPreference` (`on conflict (org_id,contact_id,channel,comm_type) do update` — the granular per-comm-type save), the P7 auto-flip trio `fetchNonLockedEmailCommTypeKeys`/`fetchEmailPreferenceStates`/`flipEmailUnsubscribed` (one-direction email_subscribed→false, honours the erased_at guard), and the confirmation reads `fetchContactForConfirmation`/`fetchOrgSendConfig`/`fetchBrandSettings`. All WRITES best-effort (catch+log, never throw) to preserve the supabase originals' silent-continue-on-write-error control flow (a throw would have escaped into the outer try/catch and turned a swallowed write into a 500). Route rewired: dropped the module-level service-role `createClient`, kept `unsubscribeAllForContact(db, …)` (already on Drizzle), swapped `isContactErased(supabase, …)` → `isContactErasedExec(contact_id)`; all branching/comments (#1141/#1142/#1147/#1456/#1436/P7) preserved verbatim. **Tests:** `route.test.ts` fully re-pointed off the supabase-mock onto the twin boundary (`jest.mock('./preferences.drizzle')` + mocked `isContactErasedExec`) — 25 cases preserving every regression (sms opt-in/out consent stamps + records, unsubscribe-all includeSms + confirmation fall-through + same-instant-contradiction guard, #1456 subaccount/unverified/best-effort-fail confirmation guards + erased-never-send, GET/POST token 400/404/expiry, hard-bounce/spam resubscribe refusal, granular upsert+consent, P7 flip/no-flip). **Gate:** `rm tsconfig.tsbuildinfo` → whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 110 (route gone from the gate); prettier clean; jest 25/25; new substrate harness `preferences-check.ts` (23 checks over two orgs — token resolve/unknown, suppression read, dynamic email+sms scalar UPDATE + the erased-contact-not-mutated guard, email+sms consent rows both written #1142, upsert idempotency + the FOREIGN-org DISTINCT-row tripwire, P7 non-locked-email-keys filter + email-state read + one-direction flip + erased-not-flipped guard, confirmation contact/org/brand reads, GET aggregate) **PASS 0 defects**. Substrate lessons: drizzle expands a JS array into a `($n,$m)` param tuple not an array literal — the `comm_type in (…)` list must be built with `sql.join`, not `= any(${keys}::text[])`; and `enforce_org_region_immutable` pins `mailgun_region := region` on every org UPDATE, so the harness asserts the pinned 'eu' rather than trying to set 'us'. **T011 remains open** — 110 gate refs. Genuine remaining data-plane API-route roots ≈21: `admin/{ai-usage,credit-ledger/overview,home}`, `chat/[orgId]`, `email/{generate,send-campaign,sync-suppression}`, `event/[id]/checkin`, `forms-v2/{ingest,webhooks/typeform/[endpointId]}`, `insights/deliverability`, `integrations/canva/import`, `onboarding/provision`, `sms/{broadcast,send}`, `track/{conversion,identify,website}`, `webhooks/{gotowebinar,mailgun,stripe}`, `dashboard/events/[id]/checkin-poster` (page+data) — plus `lib/ai/client`; the rest of the 110 are `.drizzle` twins holding a T023-gated supabase shim, the lib originals still consumed by `inngest/**` (T023), and the T015 auth (`auth/callback`, `supabase-server`, `supabase`) / T021 storage shims.
  - **track/identify — the anonymous visitor→contact link + activity backfill, migrated + a latent silent-write bug fixed** (`check:no-supabase` 110 → 109, −1). Seventh grind item. identify() is called from an anonymous browser (no session), so the twin uses admin `db` with EXPLICIT org_id predicates. **File:** new co-located `identify.drizzle.ts` (admin `db` + `exec: DbExecutor = db` seam): `fetchIdentifyContact` (id+deleted_at read scoped to id **and org_id** — the cross-tenant guard; COLLAPSES no-row/DB-error → null so the route falls to its benign no-op, no valid-token/contact oracle), `upsertVisitorIdentity` (BEST-EFFORT — the original awaited the upsert without checking `error`, so a hiccup silently continued to the backfill; `on conflict (org_id,visitor_token) do update`), and `backfillWebsiteEvents`/`backfillWebsiteSessions` (each `update … where org_id=… and visitor_token=… and contact_id is null returning id`, RETURNING the claimed-row count; scoped so a row already owned by ANOTHER contact — shared-device — a different visitor_token, or a foreign org are all left untouched; return 0 on error, matching the original `.select('id').data?.length ?? 0`). **Bug fixed (tightly coupled to the migrated write):** the original wrote `identified_via: 'orbit_identify'`, but `form_visitor_identities`'s `identified_via` CHECK only permits `'cookie' | 'email_link'` — so the upsert **always violated the constraint and, because the error was swallowed, the identity link was silently never written**. The twin writes the constraint-valid, semantically-correct `'email_link'` (the contact is resolved from an HMAC-signed identity token minted into an email `orbit_ct` link), so the write now actually lands; readers (`track/website`, `forms-v2/prefill`) only read `contact_id`, unaffected. Route rewired: dropped the module-level service-role `createServiceClient`/`service()`, kept the GPC no-op + `verifyIdentityToken` + `isArchivedMatch` (pure helpers) untouched, moved all 4 DB ops onto the twin. **Tests:** `route.test.ts` re-pointed off the supabase-mock onto the twin (`jest.mock('./identify.drizzle')`) — 6 cases (GPC:1 writes nothing/benign no-op, happy identify+backfill with counts flowing through, GPC:0 proceeds, archived → no-op #645, contact-not-in-org → null → no-op, missing-fields 400). **Gate:** `rm tsconfig.tsbuildinfo` → whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 109; prettier clean; jest 6/6; new substrate harness `track-identify-check.ts` (11 checks over two orgs — in-org vs foreign-org contact read, archived deleted_at, upsert update-in-place, backfill claims exactly the NULL org+visitor rows + leaves shared-device/other-visitor/foreign-org rows + idempotent second run + session backfill) **PASS 0 defects**. **T011 remains open** — 109 gate refs; remaining route roots ≈20 (the track/{website,conversion}, event/[id]/checkin, email/{generate,send-campaign,sync-suppression}, sms/{send,broadcast}, webhooks/{mailgun,gotowebinar,stripe}, forms-v2/{ingest,typeform}, insights/deliverability, canva/import, admin/{ai-usage,credit-ledger,home}, chat/[orgId], onboarding/provision, checkin-poster) + lib/ai/client + the T015/T021/T023-gated shims.
  - **track/conversion — the dual-mode (public pixel/POST + authenticated server-key) conversion-attribution endpoint + its two coupled tracking-lib helper clusters, migrated** (`check:no-supabase` 109 → 108, −1). Eighth grind item. The route threaded a module-level service-role `createClient` into BOTH its own data ops AND five lib helpers, so this migrated three files' worth of DB seams behind one root. **Files:** (1) new co-located `conversion.drizzle.ts` (admin `db` + `exec: DbExecutor = db` seam): `fetchCampaignByOrgAndUtm` (id+org_id by `org_id`+`utm_campaign`, COLLAPSES no-row/DB-error → null — the original path-1 `.maybeSingle()` returned `data||null`, ignoring the error), `fetchContactOrgsBySelector` (bounded, `order by created_at asc limit 5` list — email is unique only per-org (`contacts_org_id_email_key`), so one selector can match >1 org; the route picks the earliest deterministically + warns on ambiguity; logs `contact lookup error` + returns `[]` on error, which the route treats as unresolved), `fetchContactForAttribution` (id+deleted_at by column+`org_id`, → null; route then applies `isArchivedMatch`), and `insertConversion` (preserves the #1272 idempotency contract EXACTLY — a `23505` on `uq_campaign_conversions_org_idempotency` WITH an idempotency key present → `{ok:true,deduped:true}`; any other failure surfaces the concise pg message → 500, fixing the #1113 silent-swallow. **`column` (`'email'`/`'email_sha256'`) is interpolated via `sql.identifier` (fixed code value, never user input), `metadata` written `${JSON.stringify(v)}::jsonb`, and — because drizzle THROWS on a constraint violation (unlike supabase-js) — the 23505 vs other-error decision reads the pg code off the thrown error's `.cause` (newer drizzle wraps in `DrizzleQueryError`) via a `pgError()` helper**). (2) new `src/lib/tracking/conversion-auth.drizzle.ts` — `authenticateConversionApiKeyExec(headers)` (short-circuits `'none'` with NO DB call when no orbit-shaped key; org_api_keys lookup → `'invalid'`/`'ok'`; a swallowed DB error collapses to null → `'invalid'`, matching the original's ignored `.maybeSingle()` error) + `touchApiKeyExec(keyId)` (BEST-EFFORT last_used_at stamp — catch+log, never throw). (3) new `src/lib/tracking/conversion-rate-limit.drizzle.ts` — `checkConversion{Ip,Org,Key}LimitExec` invoking the same `increment_chat_rate(text,int)` fixed-window RPC via `db.execute`, each FAILING OPEN on any counter error; the pure `clientIpFromHeaders` + all tuning constants stay in the original module and are imported unchanged. Route rewired: dropped the module-level `createClient`; `resolveCampaign`/`resolveContactId`/`recordConversion` now call the twins (keeping the deterministic org-pick warn + `isArchivedMatch` + `resolveConversionInitiativeId` — already Drizzle — in the route). The original supabase-shaped helper modules (`conversion-auth.ts`/`conversion-rate-limit.ts`) are left in place (structural-typed, they never imported `@supabase/supabase-js`, so they don't count against the gate) alongside their own passing tests. **Tests:** `route.test.ts` re-pointed off the deep supabase-mock onto the three twin boundaries (`jest.mock('./conversion.drizzle')` + the two lib twins), driven by the SAME state vars so all **38** behavioural cases (conversion_type default/allowed/invalid→other, insert-fail 500 vs dedup-success, value validation/overflow, email_sha256 attribution, metadata size/type 400, GET pixel always-200-GIF + forwards fields + never-metadata + PII-in-URL ignored, email-only multi-org deterministic resolve, bot filtering, IP/org/key rate limits + 429s, Cubot-not-a-bot, idempotency dedup + no-key-still-500, B2 trusted-key bypass + key-org-wins-over-hostile-body + per-key cap + 401 unknown/revoked + non-orbit-header falls-through) stay green. **Gate:** `rm tsconfig.tsbuildinfo` → whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 108; prettier clean; jest 38/38; new substrate harness `track-conversion-check.ts` (two orgs sharing a `utm_campaign` + an email → campaign cross-tenant scoping, per-org contact-list ordering by email and generated `email_sha256`, archived-contact deleted_at, insert lands with exact numeric/jsonb/contact columns, idempotency dedup = no second row, same key in a DIFFERENT org still inserts (per-org dedup), out-of-set conversion_type CHECK → `{ok:false}` surfaced) **PASS 0 defects**. **T011 remains open** — 108 gate refs; remaining route roots ≈19 (track/website, event/[id]/checkin, email/{generate,send-campaign,sync-suppression}, sms/{send,broadcast}, webhooks/{mailgun,gotowebinar,stripe}, forms-v2/{ingest,typeform}, insights/deliverability, canva/import, admin/{ai-usage,credit-ledger,home}, chat/[orgId], onboarding/provision, checkin-poster) + lib/ai/client + the T015/T021/T023-gated shims.
  - **track/website — the largest ingest root (~869 lines, ~42 supabase touches: anonymous website-event/session ingest + visitor→contact linkage + activity rollups + tracking-rule fires), migrated** (`check:no-supabase` 108 → 107, −1). Ninth grind item. The route is called from an anonymous browser (no session), so the twin uses admin `db` with EXPLICIT `org_id` predicates on every read/write — the org_id in the (Zod-validated) payload is the tenant scope. **File:** new co-located `website.drizzle.ts` (admin `db` + `exec: DbExecutor = db` seam, `DbExecutor` NOT exported) carrying all 19 data ops across THREE distinct error regimes, chosen per-op to reproduce the supabase original byte-for-byte: (1) **COLLAPSE no-row/DB-error → null|[]|0** for the reads the route no-ops on — `fetchIngestOrg` (→404 on null), `fetchTrackingSettings` (null → the `?? []`/`?? {}` defaults → the #1366 fail-closed origin gate), `countIngestRateLimits` (each of the 3 sliding-window counts degrades OPEN to 0 so a limiter outage can't 500 real ingest), `fetchVisitorContactId`, `fetchContactEmail`, `fetchActiveTrackingRules`, `fetchContactEngagementScore`, `fetchFiredRuleIds`, `countContactPageViews`, `fetchContactTags`; the #1109 session ops `updateSession` (returns claimed-ids, `[]` on miss/error → fall back to insert), `insertSession` (null on error → the untrusted cross-org id is nulled out, never left in place), `checkSessionOwnership` (false on miss/error). (2) **BEST-EFFORT (catch + log, never throw)** for the post-write ops the original awaited without checking `error` — `insertIngestRateLimits` (returns `{error}` so the route keeps its exact "limits not enforced" log + degrade-open), `insertWebsiteEvents` (a CHECK-invalid batch silently lands NOTHING, matching the unchecked insert), `upsertEngagementScore` (the `upsert_engagement_score` SECURITY-DEFINER RPC via named args + `::int` casts + `${JSON.stringify(breakdown)}::jsonb`), and the four per-rule action writes `insertJourneyEnrollment`/`fetchContactTags`/`updateContactTags`/`insertRuleFire` — **critically, all four catch internally and NEVER rethrow**, because they sit inside the route's per-rule `try/catch` and the original (a non-throwing supabase insert) always reached `firesThisBatch++` + the rule-fire record; a drizzle throw would skip them, so the silent-continue is reproduced explicitly. **Drizzle gotchas handled:** `fetchFiredRuleIds` expands its JS id list with `sql.join(ids.map(id => sql\`${id}\`), sql\`, \`)` (NOT `${arr}` which emits a param tuple) + guards the empty list to skip the query; `updateContactTags` writes the `text[]` via `array(select jsonb_array_elements_text(${JSON.stringify(tags)}::jsonb))` (a bare `${arr}::text[]` fails — a text param can't cast to text[]) and keeps the `and erased_at is null` GDPR-erased skip; `countContactPageViews` uses `url ilike ${pattern}` (route derives the pattern via `trigger_url.replace('*','%')`). **Coupled helper reuse:** the notify_team rule path's `systemSendTargetForOrg(supabase, org_id)` was repointed to the already-existing `systemSendTargetForOrgExec(org_id)` twin (`email/system-notify.drizzle.ts`); `sendSystemEmail` is pure (mailgun only) and kept as-is. Route rewired: dropped the module-level service-role `@supabase/supabase-js` `createClient`/`service()`, moved all 42 touches onto the twin, cast the `unknown`-typed `session` reads through an `any` alias into the typed `SessionInsert` payload. **Tests:** `route.test.ts` re-pointed off the deep supabase-mock onto the twin boundary (`jest.mock('./website.drizzle')` + the two email helpers) driven by the SAME state vars — the #1109 org-scoping `eqCalls` the tests assert are now synthesised from the `(sessionId, orgId)` args the route passes explicitly — so all **20** behavioural cases (session update org-scoping + cross-org fallback-insert + insert-fail nulls-the-id + events-only-flush ownership check, GPC unconditional floor #1156, no self-hosted CORS bypass #1112, fail-closed empty allowed_domains #1366, body-size 413, per-IP/per-org/daily rate + quota skips #1366) stay green. **Gate:** `rm tsconfig.tsbuildinfo` → whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 107 (route gone from the gate); prettier clean; jest 20/20; new substrate harness `track-website-check.ts` (two orgs, 30 checks — cross-tenant guards on every read [org/settings/visitor-contact/contact-email/engagement-score/rules/page-views all null/[]/0 under a foreign org], the #1109 session insert/update/ownership org-scoping incl. the cross-org update-0-rows + FK-violation-nulls-the-id, event batch lands + a CHECK-invalid batch swallowed, sliding-window counts org-scoped, `upsert_engagement_score` rolls + increments in place, `fetchFiredRuleIds` IN-list + empty-guard, journey-enroll + rule-fire land, `text[]` tag write + the FOREIGN-org write TRIPWIRE + the GDPR-erased skip) **PASS 0 defects**. **T011 remains open** — 107 gate refs; remaining route roots ≈18 (event/[id]/checkin, email/{generate,send-campaign,sync-suppression}, sms/{send,broadcast}, webhooks/{mailgun,gotowebinar,stripe}, forms-v2/{ingest,typeform}, insights/deliverability, canva/import, admin/{ai-usage,credit-ledger,home}, chat/[orgId], onboarding/provision, checkin-poster) + lib/ai/client + the T015/T021/T023-gated shims.
  - **sms/{send,broadcast} — the two manual-SMS route roots, migrated together** (`check:no-supabase` 107 → 105, −2). Tenth grind item; a self-contained leaf whose lib DB helpers ALL already had Wave-A twins (`loadAndGateSendExec`, `recordSmsSendForUsageExec`, `resolveSmsAudienceExec`, `validateMmsMediaUrlsExec`, `isContactErasedExec`), so this migrated only the two routes' OWN cross-table surface + rewired them onto the twins. **Classification: admin `db`** (both routes were service-role send paths; the settled sms-subsystem call per `resolve-audience.drizzle`'s header — explicit `org_id` predicate is the tenant guard, T018 flips to `withUserId` later). `send/route.ts`: dropped the module-level service-role `createClient`; contact re-fetch → Drizzle `select(contacts) where id+org_id`; erased gate → `isContactErasedExec`; `sms_messages` insert (`.returning({id})`) + the two provider-response updates → Drizzle; gate + usage → the `*Exec` twins. `broadcast/route.ts` (POST send/draft + GET + DELETE): dropped the service-role `createClient`; ALL `sms_broadcasts` reads/writes → Drizzle (draft upsert, finalise draft→sending single-fire, dedup select aliased back to `{body,media_urls,created_at}` for `findDuplicateBroadcast`, delete org-scoped); the GET `audiences(name)` PostgREST embed → a `leftJoin(audiences)` with output keys aliased to the exact snake_case shape the UI consumes + `audience_name` flattened in directly; **`est_cost_usd` numeric hazard handled** — written as a fixed-precision string (`.toFixed(4)`) and cast back to a JSON number on read so the API response type never drifts. `supabase.auth.getUser()` stays (auth plane, T015 — pulls in no forbidden import). **Gate:** whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 105 (both routes gone from the gate); prettier clean; new substrate harness `sms-routes-check.ts` (24 checks over two orgs — org-scoped contact re-fetch + cross-tenant 0-rows, gate pass/fail incl. foreign-org block, erased fail-closed, sms_messages insert+update round-trip, audience resolve = 1, draft insert + GET leftJoin `audience_name` + null-numeric stays null, orgB GET/update/delete of orgA rows all 0-row tripwires, finalise draft→sending single-fire + est_cost_usd round-trips as `0.0079` number, dedup guard match) **PASS 0 defects**. **Deferred (correctly, not a miss):** the lib/sms ORIGINALS (`gate.ts`/`usage.ts`/`resolve-audience.ts`/`validate-media-urls.ts`) stay — `src/inngest/sms-broadcast-functions.ts` + `sms-webhook-functions.ts` still call them on the supabase path (T023); they shed supabase only when inngest migrates. The two route `route.test.ts` jest suites still assert the retired supabase path (import-red under the `@/db/client` guard) → the consolidated jest-rewrite task, harness is the interim verification. **T011 remains open** — 105 gate refs; remaining data-plane route roots ≈16 (event/[id]/checkin, email/{generate,send-campaign,sync-suppression}, webhooks/{mailgun,gotowebinar,stripe}, forms-v2/{ingest,typeform}, insights/deliverability, canva/import, admin/{ai-usage,credit-ledger,home}, chat/[orgId], onboarding/provision, checkin-poster) + lib/ai/client + the T015/T021/T023-gated shims/comments + the 29 `inngest/**` roots.
  - **admin/{ai-usage,home,credit-ledger/overview} — the portfolio-wide admin trio, migrated together** (`check:no-supabase` 105 → 102, −3). Eleventh grind item; three file-disjoint super-admin/finance surfaces sharing ONE classification: **admin `db`, deliberately CROSS-org** (no tenant predicate — these are portfolio views; the old service-role `adminClient()` factory is dropped from each). Query-translation hazards (not isolation) were the work: (1) the PostgREST `org_plans → plans(name, display_name)` embed → a `leftJoin(plans, eq(plans.id, orgPlans.planId))` in all three; (2) `admin/home` fetchDirectory — the `admin_org_directory` VIEW select with PostgREST `count:'exact'` (→ a second `count()` query with the same `and(ilike(name), eq(region))` filters), `.range()` → `.limit/.offset`, and the B5 billing-column gate reworked from column-omission to an OUTPUT-omission map (the view's LATERAL `compute_org_balances` runs inside the view regardless, so it's an output-contract guard, not a compute one — noted in-code); (3) `admin/home` fetchPortfolio dynamic org-column select → always-select + conditional `isNull(deletedAt)` when `!canSeeDeleted`; (4) `admin/ai-usage` — the Supabase 1000-row-paged `fetchAll` helper DELETED (Drizzle/pg returns every row, so the cost aggregates can no longer be silently truncated), plus `ai_usage` gte(since) + `credit_ledger` `direction='debit' AND forfeits_source IS NULL` + `credit_packs` cheapest-eligible-pack all as direct Drizzle. **Latent bug fixed (coupled to the migrated read):** `credit-ledger/overview` allOrgs counted contacts via `admin.from('contacts').select('org_id')`, which Supabase silently caps at 1000 rows → the portfolio contact_count under-counted once total contacts >1000; replaced with a proper `count()` GROUP BY orgId (exact). `perOrg`/`allOrgs`/fetch* signatures dropped their now-unused `admin: SupabaseClient` params; the stale "adminClient() constructed after the gate" comment removed. **Gate:** whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 102 (all three gone); prettier clean; new substrate harness `admin-routes-check.ts` (15 checks over two seeded orgs — org_plans→plans leftJoin resolves/omits correctly, contacts GROUP BY count = exact 3/2, the view's plan_name+contact_count+region-filter+ilike-search+count:exact+limit/offset pagination, ai_usage 30d `since` window excludes the 40-day-old row, credit_ledger debit-only/forfeit-excluded filter, credit_packs cheapest-eligible selection) **PASS 0 defects**. Harness notes: credit_ledger is append-only (immutable-ledger guard trigger) so fixture teardown drops user triggers; `entitlement_epoch` is a uuid FK (seed a `credit_entitlement_epochs` row), `forfeits_source ∈ {allowance,prepaid,adjustment}`, `plans.name ∈ {free,basic,pro,premium}`. **Note:** these routes have no `route.test.ts` (unit suites), so no test-rewrite debt; the harness is the verification. **T011 remains open** — 102 gate refs; remaining data-plane route roots ≈13 (event/[id]/checkin, email/{generate,send-campaign,sync-suppression}, webhooks/{mailgun,gotowebinar,stripe}, forms-v2/{ingest,typeform}, insights/deliverability, canva/import, chat/[orgId], onboarding/provision, checkin-poster) + lib/ai/client + the T015/T021/T023-gated shims/comments + the 29 `inngest/**` roots.
  - **email/sync-suppression + event/[id]/checkin — a trivial twin-swap + a substantial public root, migrated together** (`check:no-supabase` 102 → 100, −2). Twelfth grind item. **(a) email/sync-suppression** (43 lines): the route held only a module-level service-role `createClient` that it threaded into `syncSuppressionForOrg({ orgId, supabase })`; that lib already has a Wave-A twin (`syncSuppressionForOrgExec`, `exec` seam, own `.drizzle.test.ts`), so the migration was a one-line swap to `syncSuppressionForOrgExec({ orgId: sessionOrgId })` (admin `db`) + drop the client. The IDOR org-scope guard (session orgId vs body orgId) is unchanged; the original `syncSuppressionForOrg` stays for `src/inngest/suppression-sync.ts` (T023). **(b) event/[id]/checkin** (250 lines): the #936 PUBLIC no-auth door-check-in root — service-role scoped by the unguessable event UUID. **Classified admin `db`** (no session; explicit `org_id` predicate on every write is the tenant guard, T018 has no session to flip). All 8 ops → Drizzle: the `webinars` lookup (wrapped try/catch → null so a malformed event UUID keeps the no-oracle 404, replacing `.maybeSingle()`'s error-swallow); `assertOrgActiveForCapture(supabase,…)` → the existing `assertOrgActiveForCaptureExec(orgId)` twin; the two `ingest_rate_limits` `count:'exact',head:true` reads → `count()` queries; the attempt-insert → best-effort try/catch; `webinar_registrants` update-first (`.returning({id})`), the walk-in insert with the **23505 concurrent-race fallback** (pg code read off `(e.cause ?? e).code` via a `pgCode()` helper since Drizzle throws where supabase-js returned `{error}`), and the absolute-COUNT `attendee_count` recompute + `webinars` update. `upsertContactWithDefaults(db,…)` was already Drizzle (untouched). Stale "uses the service-role client" header comment corrected. **Gate:** whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 100 (both gone); prettier clean; new substrate harness `event-checkin-check.ts` (14 checks over two orgs — webinar id-scope load + malformed-uuid-throws→404, capture guard active-pass/soft-deleted-CaptureBlockedError, update-first keeps the registered name + marks attended, the **orgB-can't-check-in-orgA cross-tenant tripwire**, walk-in insert + the 23505 unique-race, attendee_count = absolute COUNT + orgB webinar untouched) **PASS 0 defects**. **Substrate finding (flagged):** the `ingest_rate_limits_key_type_check` on the substrate predates the `event_checkin_ip`/`event_checkin_event` key_types (allowed set is `ip,org_form_sig,prefill_ip,prefill_token,api_key,webhook_endpoint,track_ip,track_org`), so the attempt insert hits a 23514 CHECK and the route's documented best-effort branch swallows it → event check-in rate-limiting is a silent no-op locally (harmless; check-in proceeds unthrottled). A migration adding those two key_types is missing from the replay — same class as the other 002 drift items, worth a fix pass. **Note:** event/checkin has no `route.test.ts`; sync-suppression's suite mocks the (unchanged) lib boundary — no new test debt. **T011 remains open** — 100 gate refs; remaining data-plane route roots ≈11 (email/{generate,send-campaign}, webhooks/{mailgun,gotowebinar,stripe}, forms-v2/{ingest,typeform}, insights/deliverability, canva/import, chat/[orgId], onboarding/provision, checkin-poster) + lib/ai/client + the T015/T021/T023-gated shims + the 29 `inngest/**` roots.
  - **webhooks/stripe + insights/deliverability — two small admin-`db` reads, migrated together** (`check:no-supabase` 100 → 98, −2). Thirteenth grind item. **(a) webhooks/stripe** (95 lines): the signed Stripe webhook's `stripe_events` log moved off the service-role `adminClient()` onto pooled `db` (no session/tenant — the event id is the key; `handleStripeEvent(db, …)` was already Drizzle). The `.upsert({…}, {onConflict:'id'})` → `db.insert(stripeEvents).values(row).onConflictDoUpdate({ target: stripeEvents.id, set: {…} })` (best-effort try/catch, preserving the "logged, never throws — ledger idempotency is the real guard" contract); the fast-path dedupe `.maybeSingle()` → `.limit(1)` wrapped so a read error is swallowed and processing proceeds (matching the original's ignored `{error}`). **(b) insights/deliverability** (133 lines): the ONLY supabase data touch was `loadBlocklist`'s `organizations.select('mailgun_domain, mailgun_region').eq('id', orgId).single()` (the placement read `loadLatestDeliverabilityPlacement(db,…)` was already Drizzle) → moved onto `db` scoped by the explicit `id = orgId` (session's own org); dropped the `supabase` param + the `SupabaseClient` type import; stale "via the user-scoped client" header comment corrected. **Gate:** whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 98 (both gone); prettier clean; new substrate harness `stripe-deliverability-check.ts` (7 checks — first upsert inserts, **onConflict updates IN PLACE (exactly 1 row, status error→processed) proving retry-idempotency**, dedupe read sees processed, org sending-domain read returns the right domain/region per id + unknown-id → null → TRIAL_MARKETING_DOMAIN fallback) **PASS 0 defects**. (Substrate note: Docker Desktop had quit mid-session; relaunched + `local:up`, volume persisted so no re-replay needed.) **Note:** neither route has a `route.test.ts` beyond the unchanged lib boundaries — no new test debt. **T011 remains open** — 98 gate refs; remaining data-plane route roots ≈9 (email/{generate,send-campaign}, webhooks/{mailgun,gotowebinar}, forms-v2/{ingest,typeform}, canva/import, chat/[orgId], onboarding/provision, checkin-poster) + lib/ai/client + the T015/T021/T023-gated shims + the 29 `inngest/**` roots.
  - **forms-v2/ingest — the public track.js ingestion root (406 lines, 10 DB ops), migrated** (`check:no-supabase` 98 → 97, −1). Fourteenth grind item. Public + unauthenticated (called from customer-site `track.js`); **classified admin `db`** — the Zod-validated payload `org_id` is the trust boundary + explicit predicate on every op, exactly as the service-role original. **All FOUR lib delegations already had Wave-A twins**, so this rewired to them + migrated the route's OWN cross-table surface: `assertOrgActiveForCapture`→`assertOrgActiveForCaptureExec`, `ensureCapturedForm`→`ensureCapturedFormExec`, `buildPipelineContext`→`buildPipelineContextExec`, `runSubmissionPipeline({supabase,…})`→`runSubmissionPipelineExec({…})` (drop the `supabase` field). Direct ops → Drizzle: the `organizations` existence check (try/catch→null so a malformed org_id keeps the 404, replacing `.maybeSingle()`'s error-swallow); the two `ingest_rate_limits` `count:'exact',head:true` reads → `count()`; the `tracking_settings` read + the authorised-domain auto-add `.upsert(…,{onConflict:'org_id'})` → `onConflictDoUpdate`; and the **four `form_submissions` inserts** (quarantine full-payload, turnstile-failed, consent-required, durable-first with `.select('id').single()`→`.returning({id})`) — all snake→camel field-mapped (jsonb `rawPayload`/`data`/`mappingErrors`, the utm_* set). **Critical error-semantics preservation:** the route has **NO outer try/catch** and supabase-js RESOLVED with `{error}` (never threw), so each op reproduces its exact swallow-vs-surface behaviour now that Drizzle throws — the ops that IGNORED `{error}` (rate-limit insert, tracking read+upsert, turnstile/consent inserts) are wrapped in swallowing try/catch; the ones that CHECKED it (quarantine, durable-first) keep their log-and-continue. Without this a single transient DB hiccup would have turned a silent best-effort write into an unhandled 500 on a public endpoint. **Gate:** whole-project `tsc --noEmit` 0 errors (first pass); `check:no-supabase` 97; prettier clean; new substrate harness `forms-v2-ingest-check.ts` (14 checks over two orgs — org existence + malformed-uuid-throws, rate-limit count+insert, tracking_settings onConflict updates-in-place + authorised_domains jsonb round-trips both domains + no orgB leak, durable-first `.returning(id)` + raw_payload jsonb round-trip + utm camel→snake mapping, quarantine row persists + no orgB submission leak) **PASS 0 defects**. **Deferred (correctly):** the sibling `forms-v2/webhooks/typeform/[endpointId]` root is still on supabase — next grind item. **T011 remains open** — 97 gate refs; remaining data-plane route roots ≈8 (email/{generate,send-campaign}, webhooks/{mailgun,gotowebinar}, forms-v2/webhooks/typeform, canva/import, chat/[orgId], onboarding/provision, checkin-poster) + lib/ai/client + the T015/T021/T023-gated shims + the 29 `inngest/**` roots.
  - **forms-v2/webhooks/typeform/[endpointId] — the signed Typeform webhook receiver, migrated** (`check:no-supabase` 97 → 96, −1). Fifteenth grind item; sibling of forms-v2/ingest. **Classified admin `db`** — the HMAC over the raw body is the trust boundary + the opaque endpoint id/org_id is the scope; the shared pipeline `runServerSubmission(db,…)` was already Drizzle. Direct ops → Drizzle: the `form_webhook_endpoints` lookup (try/catch→null so a malformed endpoint id keeps the 404); the `ingest_rate_limits` `count:'exact',head:true` + best-effort insert (key_type `webhook_endpoint`); the **#589 idempotency receipt** `.upsert({…},{onConflict:'endpoint_id,event_id',ignoreDuplicates:true}).select('id')` → `onConflictDoNothing({ target: [endpointId, eventId] }).returning({id})` (empty returning == a retry → ack duplicate 200; matches the unique index `form_webhook_events_endpoint_event_key`); the best-effort `last_event_at` touch; and the **receipt-release delete** in the catch (so a mid-processing throw releases the claim → the Typeform retry reprocesses instead of being a false duplicate → never a silently-lost submission). All error semantics preserved: the dedup `dedupErr`-checked branch → `dedupThrew` flag reproducing "throw → log + proceed (claim not owned)" vs "empty → duplicate 200" vs "owned → claimedEventId set". **Gate:** whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 96; prettier clean; new substrate harness `typeform-webhook-check.ts` (10 checks — endpoint resolve + malformed-id-throws, rate-limit count+insert, **dedup receipt: first-claim inserts / retry returns 0-rows-→-duplicate / same event_id under a DIFFERENT endpoint claims its own** / release-on-error re-claims, last_event_at stamp) **PASS 0 defects** (seed note: `form_webhook_endpoints_one_target` CHECK requires exactly one of external_form_id/target_form_id). **Both forms-v2 ingest roots now done**; their lib submission twins' originals (`run-pipeline.ts`, `ensure-captured-form.ts`, `capture-guard.ts`) stay for `inngest/**` (T023). **T011 remains open** — 96 gate refs; remaining data-plane route roots ≈7 (email/{generate,send-campaign}, webhooks/{mailgun,gotowebinar}, canva/import, chat/[orgId], onboarding/provision, checkin-poster) + lib/ai/client + the T015/T021/T023-gated shims + the 29 `inngest/**` roots.
  - **webhooks/mailgun — the signed open/click writeback receiver, migrated** (`check:no-supabase` 96 → 95, −1). Sixteenth grind item. **Classified admin `db`** — the HMAC over `timestamp+token` is the trust boundary; org_id comes from the event's user-variables (no session). The full HMAC/replay/timing-safe `verifyWebhook` gate is pure crypto (untouched). The shared processor `processRecordedEvent(supabase,row)`→`processRecordedEventExec(row)` twin (original stays for `inngest/mailgun-event-reconcile.ts`, T023). Direct ops → Drizzle: the contact resolve (`.eq('email').eq('org_id')` → `and(eq,eq)`); the **#1457 durable receipt** `.insert({…}).select(EVENT_ROW_COLUMNS).single()` → `.returning(EVENT_ROW_SELECT)` where the PostgREST column-string became a Drizzle column-map with **snake_case output keys** (`org_id`/`event_type`/`raw_payload`/`processing_attempts`) so the returned row IS the `EmailEventRow` contract the processor + reconciler both consume; and the **23505 idempotency path** (unique partial index `email_events_mailgun_event_token_uniq`) — Drizzle throws so the code is read off `(e.cause ?? e).code`, then the existing-receipt lookup (`EVENT_ROW_SELECT` + `processing_status`) drives the processed→dedupe / unfinished→re-drive branch. Error semantics preserved: a non-23505 insert failure → the retryable 503 `event_persist_failed` (#1219 "no side effect without a recorded receipt"); a missing-after-23505 → 503 `event_lookup_failed`. **Gate:** whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 95; prettier clean; new substrate harness `mailgun-webhook-check.ts` (8 checks — contact resolve org-scoped + orgB-no-match, receipt insert `.returning` shapes the snake_case EmailEventRow incl. raw_payload jsonb + processing_attempts number, **duplicate token → 23505**, existing-receipt lookup reads pending→re-drive then processed→dedupe) **PASS 0 defects**. **T011 remains open** — 95 gate refs; remaining data-plane route roots ≈6 (email/{generate,send-campaign}, webhooks/gotowebinar, canva/import, chat/[orgId], onboarding/provision, checkin-poster) + lib/ai/client + the T015/T021/T023-gated shims + the 29 `inngest/**` roots.
  - **chat/[orgId] — the public embed-chat root (462 lines, 8 tables, 2 exported helpers) + its 2 chat libs, migrated** (`check:no-supabase` 95 → 94, −1). Seventeenth grind item; the biggest remaining route. **Classified admin `db`** — CORS-open, orgId from the URL, scoped by the embed token; explicit `org_id` on every op. **Two shared chat libs migrated IN PLACE** (both had only a LOCAL `SupabaseLike` type, not `@supabase/*` imports, and each has exactly one runtime caller — this route): `chat/embed-auth.isValidEmbedToken(supabase,orgId,token)` → `(orgId, token, exec=db)` reading `organizations.chat_embed_token` over Drizzle (timing-safe compare untouched); `chat/rate-limit.checkChatRateLimit(supabase,orgId,ip)` → `(orgId, ip, exec=db)` running the `increment_chat_rate(text,int)` fixed-window fn via `exec.execute` (fail-open preserved). Both lib `.test.ts` rewritten to the new signature over a mock `exec` (+ `jest.mock('@/db/client',{db:{}})` to dodge the DATABASE_URL guard) — **16 tests green**. Route: dropped the module-level service-role `createClient`; `resolveConversation` (exported, used by /resolve) + `upsertContactFromChat` (exported) + POST + all 8 tables → Drizzle (`chat_messages` read `.neq('role','system')`→`ne` + insert×2, `chat_conversations` insert `.returning(id,created_at)`+update, `usage_summaries` read/update/insert, `agent_configs` read (camelCase `responseLength`/`autoCreateContact`), `contacts` read (aliased snake for the ArchivedMatch shape)+insert, `contact_activities` insert with jsonb metadata); twin rewires `assertOrgActiveForCaptureExec`/`resolveIntentfulMatchExec`/`isEmailErasedExec`; the already-Drizzle `loadOwnedChatConversation`/`loadRecentChatHistory`/`runChatTurn` unchanged (client-less). **TWO substrate-drift findings handled (behaviour preserved, drift surfaced):** (1) **`chat_journey_triggers`** — the current table is a per-conversation LOG `(id, conversation_id, contact_id, journey_id, trigger_event, created_at)`, NOT the org-keyword-config shape (`org_id`/`enabled`/`keywords`/`last_fired_at`) `detectAndFireTriggers` queried, so the supabase version ALREADY silently no-ops (PostgREST 400 → null → early return); ported as an explicit documented no-op (the columns don't exist → can't compile a faithful port). (2) **`usage_summaries.dimension` CHECK** has no `'chat_outcomes'` value (allowed: contacts/journeys/ai_tokens/emails/sms/api_calls), so `resolveConversation`'s rollup insert ALWAYS fails — the supabase version ignored the `{error}`; wrapped the rollup in a nested best-effort try/catch so the throw can't turn a resolved conversation into a null return. Both memoried. **Gate:** whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 94; prettier clean; new substrate harness `chat-route-check.ts` (18 checks over two orgs — embed-token valid/wrong/cross-org-scoped/unknown-org, rate-limit ok, agent_configs camelCase read, conversation insert `.returning`, newest-non-system message (system excluded), resolve flip, **chat_outcomes rejected → best-effort no-op (23514)** + valid-dimension upsert mechanics, contact + jsonb contact_activity + conv contact_id update, cross-org isolation on usage + contacts) **PASS 0 defects**. **T011 remains open** — 94 gate refs; remaining data-plane route roots ≈5 (email/{generate,send-campaign}, webhooks/gotowebinar, canva/import, onboarding/provision, checkin-poster) + lib/ai/client + the T015/T021/T023-gated shims + the 29 `inngest/**` roots.
  - **email/send-campaign — the campaign send-trigger root (468 lines, 4 tables, 5 guard libs), migrated** (`check:no-supabase` 94 → 93, −1). Eighteenth grind item. Session-authed + org-boundary-checked (`orgId === sessionOrgId`); **classified admin `db`** (service-role → `db`, explicit id/org_id predicates as the guard). **All 5 lib delegations had twins**: `assertOrgCanSend(supabase,orgId,org)`→`assertOrgCanSendExec({orgId,org})`, `assertDailySendCapNotExceeded(supabase,orgId,cap)`→`assertDailySendCapNotExceededExec({orgId,cap})`, `getEffectiveThroughput(supabase,orgId)`→`getEffectiveThroughputExec(orgId)`, `getCurrentBrandRevision(orgId,supabase)`→`getCurrentBrandRevisionExec(orgId)`, and — since `countCampaignRecipients` was NOT ported (it's `SupabaseLike`, only this route called it) — `countCampaignRecipients(supabase,{…}).===0` → `(await computeRecipientBreakdownExec({audienceId,orgId,isTransactional})).reachable === 0` (the sibling recipient-count route proved `.reachable` is the byte-for-byte same count). Direct ops → Drizzle: the big `campaign_emails` record read (20-col snake-aliased select feeding `SchedulableEmail` + `data.authoring_mode`/`email_document`; try/catch→null keeps the 404 on a malformed id), the `campaigns` read, two `content_assets` reads (structured-asset `inArray`+org-scoped, header-image `eq`+org-scoped), the `organizations` send-config read, and the **atomic claim** `.update(…).is('inngest_run_id',null)[.eq('updated_at',frozen)].select('id')` → `.where(and(eq(id), isNull(inngestRunId)[, eq(updatedAt)])).returning({id})` — the #358 double-click single-fire guard + the schedule-freeze `updated_at` guard, with `SEND_FAILURE_CLEARED`={last_send_status,send_failed_reason} inlined as camelCase in the `.set()`. **Gate:** whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 93; prettier clean; new substrate harness `send-campaign-check.ts` (9 checks over two orgs — campaign_emails snake read, org send-config read, content_assets org-scoped `inArray` (+ orgB-can't-read tripwire), **atomic claim first-matches-1 / racing-second-matches-0 (single-fire #358)**, updated_at-stale-0-rows / matching-1-row freeze guard) **PASS 0 defects**. **T011 remains open** — 93 gate refs; remaining data-plane route roots ≈4 (email/generate, webhooks/gotowebinar, canva/import (storage=T021), onboarding/provision (profiles drift), checkin-poster (auth=T015)) + lib/ai/client + the T015/T021/T023-gated shims + the 29 `inngest/**` roots.
  - **webhooks/gotowebinar — the GTW webhook receiver (713 lines, 5 fns, 6 tables, rpc), migrated** (`check:no-supabase` 93 → 92, −1). Nineteenth grind item; the largest/hardest remaining route. Signed webhook (HMAC verified, fail-closed); **classified admin `db`**. The 4 internal handlers (`handleRegistrantAdded`/`Joined`/`WebinarCreated`/`Changed`) dropped their `supabase: any` param and use module-level `db`; `recordWebinarRegistrationConversion(supabase, args)` → `(args, exec=db)`. Lib delegations → twins: `isEmailErased`→`isEmailErasedExec`, `resolveIntentfulMatch`→`resolveIntentfulMatchExec`, `validateContactFieldValuesForOrg`→`validateContactFieldValuesForOrgExec`; `resolveConversionInitiativeId` already Drizzle. Direct ops → Drizzle: the `integrations` organizer_key `.limit(2)` three-outcome lookup (Drizzle-throw→503 retryable, preserving the no-silent-drop fix); the `webinars` upsert (`onConflict org_id,webinar_key`) + dynamic-column update; the `webinar_registrants` upsert (`onConflict webinar_id,email`) + attended update; the `contact_activities` **jsonb-path dedup** (`.filter('metadata->>webinar_key','eq',…)` → `sql\`${metadata}->>'webinar_key' = …\``) + inserts; the `contacts` tags[]+custom_fields update; the `campaign_events` read; and the `record_webinar_registration_conversion` **rpc** via `exec.execute(sql\`select …(p_org_id => …::uuid, …)\`)` (named args). **LATENT RUNTIME BUG FIXED (coupled to the migration):** the route called `createContactWithDefaults(supabase, orgId, …)` but that helper had ALREADY been migrated to `createContactWithDefaults(exec, orgId, …)` — so the new-contact GTW path was passing a supabase client where a Drizzle exec was expected (only typecheck-safe because the handler's `supabase` was `any`); the new-contact registration path was broken at runtime. Now passes `db` — fixed. **Test:** added `jest.mock('@/db/client',{db:{}})` so the file loads; rewrote the 2 `recordWebinarRegistrationConversion` cases to the `(args, exec)` seam (2 pass); the POST supabase-mock cases are deferred to the consolidated jest-rewrite task (harness is the interim verification). **Gate:** whole-project `tsc --noEmit` 0 errors; `check:no-supabase` 92; prettier clean; new substrate harness `gotowebinar-check.ts` (14 checks over two orgs — organizer_key lookup, webinars upsert idempotent + in-place update, webinar_registrants upsert + attended flip, jsonb metadata->>webinar_key dedup, tags[]+custom_fields update, campaign_events read, **rpc records exactly 1 conversion across 2 calls (idempotent Δ=1)**, org-scope tripwire) **PASS 0 defects**. **T011 remains open** — 92 gate refs; remaining data-plane route roots ≈3 (email/generate (866), canva/import (storage=T021), onboarding/provision (profiles drift), checkin-poster (auth=T015)) + lib/ai/client + the T015/T021/T023-gated shims + the 29 `inngest/**` roots.
- [ ] T012 [US1] Remove all `supabase-js` imports from `src/**` and `inngest/**`; `scripts/check-no-supabase.sh` green (FR-003, SC-005)
- [ ] T013 [US1] Remove Supabase Realtime client code; audit for zero runtime dependency (FR-009)
- [ ] T014 [US1] `npm run typecheck && npm test` pass against the substrate (SC-001)

**Checkpoint**: data plane fully on Drizzle/substrate.

---

## Phase 4: User Story 3 - Enterprise-grade authentication via Auth0 (Priority: P1)

**Goal**: Auth0 (dev tenant) login + local user mirror. Produces the identity US2's RLS session consumes — **must complete before US2**.

**Independent Test**: auth E2E flows pass against the dev tenant; login upserts the mirror (SC-003).

- [ ] T015 [US3] Integrate `@auth0/nextjs-auth0` v4 (`proxy.ts` on Next 16) across the 51 auth touchpoints in `lib/auth/`, configured against the **Auth0 dev tenant** via `.env.local` (FR-004, contract `auth-user-mirror.md`)
- [ ] T016 [US3] Create the local user mirror table + upsert-on-login sync; repoint the 8 user-referencing tables' FKs to the mirror, in `db/schema.ts` + `lib/auth/sync.ts` (FR-005, research R7)
- [ ] T017 [US3] Auth E2E tests (sign-up, login, logout, refresh, MFA, reset) + a mirror↔Auth0 reconciliation check in `tests/auth/` (SC-003)

**Checkpoint**: auth live on the dev tenant, mirror in sync — identity available for RLS.

---

## Phase 5: User Story 2 - Multi-tenant isolation preserved (Priority: P1) 🔒 gate

**Goal**: No cross-tenant read/write, enforced in the DB. **Depends on US3 identity (T015, T016) and the RLS primitive (T008).**

**Independent Test**: `tests/isolation/` green on the substrate — zero cross-tenant, no pool bleed (SC-002).

- [ ] T018 [US2] Repoint the ~150 RLS policies to `current_setting('app.user_id')` (replacing `auth.uid()`), applied to the substrate DB, in `db/migrations/` (FR-006)
- [ ] T019 [US2] Build the tenant-isolation suite (cross-tenant deny per table, pooled-connection bleed, policy-count parity) in `tests/isolation/` (contract `rls-session.md`)
- [ ] T020 [US2] Run the isolation suite on the substrate → zero cross-tenant reads/writes; **this is 002's security gate** (NFR-001, SC-002)

**Checkpoint**: isolation proven on the substrate.

---

## Phase 6: User Story 4 - Files through the Azure Blob client on Azurite (Priority: P2)

**Goal**: `@azure/storage-blob` against Azurite; ~9 call sites migrated. (Real object transfer = 004.)

**Independent Test**: upload/download/delete work against Azurite; no Supabase storage refs (contract `storage.md`).

- [ ] T021 [US4] Azure Blob client (`@azure/storage-blob` against `AZURE_STORAGE_CONNECTION_STRING` → Azurite) + migrate the ~9 call sites in `lib/storage/` (FR-007)
- [ ] T022 [US4] Verify upload/download/delete against Azurite; remove all Supabase storage-client references; CI no-supabase check green (FR-007, SC-005)

**Checkpoint**: storage on the Azure Blob client, proven on Azurite.

---

## Phase 7: User Story 5 - Background jobs repointed to the substrate (Priority: P2)

**Goal**: 47 Inngest functions run against the substrate; only the connection changes.

**Independent Test**: all 47 execute (scheduled + event) against the substrate (SC-006).

- [ ] T023 [US5] Repoint all 47 Inngest functions' DB access to the Drizzle/local client in `inngest/**` (FR-008, A2)
  - **crons batch 1 — 5 functions off supabase** (`check:no-supabase` 92 → 87, −5). Two kinds:
    - **(a) 3 self-contained** (only supabase touch was the service-role `createClient` + inline reads/writes, no lib coupling): **`contact-memory-backfill.ts`** (paged `contacts` read → admin `db.execute`, byte-identical org_id/deleted_at/created_at-asc predicates + `limit/offset` for the old `.range()`), **`ai-drafts-cleanup.ts`** (platform-wide `ai_generation_drafts` DELETE-with-returning; the `.limit(10000)` cap preserved via a `ctid in (… limit 10000)` subquery since PG DELETE takes no LIMIT), **`non-gtw-auto-finalize.ts`** (the `webinars` scan → admin `db`, fail-soft `[]` on read error preserved; rest was already a fetch to the migrated `finalize` route — its jest test rewritten to mock `@/db/client` + render the drizzle `sql` via `PgDialect().sqlToQuery` to assert the same predicates).
    - **(b) 2 twin-backed repoints** — the shared lib helper's exec-seam `.drizzle` twin ALREADY existed, so the cron just repoints onto it: **`industry-template-reconcile.ts`** (→ `provisionIndustryTemplateExec` + a `db.execute` candidate load; DI seam changed from a `SupabaseClient` to a Drizzle `exec`, and its jest test migrated to match — 3/3 green), **`mailgun-event-reconcile.ts`** (→ `processRecordedEventExec(row, db)` + a `db.execute` `email_events` scan; `EmailEventRow` type kept as an erased `import type` from the supabase original, `MAX` re-declared locally to stay supabase-free — exactly the pattern the already-migrated `webhooks/mailgun` route uses).
    - All **classified admin `db`** (Inngest bypasses RLS → plain pooled `db`, no `withUserId`). Verified: `npm run typecheck` GREEN; full `src/inngest` jest suite GREEN (23 suites / 262 tests); every new SQL statement executed clean against the 001 substrate (the DELETE inside a rolled-back tx so it mutates nothing).
  - **crons batch 2 — 4 more files / 7 functions off supabase** (`check:no-supabase` 87 → 83, −4). **`suppression-sync.ts`** (twin-backed → `syncSuppressionForOrgExec`; DI seam `SupabaseClient`→`exec`, `organizations` load → `db.execute`; DI test migrated, 2/2), **`org-notifications.ts`** (pure repoint of 2× `sendManagedSystemEmail` + `systemSendTargetForOrg` → the `system-notify.drizzle` `…Exec` twins; its pure-fn `__tests__` suite got `jest.mock('@/db/client')` so the new import chain loads), **`gtw-functions.ts`** (2 crons: `webinars` `.or(...)` scan → `(avg_interest_rating is null or updated_at < …)`, `oauth_states` DELETE-returning; test rewritten to render the `sql` via `PgDialect`), **`overage-invoicer.ts`** (3 functions: `selectDueCharges`/`selectDueReconciles` inline `.or` PostgREST → parameterised SQL, and the reconcile+worker handlers repointed to `reconcileOverageChargeByIdExec`/`runOverageChargeWorkerExec`; `OVERAGE_MAX_ATTEMPTS` re-declared locally to stay supabase-free; 8-case sweep test rewritten to assert on rendered SQL + bound params). Verified: `npm run typecheck` GREEN; full `src/inngest` jest GREEN (23 suites / 262 tests); all 5 new SQL statements executed clean on the 001 substrate (the oauth DELETE inside a rolled-back tx).
  - **crons batch 3 — 2 more files / 3 functions off supabase** (`check:no-supabase` 83 → 81, −2). **`forms-v2-enrolment.ts`** (self-contained: `captured_forms` load keyed by (id, org_id) + the `form_submissions` mapping-errors read/update → `db.execute`; the jsonb `mapping_errors` write binds a JSON string with `::jsonb` cast; the issue-marker helper preserves the original's "discard the read error, still attempt the update" behaviour; no test), **`insights-v2-events.ts`** (2 crons: the `insights_v2_detector_runs` insert→succeeded/failed lifecycle → `db.execute` keyed by (id, org_id), the org fanout select → `db.execute`, and the three helper calls repointed to `loadEventDetectorInputsExec`/`upsertInsightsV2RecommendationExec`/`resolveInsightsV2RecommendationExec`; `refreshInsightsV2EventsForOrg` signature `SupabaseClient`→`exec` — no external callers; no test). Verified: `npm run typecheck` GREEN; full `src/inngest` jest GREEN (23/262); new SQL valid on the 001 substrate.
  - **remaining T023 = 18 Inngest files; the path is twin-first, cron-second (NOT a flat sweep).** Cumulative 92 → 81 (11 files / 15 functions migrated). Most remaining crons thread a client into a shared lib helper — some helpers ALREADY have exec-seam twins → quick repoints (`insights-v2-events`→`loader`/`repository.drizzle`, `forms-v2-notifications`→`system-notify.drizzle`, `overage`-style billing); others need the **helper migrated first**: `audience-reconciler`→`lib/audienceReconciler` (untyped `supabase: any`, no twin yet), `content-storage-cleanup`→Supabase **storage** (needs the T021 Blob client, cross-plane). The big single-file jobs (`functions.ts` 2402, `journey-functions.ts` 2198, `salesforce-sync.ts` 1447, `journey-email-batch.ts` 1241) are each their own vertical.
- [ ] T024 [US5] Verify all 47 functions execute against the substrate for scheduled and event-driven triggers (SC-006)

**Checkpoint**: jobs healthy on the substrate.

---

## Phase 8: Polish & Cross-Cutting

- [ ] T025 [P] Wire `scripts/check-no-supabase.sh` into CI as a required gate (SC-005)
- [ ] T026 [P] Document in `repostories/orbit/CLAUDE.md` that data access is Drizzle-on-Postgres + Auth0 + Blob, developed on the 001 substrate, and that the real Azure landing + production cutover are spec 004
- [ ] T027 Final validation: on a clean substrate (`local:down && local:up && db-replay`), run the full suite + isolation + auth + no-supabase checks green (data/storage/RLS plane offline; auth via the Auth0 dev tenant)

---

## Deferred to spec 004 (real Azure + production cutover)

Explicitly **not** in 002 (tracked for the Azure-implementation spec, seeded by `_archive/001-azure-host`):

- Terraform: VNet + subnets, PG Flexible Server (private access, Entra auth), Storage Account + private endpoint, Key Vault, user-assigned Managed Identity + role assignments.
- Auth0 **production** tenant provisioning (region decision, DPA/SCCs), Key Vault secret wiring, Managed-Identity DB/Blob/KV auth (no passwords).
- Data sync Supabase→Azure with per-table row-count + checksum parity; the real bucket object transfer + checksums.
- Parallel-run cutover, rehearsed rollback, Supabase decommission after N read-only days.
- p95 latency baseline on Supabase + Azure re-measure; 48h post-cutover Inngest watch.
- `terraform fmt -check`/`validate` CI gate.

---

## Dependencies & Execution Order

- **Setup (P1)** → **Foundational (P2)** blocks everything. T004 (replay shim) before T005; T005 before Drizzle introspection (T007).
- **US1 (P1)** needs Foundational (Drizzle + schema). Runs parallel with US3.
- **US3 (P1, Phase 4)** needs Foundational. **US2 (P1, Phase 5) depends on US3** — the RLS session (T008) consumes the validated Auth0 identity (T015) + mirror (T016), so **auth is sequenced before isolation**.
- **US4 (P2)** and **US5 (P2)** need Foundational; independent of US1/US2/US3, can run parallel.
- Critical path: Setup → Foundational → US1 ∥ US3 → US2 (gated on US3) → fold in US4/US5 → T027.

## Parallel Opportunities

- Setup: T002, T003 together.
- Cross-story once Foundational is done: US1 (rewrite) ∥ US3 (auth) ∥ US4 (storage) ∥ US5 (Inngest). **US2 waits on US3.**
- The batched rewrite (T011) is partitioned by module/table (T010) for heavy parallel automation.

## Notes

- The isolation, auth, and trigger-behaviour suites are acceptance criteria, not optional TDD.
- `db-replay.sh` (T004) is `$DATABASE_URL`-parameterized: identical shim+replay runs locally (002) and against Azure (004) — do not fork it.
- The Blob client (T021) is the **same** `@azure/storage-blob` the Azure target uses; 001's smoke already proved it works against Azurite — 002 just wires the app's ~9 call sites onto it.
- The data/storage/RLS plane runs offline on the 001 substrate — no Azure account, no VPN. The one network dependency is the **Auth0 dev tenant** for auth flows (US3/T017).

## Phase 9: Convergence

- [X] T028 Reconcile the spec artifacts with the verified replay outcome: the clean 283/283 replay measured **169 base tables + 2 views** at head — the "88" in FR-002/SC-004 is the `initial_schema` (Apr-30 dump) baseline (a subset), not the head count — and the clean replay **required 11 migration-history fixes** (1 lost-merge restore `marketing_framework_tables`, 1 duplicate delete `event_briefs _01`, 2 create-before-use reorders, 6 content fixes; orbit commit `b633572a`), which SC-004's "no manual fix-ups" does not reflect. Update `data-model.md` + `spec.md` (FR-002, SC-004) so the artifacts match measured reality and the 004 Azure replay inherits the same fixes per SC-004, FR-002 (contradicts)

---

## Phase 10: Convergence

- [X] T029 Fix the replayed `profiles_guard_role_org_change` BEFORE-UPDATE trigger so `profiles` writes succeed on the 001 substrate: its pg_dump-derived body tests `"current_user"` (double-quoted), which on stock PostgreSQL 17 resolves as a reference to a non-existent column rather than the `current_user` session-user function, so every `profiles` UPDATE that fires the trigger errors on the substrate (blocking onboarding/team/contacts write paths). Correct the identifier to the `current_user` function (or the `SET LOCAL app.user_id` equivalent under the T018 RLS repoint) in the replay/migration history and add a profiles-write behaviour check, so the migrations replay to a behaviourally-correct schema and the identical fix carries to the 004 Azure replay — per FR-010, US1/AC2, FR-002 (contradicts) — **DONE**: added forward migration `supabase/migrations/20260728010000_fix_profiles_guard_current_user.sql` that `CREATE OR REPLACE`s the function with the UNQUOTED `current_user` function (only that one token changes vs 20260714200000; idempotent, safe to replay on prod/004). Verified live on the substrate (pg17 :5435): BEFORE — a benign `UPDATE profiles SET full_name=…` failed with `column "current_user" does not exist` at the trigger's line-8 IF; AFTER applying the migration — the same benign update succeeds (`UPDATE 1`) AND the security guard is preserved (a non-admin `role`-change still RAISEs `Only an org admin can change role or org_id on a profile`). `tsc --noEmit` clean; full jest 701 suites/6316 tests green; `check:no-supabase` unchanged (131).
- [X] T030 Resolve the `integrations.metadata` write drift in `src/app/api/integrations/linkedin/callback/callback.drizzle.ts`: the upsert writes a `metadata` jsonb key that exists in NO migration and is absent from the substrate `integrations` table (16 columns, verified live), so on the substrate the write raises 42703 and is swallowed and the LinkedIn grant metadata is never persisted (byte-identical to the prior PostgREST PGRST204 no-op). Either drop the out-of-migration `metadata` write so app code matches the 283-migration schema source of truth, or record an explicit schema-owner/004 decision to add the column via a forward migration; add a persistence check for whichever path is chosen — per FR-002, NFR-002 (partial) — **DONE (dropped, per NFR-002)**: the `metadata` key made the WHOLE upsert 42703-fault → the LinkedIn grant (access_token/refresh_token/expires_at/organizer_key) never persisted on the substrate at all, and NO app code reads `integrations.metadata` (write-only dead data, verified full-tree). Dropped `metadata` from the twin's INSERT/values/DO-UPDATE-SET + the `LinkedinIntegrationUpsert`/`LinkedinIntegrationMetadata` types; dropped the `metadata:{…}` payload + the now-dead userinfo fetch from `callback/route.ts`; reworked `scripts/local/linkedin-callback-check.ts` to drop the old add/drop-`metadata`-column bridge. Verified live: the harness now passes NATIVELY with no bridge (9/9, 0 defects) — the grant persists, conflict-updates in place, and the cross-tenant token tripwire holds. `tsc --noEmit` clean; full jest green; `check:no-supabase` unchanged (131). FOLLOW-UP for the schema owner / 004: if LinkedIn member metadata is genuinely wanted, add `integrations.metadata jsonb` via a forward migration + `src/db/schema.ts` and re-add the write (noted in the twin header) — do not reintroduce the write without the column.
- [X] T031 Close the deferred `webinars.description_override` schema-drift decision flagged in T011: the write was dropped and `src/lib/ai/context/event.drizzle.ts` synthesizes `description_override: null` so the `?? description` read always falls back (functionally safe), but the column exists in no migration/schema/substrate and the drift between app intent and the migration source is not formally closed. Retire the residual `description_override` fallback references (in scope: a call-site cleanup), or record an explicit schema-owner/004 decision to (re)add the column via a forward migration so the write persists editorial copy — per FR-002, NFR-002 (partial) — **DONE (already retired at code level in T011; verified)**: assessment found the CODE-level drift is already fully resolved — `event.ts:114` reads `event.description` directly (no `?? description_override`) and `event.drizzle.ts` selects only `description` (no synthesized `description_override: null`); a full-tree scan found ZERO non-comment references to `description_override` (the only mentions are explanatory `SCHEMA DRIFT — RESOLVED` comments in `event.drizzle.ts` + `commit-ai-draft/route.ts`, which correctly document the resolved state). No residual fallback exists to retire, so the in-scope resolution is satisfied. Verified: `scripts/local/event-context-check.ts` PASS (0 defects); `tsc --noEmit` clean; full jest green. The only open item is the schema-owner/004 DECISION on whether to (re)add the `webinars.description_override` column via a forward migration to persist editorial copy — deferred to the owner (a schema change, out of 002's mechanical-schema scope per Out-of-Scope #3); the app is functionally correct without it.
